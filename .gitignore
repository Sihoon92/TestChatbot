# 로컬 도구가 만드는 것 — worktree 체크아웃이 들어와 저장소가 뒤엉킨다.
.claude/

# 루트에서 dev.py 를 돌리면 생긴다. backend/.gitignore 는 backend/ 만 덮는다.
__pycache__/

# 실데이터가 섞인 설명 문서는 여기서 막지 않는다. 무엇을 공개할지는 사람이
# 파일마다 판단할 일이고, 규칙으로 숨기면 판단 자체가 사라진다.
