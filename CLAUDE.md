# 프로젝트 작업 규칙 (TestProject)

## 설정값은 항상 `.env` 를 단일 출처로 참조한다 (필수)

**모든 기본 세팅값(LLM 백엔드/URL/모델/키, 포트, 로그 경로, SSL 등)은 `backend/.env` 에서 읽는다.**
코드·스크립트·예제·테스트 어디서도 이 값들을 하드코딩하지 않는다.

### 규칙
- **설정 접근은 `app.config.get_settings()` 로 한다.** `Settings` 는 cwd 와 무관하게 항상
  `backend/.env` 를 읽도록 절대경로로 고정돼 있다(`backend/app/config.py`). 새 스크립트도
  이 경로를 재사용하면 어디서 실행하든 같은 설정을 본다.
- **`get_settings()` 를 못 쓰는 순수 예제(app 미의존)** 는 `backend/examples/connect_llm.py`
  의 `load_env_file()` 처럼 `backend/.env` 를 명시적으로 로드한다. cwd 상대경로(`".env"`)에
  의존하지 않는다.
- **하드코딩 금지 대상**: `INTERNAL_LLM_BASE_URL`, `INTERNAL_LLM_MODEL`, `INTERNAL_LLM_API_KEY`,
  `OLLAMA_*`, `BACKEND_PORT`, `DEBUG_LOG_*`, SSL/프록시 관련 값. 기본값이 필요하면
  `.env` 또는 `.env.example` 에 정의하고 거기서 읽는다.
- **런타임 오버라이드가 필요하면** `settings.model_copy(update={...})` 로 복제해 쓴다
  (예: 특정 스크립트에서 `llm_backend` 강제). 원본 설정/`.env` 는 건드리지 않는다.
- **새 설정 항목을 추가할 때** `Settings` 필드 + `backend/.env.example` 에 기본값을 함께 넣어,
  `.env` 가 항상 "무엇을 설정할 수 있는지"의 문서 역할을 하게 한다.

### 이유
실행 위치(cwd)나 환경마다 설정이 달라지면 "왜 안 되는지" 디버깅이 폭발한다(과거 포트/백엔드
불일치로 오래 헤맴). 설정의 단일 출처를 `.env` 로 고정하면 재현성과 이식성이 보장된다.

## 실데이터는 Claude 가 볼 수 없다 — 사용자가 실행할 수 있는 형태로 준다 (필수)

**이 저장소는 사내 프로젝트다. 실제 데이터(`backend/data/`, 사업부 CSV·XLSX·parquet)는
사내 PC 에만 있고 Claude 는 절대 열어볼 수 없다.** Claude 가 가진 것은 코드와
`backend/tests/fixtures/` 의 합성 샘플뿐이다.

### 규칙
- **"경로를 주면 돌려 보겠다" 고 하지 않는다.** 그 파일은 오지 않는다. 대신 사용자가
  자기 PC 에서 **그대로 붙여넣어 실행할 명령**과, 그 출력을 **어떻게 읽을지**를 준다.
- **결과 해석을 사용자에게 떠넘기지 않는다.** 숫자를 받아 적는 것이 아니라, 나올 수 있는
  경우를 미리 갈라 **"이 값이면 이 뜻이고 다음은 이것"** 까지 적어 준다. 판정 분기표가
  기본 산출물이다.
- **검증은 합성 데이터로 한다.** 실데이터 형태(결측 zone·이벤트 수·항목 구성)를 사용자
  진술에서 받아 합성으로 재현하고, 그 위에서 코드가 맞게 도는 것을 보인 뒤 넘긴다.
  "돌려 봐야 안다" 로 끝내지 않는다.
- **사용자가 돌려줄 것을 최소화한다.** 요청은 파일이 아니라 **출력 텍스트 몇 줄**이어야
  한다(리포트 md 의 해당 절, `--dump` CSV 의 요약). 원본 데이터를 달라고 하지 않는다.
- **실패도 산출물로 만든다.** 코드가 예외로 죽으면 사용자는 원인을 못 돌려준다.
  진단은 문장으로 출력되게 하고(트레이스백 금지), 그 문장만 붙여넣으면 다음 판단이
  가능하도록 짠다. `merge.py`·`report.py`·`convert.py` 의 기존 관례가 이것이다.

### 이유
Claude 가 데이터를 못 보는 것은 일시적 불편이 아니라 이 프로젝트의 고정 조건이다.
"파일을 주면 봐 주겠다" 는 제안은 매번 막히고, 그때마다 왕복이 한 번씩 낭비된다.
처음부터 **사용자가 혼자 실행하고 혼자 판단할 수 있는 형태**로 내놓는 것이 유일하게
작동하는 방식이다.
