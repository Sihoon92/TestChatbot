# 관리대장 시트-금형 귀속 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 관리대장(EES)에서 금형을 확정하는 키를 "행의 JIG ID 열"에서 "워크북의 실제 시트 이름"으로 바꾼다.

**Architecture:** 귀속 규칙은 `join.py` 에 둔다(파서는 계속 귀속하지 않는다). 실제 시트명이 LLM 을 거치지 않도록 `parse_rows` 가 시트명을 필수 인자로 받고, 그 위에서 레이아웃 캐시를 kind 로만 조회해 같은 양식 시트가 LLM 발견을 한 번만 돌게 한다. `SheetLayout` 스키마는 바뀌지 않는다.

**Tech Stack:** Python 3.12 / pydantic v2 / pytest / sqlite3 / React + TypeScript + Vitest / xlwings(샘플 생성만)

## Global Constraints

- 설계 원본: `docs/superpowers/specs/2026-07-30-관리대장-시트-금형-귀속-design.md`
- 실물 관리대장: 시트 하나 = 금형 하나, 시트 이름이 JIG ID(`#RX39513` 꼴, 부가 텍스트 없음), 시트 안에 JIG ID 열 **없음**, 파일 내 모든 시트가 금형 시트, 시트 수 10개 미만.
- **JIG ID 열 경로는 지원하지 않는다.** 열이 있어도 무시한다.
- `normalize_mold_no` 는 그대로 쓴다 — `#` 접두사와 대소문자를 이미 처리한다(`#RX39513` → `RX39513`). `_NOT_A_MOLD = {"소계","합계","총계","계","-","n/a","na"}` 는 `None` 이 된다.
- `SheetLayout` 스키마에 필드를 추가하지 않는다.
- `assemble.py` 의 IQC/PQC 귀속은 건드리지 않는다.
- 손실 집계는 `extract_runs` 한 곳에서만 한다. `latest_locations` 는 같은 시트를 다시 세지 않는다.
- 설정값은 하드코딩하지 않는다(프로젝트 `CLAUDE.md`). 이 계획에는 새 설정 항목이 없다.
- 백엔드 테스트: `cd backend && ./.venv/Scripts/python.exe -m pytest`
  **반드시 venv 인터프리터를 쓴다.** 맨 `python` 은 anaconda 로 잡혀
  `langchain_ollama`·`langgraph` 가 없어 8개 모듈이 **수집 단계에서 조용히
  빠진다**(301개 중 81개만 돈다). 그러면 초록불이 거짓이 된다.
  전부 통과 시 `301 passed` 가 기준선이다.
- 프론트 테스트: `cd frontend && npm test -- --run`
- 커밋 메시지는 한국어, 본문에 **왜** 를 적는다(기존 이력과 같은 형식).

## 파일 구조

| 파일 | 책임 | 태스크 |
|---|---|---|
| `backend/app/ingest/parser.py` | 격자+레이아웃 → Row. 실제 시트명을 Row 에 심는다 | 1 |
| `backend/app/ingest/pipeline.py` | 시트 순회. 시트명 전달, 캐시 조회, 요약 조립 | 1, 2, 4 |
| `backend/app/ingest/registry.py` | 레이아웃 캐시 조회/저장, 배치 요약 영속화 | 2, 4 |
| `backend/app/ingest/join.py` | **관리대장 귀속 규칙**(시트명 → 금형) + 손실 | 3 |
| `backend/app/ingest/schemas.py` | `EES_FIELDS`, `RunSummary` | 3, 4 |
| `backend/app/ingest/db.py` | `ingest_run` 스키마 + 컬럼 추가 마이그레이션 | 4 |
| `backend/app/ingest/discover.py` | EES 발견 프롬프트, 커버리지 검증 | 6 |
| `frontend/src/api/ingest.ts` | `RunSummary` 타입 | 5 |
| `frontend/src/components/Dashboard/IngestPanel.tsx` | 손실 표시 | 5 |
| `backend/examples/make_sample_uploads.py` | 샘플 관리대장 생성 | 7 |
| `backend/examples/check_samples.py` | LLM 없는 기대값 계산기 | 7 |

**태스크 순서가 중요하다.** Task 1 이 Task 2 보다 먼저다 — 캐시를 kind 로만 조회하면 시트 A 의 레이아웃이 시트 B 에 재사용되는데, Task 1 없이 그러면 `Row.sheet` 가 A 의 이름이라 모든 금형이 A 로 뭉친다.

---

### Task 1: `parse_rows` 가 실제 시트명을 받는다

`Row.sheet` 가 LLM 이 적어낸 `layout.sheet_name` 인 것을 끊는다. 이 값이 곧 금형 정체성이 되므로, LLM 오기나 캐시 재사용이 오귀속으로 이어져선 안 된다.

**Files:**
- Modify: `backend/app/ingest/parser.py`
- Modify: `backend/app/ingest/pipeline.py:69,80`
- Test: `backend/tests/test_ingest_parser.py`

**Interfaces:**
- Produces: `parse_rows(grid: list[list], top_left: str, layout: SheetLayout, source_file: str, sheet_name: str) -> list[Row]` — `sheet_name` 은 워크북의 실제 시트명이며 **필수**다. `Row.sheet` 가 이 값이 된다.
- Produces: `_table_rows(grid, top_left, table, defaults, layout, source_file, sheet_name)` (모듈 내부).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_ingest_parser.py` 끝에 추가:

```python
def test_row_sheet_is_the_real_sheet_name_not_the_layout_echo():
    """layout.sheet_name 은 LLM 이 적어낸 값이고, 캐시된 레이아웃이 다른 시트에
    재사용되면 남의 시트 이름이 들어 있다. 관리대장에서는 시트 이름이 곧
    금형번호라, 그대로 믿으면 모든 금형이 한 시트로 뭉친다."""
    grid = [
        ["No", "위치"],
        [1, "설비"],
    ]
    layout = SheetLayout(
        sheet_name="#RX00000",  # 캐시에서 딸려온 남의 시트 이름
        anchors=ANCHOR,
        tables=[TableBlock(
            name="이력", role="detail", header_rows=[1], data_start_row=2,
            columns=[ColumnMap(field="location", column="B")],
        )],
    )

    rows = parse_rows(grid, "A1", layout, "ledger.xlsx", "#RX39513")

    assert [r.sheet for r in rows] == ["#RX39513"]
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest tests/test_ingest_parser.py::test_row_sheet_is_the_real_sheet_name_not_the_layout_echo -v`

Expected: FAIL — `TypeError: parse_rows() takes 4 positional arguments but 5 were given`

- [ ] **Step 3: `_table_rows` 에 인자를 더한다**

`backend/app/ingest/parser.py` — 시그니처:

```python
def _table_rows(
    grid: list[list],
    top_left: str,
    table: TableBlock,
    defaults: dict[str, str | None],
    layout: SheetLayout,
    source_file: str,
    sheet_name: str,
) -> list[Row]:
```

두 `raise ValueError` 메시지에서 `layout.sheet_name` → `sheet_name`:

```python
        raise ValueError(
            f"detail 표 '{table.name}' 에 컬럼 매핑이 없다 "
            f"(시트 '{sheet_name}', {source_file})"
        )
```

```python
        raise ValueError(
            f"표 '{table.name}' 의 data_end_row({table.data_end_row}) 가 "
            f"data_start_row({table.data_start_row}) 보다 작다 "
            f"(시트 '{sheet_name}', {source_file})"
        )
```

`Row(...)` 생성에서 `sheet=layout.sheet_name` → `sheet=sheet_name`.

- [ ] **Step 4: `parse_rows` 에 인자를 더한다**

```python
def parse_rows(
    grid: list[list],
    top_left: str,
    layout: SheetLayout,
    source_file: str,
    sheet_name: str,
) -> list[Row]:
    """detail 표들에서 행을 뽑는다.

    role="summary" 인 표는 건너뛴다. 실물 IQC 시트에는 summary 표가 두 개 있고,
    파싱하면 '소계'/'총계'가 금형번호로 읽힌다.

    표가 하나도 없으면(성적서형) 키-값 블록만으로 한 행을 만든다.

    `sheet_name` 은 **워크북의 실제 시트명**이다. layout.sheet_name 을 쓰지
    않는 이유: 그 값은 LLM 이 적어낸 것이고, 캐시된 레이아웃이 다른 시트에
    재사용되면 남의 시트 이름이 들어 있다. 관리대장은 시트 이름이 곧
    금형번호라 그대로 믿으면 모든 금형이 한 시트로 뭉친다.
    """
    defaults = _key_value_defaults(grid, top_left, layout)
    detail_tables = [t for t in layout.tables if t.role == "detail"]

    if not detail_tables:
        if not defaults:
            return []
        return [
            Row(
                source_file=source_file,
                sheet=sheet_name,
                row_no=1,
                values=defaults,
            )
        ]

    rows: list[Row] = []
    for table in detail_tables:
        rows.extend(
            _table_rows(
                grid, top_left, table, defaults, layout, source_file, sheet_name
            )
        )
    return rows
```

- [ ] **Step 5: 새 테스트가 통과하는지 확인한다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest tests/test_ingest_parser.py::test_row_sheet_is_the_real_sheet_name_not_the_layout_echo -v`

Expected: PASS

- [ ] **Step 6: 기존 호출부 15곳을 고친다**

`backend/tests/test_ingest_parser.py` 의 기존 `parse_rows(...)` 호출은 **15곳 전부 `sheet_name="Sheet1"` 인 레이아웃**을 쓴다(확인 완료). 그러므로 일괄 치환이 안전하다:

```bash
cd backend && python - <<'PY'
import pathlib
p = pathlib.Path("tests/test_ingest_parser.py")
s = p.read_text(encoding="utf-8")
before = s.count('layout, "iqc.xlsx")')
s = s.replace('layout, "iqc.xlsx")', 'layout, "iqc.xlsx", "Sheet1")')
p.write_text(s, encoding="utf-8")
print("치환:", before)
PY
```

Expected: `치환: 15`

이러면 기존 단언(`rows[0].sheet == "Sheet1"`)이 그대로 성립한다. `top_left="C3"` 인 호출(210행)도 같은 형태라 함께 치환된다.

- [ ] **Step 7: 파이프라인 두 호출부를 고친다**

`backend/app/ingest/pipeline.py` 의 `_read_file` 안, 69행과 80행 **둘 다**:

```python
                parsed = parse_rows(grid, top_left, layout, found.path, sheet_name)
```

`sheet_name` 은 56행 `for sheet_name in wb.sheet_names():` 의 순회 변수다.

- [ ] **Step 8: 백엔드 전체 테스트를 돌린다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest`

Expected: 전부 PASS. 실패가 남으면 인자를 빠뜨린 호출부다.

- [ ] **Step 9: 커밋**

```bash
git add backend/app/ingest/parser.py backend/app/ingest/pipeline.py backend/tests/test_ingest_parser.py
git commit -F - <<'EOF'
fix(ingest): 행의 시트 이름을 LLM 이 적은 값이 아니라 실물에서 받는다

Row.sheet 가 layout.sheet_name 이었다. 그 값은 LLM 이 적어낸 것이라, 관리대장
처럼 시트 이름이 곧 금형번호인 문서에서는 오기가 그대로 금형 오귀속이 된다.

캐시된 레이아웃을 다른 시트에 재사용하기 시작하면 더 나빠진다 — 그 레이아웃의
sheet_name 은 학습한 시트의 이름이라, 모든 금형이 한 시트로 뭉친다.

parse_rows 가 실제 시트명을 필수 인자로 받는다. 기본값을 두면 호출부에서
빠뜨렸을 때 이 사고가 조용히 난다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 2: 레이아웃 캐시를 kind 로만 조회한다

시트 이름이 금형번호가 되면 캐시 키로 쓸 수 없다. 양식이 같은 시트들이 각각 LLM 발견을 돌기 때문이다. 양식 일치 판정은 이미 있는 `anchors_match` 에 맡긴다.

**Files:**
- Modify: `backend/app/ingest/registry.py:57-69`
- Modify: `backend/app/ingest/pipeline.py:60-62`
- Test: `backend/tests/test_ingest_registry.py`, `backend/tests/test_ingest_pipeline.py`

**Interfaces:**
- Consumes: Task 1 의 `parse_rows(..., sheet_name)` — 이것 없이는 이 태스크가 오귀속을 만든다.
- Produces: `registry.load_layouts(conn: sqlite3.Connection, kind: str) -> list[SheetLayout]` — `sheet_name` 인자가 사라진다.

- [ ] **Step 1: 기존 테스트를 새 계약으로 고쳐 쓴다**

`backend/tests/test_ingest_registry.py` 에는 이미 픽스처 `conn` 과 헬퍼 `_layout(text="금형번호", sheet="Sheet1")` 가 있다. `load_layouts` 를 부르는 곳이 네 군데이며 그중 하나는 **시트 필터가 있다는 것 자체를 단언**하므로 지운다.

`test_load_layouts_returns_newest_first` 안의 호출을 고친다:

```python
    layouts = registry.load_layouts(conn, "iqc")
```

`test_old_layouts_are_kept_for_traceability` 안의 호출을 고친다:

```python
    assert len(registry.load_layouts(conn, "iqc")) == 2
```

`test_load_layouts_filters_by_kind_and_sheet` 를 **통째로 아래 두 테스트로 교체**한다:

```python
def test_load_layouts_offers_other_sheets_of_the_same_kind(conn):
    """시트 이름이 금형번호가 되면서 캐시 키로 쓸 수 없어졌다. 양식이 같은
    시트들이 각각 LLM 발견을 도는 것을 막으려면 후보를 kind 로 모아 주고,
    양식 일치 판정은 pick_layout 의 앵커 대조에 맡겨야 한다."""
    registry.save_layout(conn, "ees", _layout(sheet="#RX39513"), "ledger.xlsx")

    found = registry.load_layouts(conn, "ees")

    assert [x.sheet_name for x in found] == ["#RX39513"]


def test_load_layouts_still_separates_kinds(conn):
    """앵커가 우연히 맞을 수 있으므로 단계 경계는 남겨 둔다."""
    registry.save_layout(conn, "iqc", _layout(sheet="Sheet1"), "a.xlsx")
    registry.save_layout(conn, "mes", _layout(sheet="Sheet1"), "b.xlsx")

    assert len(registry.load_layouts(conn, "iqc")) == 1
    assert len(registry.load_layouts(conn, "mes")) == 1
    assert registry.load_layouts(conn, "ees") == []
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest tests/test_ingest_registry.py -k load_layouts -v`

Expected: FAIL — `TypeError: load_layouts() missing 1 required positional argument: 'sheet_name'`

- [ ] **Step 3: `load_layouts` 에서 시트명 필터를 뺀다**

`backend/app/ingest/registry.py`:

```python
def load_layouts(conn: sqlite3.Connection, kind: str) -> list[SheetLayout]:
    """이 단계에서 지금까지 학습한 레이아웃 전부. 최신순.

    시트 이름으로 거르지 않는다. 관리대장은 시트 이름이 곧 금형번호라 시트마다
    이름이 달라, 시트명으로 거르면 양식이 똑같은 시트들이 각각 LLM 발견을
    돈다. 어느 레이아웃이 이 시트에 맞는지는 pick_layout 의 앵커 대조가
    판정한다 — 원래 그러라고 만든 장치다.

    최신순인 이유는 그대로다: 바뀐 양식이 먼저 맞아야 옛 규칙으로 파싱하는
    일이 없다.
    """
    rows = conn.execute(
        """
        SELECT layout_json FROM sheet_mapping
        WHERE kind = ?
        ORDER BY id DESC
        """,
        (kind,),
    ).fetchall()
    return [SheetLayout.model_validate_json(r["layout_json"]) for r in rows]
```

`save_layout` 은 그대로 둔다 — `sheet_name` 은 계속 기록한다(추적용이지 조회 키가 아니다).

- [ ] **Step 4: 파이프라인 호출부를 고친다**

`backend/app/ingest/pipeline.py:60-62`:

```python
            cached = pick_layout(
                grid, top_left, registry.load_layouts(conn, found.kind)
            )
```

- [ ] **Step 5: 레지스트리 테스트가 통과하는지 확인한다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest tests/test_ingest_registry.py -v`

Expected: PASS

- [ ] **Step 6: 캐시 재사용 통합 테스트를 더한다**

`backend/tests/test_ingest_pipeline.py` 의 `FakeWorkbook` 정의 **아래**에 다중 시트 워크북을 더한다:

```python
class MultiSheetWorkbook:
    """시트마다 격자가 다른 워크북. 시트 하나가 문서 단위인 파일을 흉내낸다."""

    def __init__(self, by_sheet: dict):
        self._by_sheet = by_sheet

    def sheet_names(self):
        return list(self._by_sheet)

    def used_values(self, sheet):
        return self._by_sheet[sheet], "A1"

    def range_values(self, sheet, address):
        return self._by_sheet[sheet]

    def column_values(self, sheet, column, max_rows=5000):
        return [r[0] for r in self._by_sheet[sheet]]
```

그리고 파일 끝에 테스트를 더한다:

```python
def test_same_form_sheets_reuse_one_discovered_layout(env, monkeypatch):
    """양식이 같은 시트가 여러 개면 레이아웃 발견은 한 번이어야 한다.
    캐시를 시트명으로 조회하면 시트 수만큼 LLM 이 돈다 — 관리대장은 시트
    이름이 금형번호라 시트마다 이름이 다르다."""
    calls = []

    def _counting_discover(model, wb, kind, sheet_name, *, config=None):
        calls.append((kind, sheet_name))
        return LAYOUTS[kind]

    monkeypatch.setattr(
        "app.ingest.pipeline.discover_layout", _counting_discover
    )

    iqc_sheets = {
        "첫째": [["금형번호", "punch"], ["RX28312", 12.5]],
        "둘째": [["금형번호", "punch"], ["RX28312", 9.0]],
    }

    @contextmanager
    def _open(path):
        kind = _kind_of(path)
        if kind == "iqc":
            yield MultiSheetWorkbook(iqc_sheets)
        else:
            yield FakeWorkbook(GRIDS[kind])

    summary = run_ingest(env, model=object(), open_wb=_open)

    assert summary.status == "ok"
    iqc_calls = [c for c in calls if c[0] == "iqc"]
    assert len(iqc_calls) == 1, f"시트마다 발견을 돌았다: {iqc_calls}"
```

IQC 로 검증하는 이유: IQC 는 금형번호 **열**로 귀속하므로 시트 이름과 무관하다. 이 태스크만으로 초록불이 되어야 하는데, 관리대장으로 하면 Task 3 이 없는 상태에서 시트명 귀속이 아직 안 붙어 있다.

- [ ] **Step 7: 백엔드 전체 테스트를 돌린다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest`

Expected: 전부 PASS

- [ ] **Step 8: 커밋**

```bash
git add backend/app/ingest/registry.py backend/app/ingest/pipeline.py backend/tests/test_ingest_registry.py backend/tests/test_ingest_pipeline.py
git commit -F - <<'EOF'
perf(ingest): 레이아웃 캐시를 시트명이 아니라 단계로 조회한다

관리대장은 시트 이름이 곧 금형번호라 시트마다 이름이 다르다. 캐시를 (단계,
시트명) 으로 조회하면 양식이 똑같은 시트들이 각각 LLM 발견을 돈다 — 금형이
열 개면 발견도 열 번이다.

어느 레이아웃이 이 시트에 맞는지는 pick_layout 의 앵커 대조가 이미 판정한다.
원래 그러라고 만든 장치이므로 시트명 필터를 걷어내고 후보만 넓힌다. 단계
경계는 남긴다 — 앵커가 우연히 맞는 경우까지 열어줄 이유는 없다.

DB 마이그레이션은 없다. 기존 sheet_mapping 행이 그대로 후보가 된다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 3: 관리대장의 금형을 시트 이름이 확정한다

귀속 규칙 본체. `parser.py` 가 선언한 "금형 귀속은 하지 않는다" 원칙을 따라 `join.py` 에 둔다.

**Files:**
- Modify: `backend/app/ingest/join.py`
- Modify: `backend/app/ingest/schemas.py:25-35`
- Test: `backend/tests/test_ingest_join.py`, `backend/tests/test_ingest_pipeline.py`, `backend/tests/test_ingest_assemble.py`

**Interfaces:**
- Consumes: Task 1 의 `Row.sheet`(실제 시트명).
- Produces: `JoinLosses.bad_sheet_names: list[str]` — JIG ID 로 안 읽힌 시트 이름(중복 제거). `extract_runs` 만 채운다.
- `extract_runs`/`latest_locations` 시그니처는 바뀌지 않는다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_ingest_join.py` 에 추가. 이 파일에는 이미 헬퍼 `_row(values, *, sheet="관리대장", file="ledger.xlsx", no=1)` 와 상수 `MASTER` 가 있다.

```python
def test_sheet_name_decides_the_mold_not_a_column():
    """실물 관리대장은 시트 하나가 금형 하나이고 시트 이름이 그 금형의 JIG ID 다.
    시트 안에 JIG ID 열은 없다."""
    jig_index, _ = build_jig_index(MASTER)
    equip_index = build_equipment_index(MASTER)
    rows = [
        _row({"event_at": "2026-07-01 08:00", "location": "설비",
              "equipment": "POU WND10_Stack(1차)_01"}, sheet="#RX39513"),
        _row({"event_at": "2026-07-02 08:00", "location": "내부 수리",
              "equipment": "POU WND10_Stack(1차)_01"}, sheet="#RX39513", no=2),
    ]

    runs, losses = extract_runs(rows, jig_index, equip_index)

    assert [r.mold_no for r in runs] == ["RX39513"]
    assert losses.bad_sheet_names == []


def test_a_mold_no_column_is_ignored_when_it_disagrees_with_the_sheet():
    """규칙이 둘이면 어긋났을 때 무엇이 옳은지 정할 근거가 없다. 시트가 이긴다."""
    jig_index, _ = build_jig_index(MASTER)
    equip_index = build_equipment_index(MASTER)
    rows = [
        _row({"mold_no": "#RX28312", "event_at": "2026-07-01 08:00",
              "location": "설비", "equipment": "POU WND10_Stack(1차)_01"},
             sheet="#RX39513"),
        _row({"mold_no": "#RX28312", "event_at": "2026-07-02 08:00",
              "location": "내부 수리"}, sheet="#RX39513", no=2),
    ]

    runs, _losses = extract_runs(rows, jig_index, equip_index)

    assert [r.mold_no for r in runs] == ["RX39513"]


def test_unreadable_sheet_name_is_reported_by_name():
    """시트명 하나가 깨지면 그 시트의 전 행이 한꺼번에 죽는다. 행 수만 세면
    어느 시트를 고쳐야 하는지 알 수 없다."""
    jig_index, _ = build_jig_index(MASTER)
    equip_index = build_equipment_index(MASTER)
    rows = [
        _row({"event_at": "2026-07-01 08:00", "location": "설비"}, sheet="합계"),
        _row({"event_at": "2026-07-02 08:00", "location": "설비"}, sheet="합계", no=2),
    ]

    runs, losses = extract_runs(rows, jig_index, equip_index)

    assert runs == []
    assert losses.bad_sheet_names == ["합계"], "시트 이름이 한 번만 오른다"
    assert losses.rows_without_mold_no == 2, "행 수는 skipped_rows 로 합산된다"


def test_latest_locations_uses_the_sheet_name_too():
    rows = [
        _row({"event_at": "2026-07-01 08:00", "location": "설비"}, sheet="#RX39513"),
        _row({"event_at": "2026-07-02 08:00", "location": "폐기"},
             sheet="#RX39513", no=2),
    ]

    assert latest_locations(rows) == {"RX39513": "폐기"}
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest tests/test_ingest_join.py -k "decides_the_mold or unreadable_sheet or disagrees or latest_locations_uses" -v`

Expected: FAIL — `AttributeError: 'JoinLosses' object has no attribute 'bad_sheet_names'`, 그리고 시트명을 안 읽으므로 `runs == []`

- [ ] **Step 3: `JoinLosses` 에 필드를 더한다**

`backend/app/ingest/join.py` 의 `JoinLosses`, `unknown_equipment` 선언 **아래**:

```python
    # 시트 이름이 JIG ID 로 안 읽힌 시트(중복 제거). 관리대장은 시트 이름이 곧
    # 금형이라 이름 하나가 깨지면 그 시트의 전 행이 한꺼번에 죽는다. 행 수만
    # 세면 어느 시트를 고쳐야 하는지 알 수 없다.
    bad_sheet_names: list[str] = []
```

`rows_without_mold_no` 주석을 고친다:

```python
    # 시트 이름을 JIG ID 로 못 읽어 어느 금형에도 못 붙인 행 수. 시트 단위
    # 사고이므로 어느 시트인지는 bad_sheet_names 를 본다.
    rows_without_mold_no: int = 0
```

`merge` 의 `unknown_equipment` 루프 **아래**:

```python
        for sheet in other.bad_sheet_names:
            if sheet not in self.bad_sheet_names:
                self.bad_sheet_names.append(sheet)
```

- [ ] **Step 4: `extract_runs` 와 `latest_locations` 를 시트명으로 바꾼다**

`extract_runs` 의 행 순회 첫머리:

```python
    for row in rows:
        mold_no = normalize_mold_no(row.sheet)
        if mold_no is None:
            losses.rows_without_mold_no += 1
            if row.sheet not in losses.bad_sheet_names:
                losses.bad_sheet_names.append(row.sheet)
            continue
```

(`seen` 키와 그 아래 로직은 그대로 둔다.)

`latest_locations` 안:

```python
        mold_no = normalize_mold_no(row.sheet)
```

`latest_locations` docstring 끝에 한 줄 더한다:

```python
    손실은 세지 않는다. extract_runs 가 같은 행을 이미 봤고, 여기서 또 세면
    같은 시트가 두 번 보고된다.
```

- [ ] **Step 5: 모듈 docstring 과 `EES_FIELDS` 를 고친다**

`backend/app/ingest/join.py` 모듈 docstring 의 "## 왜 세 소스가 필요한가" 절과 다이어그램:

```
관리대장은 **시트 이름**으로 어느 금형인지 알려주지만(시트 하나가 금형 하나다)
그 금형이 얼마나 불량을 냈는지는 모른다. MES 는 실적을 알지만 **금형번호를
모른다**. 기준정보가 둘 사이에서 MES 조회 키(설비코드·Line명)를 내준다:

    관리대장 시트명 ─────────────────→ 금형 확정
    관리대장.설비명 ─→ 기준정보[설비명] ─┐
                        (없으면 폴백)     ├─→ 설비코드 + Line명 ─→ MES
    관리대장 시트명 ─→ 기준정보[JIG ID] ─┘
```

"## 식별과 조회를 왜 나누는가" 절 첫 문장의 "**JIG ID 하나**다" 를 "**시트 이름 하나**다" 로 바꾼다. 나머지 두 항목(설비 이동, 폴백)은 그대로 참이다.

`extract_runs` docstring 의 "**JIG ID 로 묶어서 짝짓는다.** 한 시트에 여러 금형의…" 문단을 교체한다:

```
    **시트 이름이 확정한 금형으로 묶어서 짝짓는다.** 시트 하나가 금형 하나라
    보통은 시트 경계와 같지만, 관리대장이 여러 번 올라오거나 파일이 나뉘면 같은
    금형의 이력이 여러 시트·파일에 흩어진다. 금형 이력은 파일이 아니라 금형을
    따라 이어지므로 파일 경계를 넘어 묶는다. 대신 완전히 같은 이벤트는 하나로
    접는다(안 접으면 길이 0 짜리 유령 구간이 생긴다).
```

`backend/app/ingest/schemas.py:25-35` 의 `EES_FIELDS`:

```python
# JIG 관리대장: **시트 하나가 금형 하나**이고 시트 이름이 그 금형의 JIG ID 다.
# 시트 안에 JIG ID 열은 없다 — 금형 귀속은 join.py 가 시트 이름으로 한다.
# 한 행은 이벤트 하나(그 금형이 어디로 옮겨졌는지)다.
EES_FIELDS = [
    "event_at",    # 이벤트시간
    "location",    # 위치. 이 값이 "설비" 인 구간이 곧 사용구간이다
    "equipment",   # 설비명 — 식별용이 아니라 **그 구간의 MES 조회 키**를 고른다
    "line",        # 관리대장의 라인(공장 접두사가 없어 MES 와 직접 비교 불가)
    "process",
]
```

- [ ] **Step 6: 기존 관리대장 테스트를 시트명 기반으로 고친다**

`backend/tests/test_ingest_join.py` 에서 관리대장 행을 만드는 곳들이 `values={"mold_no": …}` 로 금형을 지정한다. 그 값을 `sheet=` 로 옮긴다:

```python
_row({"mold_no": "#RX39513", "event_at": "2026-07-01 08:00", "location": "설비"})
```
→
```python
_row({"event_at": "2026-07-01 08:00", "location": "설비"}, sheet="#RX39513")
```

**기준정보(`MASTER`) 행은 그대로 둔다** — 기준정보는 여전히 `mold_no` 열로 읽는다(`build_jig_index`). 바뀌는 것은 관리대장 행뿐이다.

파일 상단 docstring 의 "금형을 확정하는 것은 **관리대장의 JIG ID 열**이다" 를 "**관리대장의 시트 이름**이다" 로 고친다.

`backend/tests/test_ingest_assemble.py` 도 EES 행을 만든다면 같은 방식으로 고친다.

- [ ] **Step 7: 파이프라인 스텁을 실물 모양으로 고친다**

`backend/tests/test_ingest_pipeline.py` 는 EES 격자에 JIG ID 열이 있고 시트 이름이 `"Sheet1"` 이다. 이대로 두면 금형이 `SHEET1` 이 되어 기준정보에 없는 것으로 빠진다.

`FakeWorkbook` 이 시트 이름을 받게 한다:

```python
class FakeWorkbook:
    def __init__(self, rows, sheet="Sheet1"):
        self._rows = rows
        self._sheet = sheet

    def sheet_names(self):
        return [self._sheet]

    def used_values(self, sheet):
        return self._rows, "A1"

    def range_values(self, sheet, address):
        return self._rows

    def column_values(self, sheet, column, max_rows=5000):
        return [r[0] for r in self._rows]
```

EES 격자에서 JIG ID 열을 빼고, 레이아웃과 시트 이름을 맞춘다:

```python
EES_GRID = [
    ["이벤트시간", "위치", "설비명"],
    ["2026-07-01T07:00:00", "설비", EQUIP],
    ["2026-07-02T07:00:00", "통합 Jig Room", EQUIP],
]

# 관리대장은 시트 이름이 곧 금형이다. 다른 단계는 시트 이름이 의미 없다.
SHEET_NAMES = {"ees": "#RX28312"}
```

`LAYOUTS["ees"]` 를 고친다:

```python
    "ees": _layout(
        [("event_at", "A"), ("location", "B"), ("equipment", "C")],
        anchor="이벤트시간"),
```

`_fake_open` 이 시트 이름을 넘기게 한다:

```python
def _fake_open(grids=None):
    grids = GRIDS if grids is None else grids

    @contextmanager
    def _open(path):
        kind = _kind_of(path)
        yield FakeWorkbook(grids[kind], sheet=SHEET_NAMES.get(kind, "Sheet1"))
    return _open
```

Task 2 에서 더한 `test_same_form_sheets_reuse_one_discovered_layout` 의 `_open` 안 `FakeWorkbook(GRIDS[kind])` 도 `FakeWorkbook(GRIDS[kind], sheet=SHEET_NAMES.get(kind, "Sheet1"))` 로 고친다.

- [ ] **Step 8: 백엔드 전체 테스트를 돌린다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest`

Expected: 전부 PASS

- [ ] **Step 9: 커밋**

```bash
git add backend/app/ingest/join.py backend/app/ingest/schemas.py backend/tests/test_ingest_join.py backend/tests/test_ingest_assemble.py backend/tests/test_ingest_pipeline.py
git commit -F - <<'EOF'
fix(ingest): 관리대장의 금형을 시트 이름이 확정한다

실물 관리대장은 시트 하나가 금형 하나이고 시트 이름이 그 금형의 JIG ID 다.
시트 안에 JIG ID 열은 없다. 어제 세운 "한 시트에 여러 금형이 섞여 있고 행마다
JIG ID 열이 있다"는 전제는 실물을 확인하지 않고 세운 것이었고, 그대로 두면
모든 행이 mold_no 를 얻지 못해 금형이 0건으로 나온다.

귀속 규칙은 join 에 둔다. parser 는 "금형 귀속은 하지 않는다"를 이미 선언하고
있고, IQC(열)·PQC(조인)에 이어 관리대장(시트명)이 그 목록의 세 번째다.
SheetLayout 스키마는 건드리지 않는다 — 관리대장에서 그 값은 항상 참이라
사실상 상수인 필드가 스키마에 생기고, LLM 이 정하게 하면 틀릴 여지가 새로
생긴다.

JIG ID 열 경로는 지원하지 않는다. 규칙이 둘이면 어긋났을 때 무엇이 옳은지
정할 근거가 없다.

손실도 시트 단위로 바꾼다. 시트명 하나가 깨지면 그 시트의 전 행이 죽는데 행
수만 세면 어느 시트를 고쳐야 하는지 안 보인다 — bad_sheet_names 를 더한다.
집계는 extract_runs 한 곳에서만 한다. latest_locations 가 같은 행을 다시 세면
같은 시트가 두 번 보고된다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 4: `bad_sheet_names` 를 배치 요약까지 흘린다

손실이 `JoinLosses` 에만 있으면 화면에서 보이지 않는다. 시트 이름 하나가 깨져 금형이 통째로 빠졌는데 아무도 원인을 못 찾는 상태가 정확히 이 변경이 막으려는 것이다.

**Files:**
- Modify: `backend/app/ingest/schemas.py` (`RunSummary`)
- Modify: `backend/app/ingest/db.py` (`SCHEMA`, `_ADDED_COLUMNS`)
- Modify: `backend/app/ingest/registry.py` (`record_run`, `latest_run`)
- Modify: `backend/app/ingest/pipeline.py:201-214`
- Test: `backend/tests/test_ingest_registry.py`

**Interfaces:**
- Consumes: Task 3 의 `JoinLosses.bad_sheet_names`.
- Produces: `RunSummary.bad_sheet_names: list[str]`, DB 컬럼 `ingest_run.bad_sheet_names_json`.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_ingest_registry.py` 에 추가(`conn` 픽스처와 `RunSummary` import 는 이미 있다):

```python
def test_run_summary_round_trips_bad_sheet_names(conn):
    """시트 이름이 깨져 금형이 빠진 사실은 화면까지 가야 한다. 여기서 끊기면
    사용자는 금형이 왜 안 나오는지 알 방법이 없다."""
    registry.record_run(conn, RunSummary(
        status="ok", started_at="2026-07-30T00:00:00",
        bad_sheet_names=["합계", "표지"],
    ))

    latest = registry.latest_run(conn)

    assert latest is not None
    assert latest.bad_sheet_names == ["합계", "표지"]
```

그리고 기존 `test_init_db_adds_columns_to_an_older_database` 의 `record_run(...)` 인자에 `bad_sheet_names=["합계"]` 를 더하고, 마지막에 단언을 하나 더 붙인다:

```python
    assert latest.bad_sheet_names == ["합계"]
```

이 테스트는 컬럼이 없는 옛 `ingest_run` 을 만들어 두므로, `_ADDED_COLUMNS` 를 빠뜨리면 여기서 잡힌다.

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest tests/test_ingest_registry.py -k "bad_sheet_names or older_database" -v`

Expected: FAIL — `AttributeError` 또는 `sqlite3.OperationalError: table ingest_run has no column named bad_sheet_names_json`

- [ ] **Step 3: `RunSummary` 에 필드를 더한다**

`backend/app/ingest/schemas.py` 의 `RunSummary`, `unknown_equipment` 아래:

```python
    # 시트 이름이 JIG ID 로 안 읽힌 관리대장 시트. 관리대장은 시트 하나가 금형
    # 하나라, 이름이 깨지면 그 금형이 통째로 안 나온다. unknown_jig_id 와는
    # 다른 사고다 — 이쪽은 관리대장을, 저쪽은 기준정보를 고쳐야 한다.
    bad_sheet_names: list[str] = []
```

- [ ] **Step 4: DB 컬럼을 더한다**

`backend/app/ingest/db.py` — `SCHEMA` 의 `ingest_run` 정의에서 `open_runs INTEGER NOT NULL DEFAULT 0` 뒤에 쉼표를 붙이고 한 줄 더한다:

```sql
    open_runs      INTEGER NOT NULL DEFAULT 0,
    bad_sheet_names_json TEXT NOT NULL DEFAULT '[]'
```

그리고 `_ADDED_COLUMNS["ingest_run"]` 에:

```python
        "bad_sheet_names_json": "TEXT NOT NULL DEFAULT '[]'",
```

**양쪽 다 필요하다** — `CREATE TABLE IF NOT EXISTS` 는 기존 테이블에 컬럼을 붙여주지 않는다(`db.py:111-114` 주석).

- [ ] **Step 5: `record_run` / `latest_run` 을 잇는다**

`backend/app/ingest/registry.py` — `record_run` 의 INSERT 컬럼 목록 끝(`open_runs` 뒤)에 `bad_sheet_names_json` 을 더하고, `VALUES` 의 `?` 를 18개에서 19개로 늘린 뒤, 값 튜플 끝(`summary.open_runs` 뒤)에:

```python
            json.dumps(summary.bad_sheet_names, ensure_ascii=False),
```

`latest_run` 의 `RunSummary(...)` 에:

```python
        bad_sheet_names=json.loads(row["bad_sheet_names_json"]),
```

- [ ] **Step 6: 파이프라인이 채우게 한다**

`backend/app/ingest/pipeline.py` 의 `RunSummary(...)` (201-214행), `open_runs=` 줄 아래:

```python
            bad_sheet_names=result.losses.bad_sheet_names,
```

- [ ] **Step 7: 테스트가 통과하는지 확인한다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest`

Expected: 전부 PASS

- [ ] **Step 8: 파이프라인 통합 단언을 더한다**

`backend/tests/test_ingest_pipeline.py` 끝에 추가한다(Task 3 에서 고친 `SHEET_NAMES`·`FakeWorkbook` 을 쓴다):

```python
def test_bad_ledger_sheet_name_is_reported_and_the_mold_is_missing(env, monkeypatch):
    """관리대장 시트 이름이 JIG ID 로 안 읽히면 그 금형이 통째로 빠진다.
    이름을 요약에 남기지 않으면 사용자가 어느 시트를 고칠지 알 수 없다."""
    monkeypatch.setattr(
        "app.ingest.pipeline.discover_layout", _fake_discover(LAYOUTS)
    )

    @contextmanager
    def _open(path):
        kind = _kind_of(path)
        sheet = "합계" if kind == "ees" else SHEET_NAMES.get(kind, "Sheet1")
        yield FakeWorkbook(GRIDS[kind], sheet=sheet)

    summary = run_ingest(env, model=object(), open_wb=_open)

    assert summary.bad_sheet_names == ["합계"]
    assert summary.mold_count == 0
    assert summary.skipped_rows >= 2
```

- [ ] **Step 9: 백엔드 전체 테스트를 다시 돌린다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest`

Expected: 전부 PASS

- [ ] **Step 10: 커밋**

```bash
git add backend/app/ingest/schemas.py backend/app/ingest/db.py backend/app/ingest/registry.py backend/app/ingest/pipeline.py backend/tests/test_ingest_registry.py backend/tests/test_ingest_pipeline.py
git commit -F - <<'EOF'
feat(ingest): 시트 이름이 깨진 관리대장 시트를 배치 요약에 남긴다

관리대장은 시트 하나가 금형 하나라, 시트 이름이 JIG ID 로 안 읽히면 그 금형이
통째로 빠진다. 손실이 JoinLosses 에만 있으면 화면에서 끊겨 사용자는 금형이 왜
안 나오는지 알 방법이 없다.

unknown_jig_id 와 다른 사고라 따로 남긴다 — 이쪽은 관리대장 시트 이름을,
저쪽은 기준정보를 고쳐야 한다. 사람이 열어볼 파일이 다르다.

행 수는 기존대로 skipped_rows 에 합산한다. 같은 뜻의 숫자를 화면에 둘 만들지
않기 위해서다.

SCHEMA 와 _ADDED_COLUMNS 양쪽에 컬럼을 적는다. 한쪽만 적으면 예전 molds.db 를
쓰던 환경에서 record_run 이 'no such column' 으로 터져 배치가 통째로 실패한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 5: 화면에 `bad_sheet_names` 를 드러낸다

**Files:**
- Modify: `frontend/src/api/ingest.ts`
- Modify: `frontend/src/components/Dashboard/IngestPanel.tsx`
- Test: `frontend/src/components/Dashboard/IngestPanel.test.tsx`

**Interfaces:**
- Consumes: Task 4 의 `RunSummary.bad_sheet_names`.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`frontend/src/components/Dashboard/IngestPanel.test.tsx` 에는 이미 `OK: RunSummary` 상수와 `api` 목이 있다. `describe("IngestPanel", …)` 안에 추가:

```tsx
  it("시트 이름이 JIG ID 로 안 읽힌 관리대장 시트를 이름까지 보여준다", async () => {
    // 관리대장은 시트 하나가 금형 하나다. 이름이 깨지면 그 금형이 통째로
    // 빠지는데, 어느 시트인지 안 보이면 사용자가 고칠 수가 없다.
    vi.mocked(api.getIngestStatus).mockResolvedValue({
      ...OK,
      mold_count: 0,
      bad_sheet_names: ["합계"],
    });

    render(<IngestPanel />);

    expect(
      await screen.findByText(/관리대장 시트 이름을 JIG ID 로 읽지 못함 1건/)
    ).toBeInTheDocument();
    expect(await screen.findByText(/합계/)).toBeInTheDocument();
  });
```

`OK` 상수에도 `bad_sheet_names: []` 를 더한다(`open_runs: 0,` 아래).

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `cd frontend && npm test -- --run IngestPanel`

Expected: FAIL — 해당 텍스트를 못 찾는다

- [ ] **Step 3: 타입에 필드를 더한다**

`frontend/src/api/ingest.ts` 의 `unknown_equipment: string[];` 아래:

```ts
  // 시트 이름이 JIG ID 로 안 읽힌 관리대장 시트. 관리대장은 시트 하나가 금형
  // 하나라 이름이 깨지면 그 금형이 통째로 빠진다. unknown_jig_id 와는 다른
  // 사고다 — 이쪽은 관리대장을, 저쪽은 기준정보를 고쳐야 한다.
  bad_sheet_names: string[];
```

- [ ] **Step 4: 손실 목록에 한 줄 더한다**

`frontend/src/components/Dashboard/IngestPanel.tsx` 의 `Losses` 안, `s.unknown_jig_id` 블록 **위**에 넣는다(관리대장 자체가 깨진 것이 더 앞선 원인이다):

```tsx
  // 관리대장은 시트 하나가 금형 하나다. 시트 이름이 JIG ID 로 안 읽히면 그
  // 금형이 통째로 빠지는데, 이름을 보여주지 않으면 어느 시트를 고쳐야 하는지
  // 알 수가 없다.
  if (s.bad_sheet_names.length > 0) {
    items.push(
      `관리대장 시트 이름을 JIG ID 로 읽지 못함 ${s.bad_sheet_names.length}건` +
        ` (해당 금형이 목록에서 빠짐): ${s.bad_sheet_names.join(", ")}`
    );
  }
```

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

Run: `cd frontend && npm test -- --run`

Expected: 전부 PASS. 다른 테스트 파일이 `RunSummary` 리터럴을 만든다면 새 필수 필드 때문에 타입 오류가 난다 — 그 목들에도 `bad_sheet_names: []` 를 더한다.

- [ ] **Step 6: 타입 검사**

Run: `cd frontend && npx tsc --noEmit`

Expected: 오류 없음

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/api/ingest.ts frontend/src/components/Dashboard/IngestPanel.tsx frontend/src/components/Dashboard/IngestPanel.test.tsx
git commit -F - <<'EOF'
feat(dashboard): 시트 이름이 깨진 관리대장 시트를 손실로 보여준다

관리대장은 시트 하나가 금형 하나라, 이름이 JIG ID 로 안 읽히면 그 금형이
통째로 빠진다. 이름을 그대로 보여줘야 사용자가 어느 시트를 고칠지 안다.

기준정보 손실(unknown_jig_id)보다 위에 둔다 — 관리대장 자체가 깨진 것이 더
앞선 원인이라, 아래에 두면 기준정보를 먼저 뒤지게 된다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 6: EES 발견 프롬프트가 실물을 말하게 한다

현재 프롬프트는 LLM 에게 **틀린 사실을 단언**하고 **없는 열을 반드시 찾으라**고 압박한다. 그대로 두면 LLM 이 엉뚱한 열을 `mold_no` 로 지목하거나 헤매다 실패한다.

**Files:**
- Modify: `backend/app/ingest/discover.py`
- Test: `backend/tests/test_ingest_discover.py`

**Interfaces:**
- Consumes: Task 3 의 `EES_FIELDS`(이제 `mold_no` 없음).
- Produces: `_gaps(wb, layout: SheetLayout, sheet_name: str) -> list[str]`, `_make_submit_tool(holder: dict, wb, sheet_name: str) -> StructuredTool`.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_ingest_discover.py` 에 추가(`FIELD_GUIDE` import 가 없으면 상단에 더한다):

```python
def test_ees_guide_does_not_ask_for_a_mold_no_column():
    """실물 관리대장에는 JIG ID 열이 없다. 반드시 찾으라고 하면 LLM 이 엉뚱한
    열을 지목하거나 헤매다 실패한다."""
    guide = FIELD_GUIDE["ees"]

    assert "mold_no" not in guide
    assert "시트 이름" in guide
    assert "여러 금형" not in guide, "틀린 전제가 프롬프트에 남아 있다"
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest tests/test_ingest_discover.py::test_ees_guide_does_not_ask_for_a_mold_no_column -v`

Expected: FAIL — `assert "mold_no" not in guide`

- [ ] **Step 3: `FIELD_GUIDE["ees"]` 를 다시 쓴다**

`backend/app/ingest/discover.py` 의 `"ees":` 항목을 통째로 교체한다:

```python
    "ees": (
        "이 시트는 JIG 관리대장이며 **금형 하나의 이력**이다. 한 행이 사건\n"
        "하나(그 금형이 어디로 옮겨졌는지)이고 시간순으로 적혀 있다.\n"
        "\n어느 금형인지는 **시트 이름**이 정한다(시트 이름이 곧 JIG ID 다).\n"
        "시트 안에 JIG ID 열은 **없다. 찾지 마라.**\n"
        "아래 필드에 해당하는 열을 찾아 field 이름을 **정확히 이 영문 이름으로**\n"
        "지정하라(없으면 넣지 않는다):\n"
        + "\n".join(f"  - {f}" for f in EES_FIELDS)
        + "\n\n짝지어 보면 이렇다:\n"
        "  '이벤트시간'                    → event_at\n"
        "  '위치'                          → location\n"
        "  '설비명'(POU WND10_Stack(1차)_01 꼴) → equipment\n"
        "\nlocation(위치) 와 event_at(이벤트시간) 은 **반드시 찾아야 한다.**\n"
        "값이 '설비' 인 행이 곧 그 금형이 설비에 투입된 시점이고 그 다음 사건의\n"
        "시각이 빠져나온 시점이라, 둘 중 하나만 없어도 금형이 언제 가동됐는지\n"
        "알 방법이 사라진다. 없으면 notes 에 적어라.\n"
        "\nequipment(설비명)은 금형을 식별하는 열이 **아니다** — 그 구간에 어느\n"
        "설비의 실적을 붙일지 고르는 데 쓴다. 있으면 잡되, 없다고 헤매지 마라.\n"
    ),
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest tests/test_ingest_discover.py -v`

Expected: PASS

- [ ] **Step 5: `_gaps` 가 실제 시트명을 쓰게 한다**

`_gaps` 는 지금 `wb.used_values(layout.sheet_name)` 로 LLM 이 적은 이름을 신뢰한다. 오기가 나면 `except` 로 빠져 커버리지 검증이 **조용히 건너뛰어진다**.

```python
def _gaps(wb, layout: SheetLayout, sheet_name: str) -> list[str]:
    """제출된 레이아웃이 시트에서 덮지 못한 곳. 검증이 못 돌면 빈 목록이다.

    시트는 호출자가 준 **실제 이름**으로 연다. layout.sheet_name 은 에이전트가
    적은 값이라, 오기가 나면 검증이 조용히 건너뛰어지고 열이 빠진 레이아웃이
    그대로 접수된다.

    검증 자체의 실패가 제출을 막을 이유는 없다 — 그러면 레이아웃을 하나도 못
    얻어 파일 전체가 실패한다.
    """
    try:
        grid, top_left = wb.used_values(sheet_name)
    except Exception:  # noqa: BLE001
        return []
    return find_layout_gaps(grid, top_left, layout)
```

`_make_submit_tool` 이 시트명을 받아 넘기게 한다(나머지 본문은 그대로):

```python
def _make_submit_tool(holder: dict, wb, sheet_name: str) -> StructuredTool:
    def _submit(**kwargs) -> str:
        layout = SheetLayout(**kwargs)
        holder["layout"] = layout

        gaps = _gaps(wb, layout, sheet_name)
```

- [ ] **Step 6: 호출부를 모두 잇는다**

Run: `grep -n "_make_submit_tool\|build_discover_agent" backend/app/ingest/discover.py backend/tests/test_ingest_discover.py`

`_make_submit_tool` 은 `build_discover_agent` 안에서 불린다. 그 함수가 시트명을 받아 넘기게 한다:

```python
def build_discover_agent(model, wb, kind: SourceKind, sheet_name: str, holder: dict):
    ...
    tools = [..., _make_submit_tool(holder, wb, sheet_name)]
```

`discover_layout` 은 이미 `sheet_name` 을 인자로 받으므로(`discover_layout(model, wb, kind, sheet_name, *, config=None)`) `build_discover_agent(model, wb, kind, sheet_name, holder)` 로 그대로 넘긴다.

기존 매개변수 순서가 다르면 **그 파일의 실제 순서에 맞춰** 넣되, `sheet_name` 은 위치 인자로 둔다 — 기본값을 주면 호출부에서 빠뜨렸을 때 다시 LLM 이 적은 이름으로 조용히 돌아간다.

`backend/tests/test_ingest_discover.py` 의 호출부도 같이 고친다.

- [ ] **Step 7: 백엔드 전체 테스트를 돌린다**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest`

Expected: 전부 PASS

- [ ] **Step 8: 커밋**

```bash
git add backend/app/ingest/discover.py backend/tests/test_ingest_discover.py
git commit -F - <<'EOF'
fix(ingest): 관리대장 발견 프롬프트가 실물 구조를 말하게 한다

프롬프트가 "한 시트에 여러 금형의 사건이 섞여 있다"고 단언하고 "mold_no 는
반드시 찾아야 한다, 놓치면 파일 전체가 버려진다"고 압박했다. 실물 관리대장은
시트 하나가 금형 하나이고 시트 안에 JIG ID 열이 없다. 없는 열을 반드시 찾으라
하면 엉뚱한 열을 지목하거나 헤매다 실패한다.

_gaps 도 실제 시트명으로 연다. layout.sheet_name 은 에이전트가 적은 값이라
오기가 나면 커버리지 검증이 조용히 건너뛰어지고, 열이 빠진 레이아웃이 그대로
접수된다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 7: 샘플 관리대장을 시트=금형으로 되돌린다

**Files:**
- Modify: `backend/examples/make_sample_uploads.py:301-366`
- Modify: `backend/examples/check_samples.py`
- Regenerate: `backend/data/uploads/EES/JIG_관리대장.xlsx`

**Interfaces:**
- Consumes: Task 3 의 귀속 규칙.
- Produces: 시트명이 `#RX39513` 꼴인 관리대장 샘플. `make_jig_ledger(app, path) -> dict` 는 `{"sheets": N, "rows": M}` 를 계속 돌려준다.

**주의:** 이 태스크는 Windows + Excel(xlwings) 이 필요하다. 그 환경이 아니면 Step 3 앞에서 멈추고 사람에게 넘긴다.

- [ ] **Step 1: 생성기를 고친다**

`backend/examples/make_sample_uploads.py` — 주석과 헤더에서 `"JIG ID"` 를 뺀다:

```python
# ────────────────────────────────────────────────────────────────────
# JIG 관리대장 — **시트 하나가 금형 하나**. 시트 이름이 그 금형의 JIG ID 다.
#
# 시트 안에 JIG ID 열은 없다. 금형 귀속은 시트 이름이 한다 — 실물이 그렇다.
# 설비명은 식별이 아니라 그 구간의 MES 조회 키를 고르는 데 쓴다.
# ────────────────────────────────────────────────────────────────────
_LEDGER_HEADER = [
    "이벤트시간", "활동", "수량", "위치", "상태", "설비명", "사용 여부",
    "Shop(공정군)", "라인", "공정", "상세 구분1", "상세 구분2",
    "규격(Spec)", "규격 단위", "제조 업체", "생산 유형", "폐기 기준",
    "1차 경고 주기", "2차 경고 주기", "3차 경고 주기",
    "설명", "수정자 사번", "수정자",
]
```

`_ledger_rows()` 를 금형 하나치만 만드는 함수로 바꾼다:

```python
def _ledger_rows(jig) -> list[list]:
    """금형 하나의 이벤트를 시간순으로. 시트 하나가 이대로 들어간다."""
    # 관리대장의 '라인' 은 공장 접두사가 없다("Pouch #10(S)"). 기준정보의
    # Line명은 붙어 있다("톈진 Pouch #10(S)"). 실물이 그렇고, 그래서 라인
    # 문자열끼리 직접 비교하면 안 된다 — MES 조회는 설비코드로 해야 한다.
    ledger_line = jig.line.split(None, 1)[1]

    rows = []
    for month, day, hour, location, *override in EVENTS[jig.jig_id]:
        # 정각으로 둔다. 분·초를 흩으면 24시간·96시간 **정확히** 걸친 구간이
        # 96.5h 가 되어 하루가 더 붙는다 — 올림 규칙의 경계가 곧 이 샘플의
        # 핵심 검증 대상인데 그게 뭉개진다.
        when = datetime(2026, month, day, hour)
        equipment = override[0] if override else jig.equip_name
        rows.append([
            when, None, None, location, None, equipment, "사용",
            jig.shop, ledger_line, "Pouch",
            *([None] * 13),
        ])
    rows.sort(key=lambda r: r[0])
    return rows
```

`make_jig_ledger` 를 시트 여러 개로 바꾼다:

```python
def make_jig_ledger(app, path: Path) -> dict:
    book = _new_book(app)
    jigs = [*JIGS, UNREGISTERED]
    total_rows = 0

    for i, jig in enumerate(jigs):
        # 시트 이름이 곧 금형이다. 시트 안에 JIG ID 열은 없다 — 실물이 그렇다.
        sht = book.sheets[0] if i == 0 else book.sheets.add(after=book.sheets[-1])
        sht.name = jig.jig_id

        sht.range("B2").value = _LEDGER_HEADER
        rows = _ledger_rows(jig)
        sht.range("B3").value = rows
        sht.range("B2:X2").api.Font.Bold = True
        # JIG ID 열이 빠져 이벤트시간이 B 열이다.
        sht.range("B3:B%d" % (2 + len(rows))).number_format = "yyyy-mm-dd hh:mm:ss"
        sht.autofit()
        total_rows += len(rows)

    _save(book, path)
    return {"sheets": len(jigs), "rows": total_rows}
```

- [ ] **Step 2: 기대값 계산기를 고친다**

`L_JIG_ID` 는 80행과 85행에서만 쓰이며 둘 다 아래에서 통째로 교체되는 블록 안에 있다(확인 완료). 그러므로 고칠 곳은 **36행과 72-86행 두 군데뿐**이다.

36행을 바꾼다:

```python
L_TIME, L_LOC, L_EQUIP = 0, 3, 5                       # 관리대장(JIG ID 열 없음)
```

72-86행(`# ── 관리대장: JIG ID 로 묶어…` 주석부터 `by_mold` 를 채우는 `for r in events:` 루프 끝까지)을 통째로 교체한다:

```python
    # ── 관리대장: 시트 하나가 금형 하나다 ────────────────────────────
    # 시트 이름이 곧 JIG ID 다. 시트 안에 JIG ID 열은 없다.
    ledger = root / "EES" / "JIG_관리대장.xlsx"
    by_mold = {}
    with open_workbook(str(ledger)) as wb:
        for sheet in wb.sheet_names():
            mold = sheet.strip().lstrip("#").strip().upper()
            rows = [r for r in wb.used_values(sheet)[0][1:] if r and r[L_TIME]]
            if rows:
                by_mold.setdefault(mold, []).extend(rows)
```

88행 이후(`molds, unknown_jig_id, … = {}, [], []` 부터)는 `by_mold.items()` 를 돌며 `L_TIME`·`L_LOC`·`L_EQUIP` 만 쓰므로 **그대로 둔다.**

기대값 자체(금형 6건 · IQC붙음 5건 · 고아 `[RX99999]` · 기준정보에 없는 설비 `[POU WND99_Unknown_01]` · MES 없는 날 `[2026-07-03]` · 가동 중 1건 · 실적 못 찾은 구간 1건 · 설치 횟수 RX28312·RX39513·RX41194·RX50177 각 4, RX28315 3, RX39002 2)는 **바뀌지 않는다** — 같은 이벤트를 시트로 나눠 담을 뿐이다.

- [ ] **Step 3: 샘플을 다시 만든다**

Run: `cd backend && python examples/make_sample_uploads.py`

Expected: `EES/JIG_관리대장.xlsx` 가 `{"sheets": 7, "rows": …}` 로 생성된다(JIGS 6 + UNREGISTERED 1).

- [ ] **Step 4: 시트 구조를 눈으로 확인한다**

Run:
```bash
cd backend && python -c "
import openpyxl
w = openpyxl.load_workbook('data/uploads/EES/JIG_관리대장.xlsx')
print(w.sheetnames)
ws = w[w.sheetnames[0]]
print([c.value for c in ws[2]][:6])
"
```

Expected: 시트명이 `['#RX39513', '#RX28312', …]` 이고 헤더 첫 칸이 `이벤트시간`(`JIG ID` 가 아님)

- [ ] **Step 5: 기대값 계산기를 돌린다**

Run: `cd backend && python examples/check_samples.py`

Expected: 금형 6건 · IQC붙음 5건 · 고아 `[RX99999]` · 기준정보에 없는 설비 `[POU WND99_Unknown_01]` · MES 파일 없는 날 `[2026-07-03]` · 가동 중 1건 · 실적 못 찾은 구간 1건

- [ ] **Step 6: 커밋**

```bash
git add backend/examples/make_sample_uploads.py backend/examples/check_samples.py
git add -f "backend/data/uploads/EES/JIG_관리대장.xlsx"
git commit -F - <<'EOF'
test(examples): 샘플 관리대장을 시트=금형으로 되돌린다

실물이 그렇다. 어제 이 파일을 "한 시트에 전 금형 혼재" 로 바꿨는데, 그건 조인
버그를 재현할 샘플이 필요해서였지 실물을 확인한 결과가 아니었다.

케이스 커버리지는 그대로 유지한다 — 수리 왕복과 정확히 96시간(올림 경계),
설비→설비 연속, 마지막이 수리/설비/폐기, 기준정보에 없는 설비명과 JIG ID.
시각도 정각으로 둔다. 분·초를 흩으면 96시간이 96.5h 가 되어 하루가 더 붙는데,
그 경계가 이 샘플의 핵심 검증 대상이다.

data/ 는 gitignore 대상이라 -f 로 담는다(기존 샘플과 같은 방식).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 8: 뒤집힌 전제를 옛 설계 문서에 정정으로 남긴다

같은 파일의 전제가 두 번 뒤집혔다. 무엇이 왜 바뀌었는지 추적할 수 없으면 다음 사람이 같은 실수를 반복한다.

**Files:**
- Modify: `docs/superpowers/specs/2026-07-29-ees-mes-조인-design.md`

- [ ] **Step 1: 조인 사슬 다이어그램에 정정 상자를 단다**

`## 조인 사슬` 절의 코드 블록 **바로 아래**에 추가:

```markdown
> **2026-07-30 재정정.** 위 다이어그램의 "한 시트에 여러 금형" 은 **틀렸다.**
> 실물 관리대장은 시트 하나가 금형 하나이고, **시트 이름이 그 금형의 JIG ID**
> 이며, 시트 안에 JIG ID 열이 없다. 이 문서의 전제는 실물을 확인하지 않고
> 세운 것이었다. 현재 규칙은
> `2026-07-30-관리대장-시트-금형-귀속-design.md` 를 보라.
```

- [ ] **Step 2: 규칙 1 의 정정 상자에 이어 붙인다**

기존 `> **2026-07-29 정정.**` 상자 **바로 아래**에 추가:

```markdown
> **2026-07-30 재정정.** 위 정정도 절반만 맞았다. JIG ID 가 금형을 확정하는
> 것은 맞지만, 그 JIG ID 는 **행의 열이 아니라 시트 이름**에 있다. "한 시트에
> 여러 금형의 이벤트가 섞여 있으므로" 로 시작하는 문단은 실물에 해당하지
> 않는다 — 시트 하나가 금형 하나다. 파일 경계를 넘어 묶는 것과 완전히 같은
> 이벤트를 접는 것은 여전히 유효하다.
```

- [ ] **Step 3: 상태 줄을 갱신한다**

문서 상단의 `**상태**` 줄을 교체한다:

```markdown
**상태** 구현됨. 단, 관리대장의 금형 귀속 전제가 **두 번** 뒤집혔다 —
조인 사슬과 규칙 1 아래의 정정 상자를 먼저 읽을 것. 현재 규칙은
`2026-07-30-관리대장-시트-금형-귀속-design.md` 다.
```

- [ ] **Step 4: 커밋**

```bash
git add "docs/superpowers/specs/2026-07-29-ees-mes-조인-design.md"
git commit -F - <<'EOF'
docs(spec): 관리대장 구조 전제가 다시 뒤집힌 것을 옛 문서에 남긴다

같은 파일의 전제가 두 번 뒤집혔다. 옛 문서를 그대로 두면 다음 사람이 "한
시트에 여러 금형" 을 읽고 같은 실수를 반복한다.

지우지 않고 정정 상자로 남긴다 — 무엇을 왜 잘못 알았는지가 이 문서의 가장
쓸모 있는 부분이다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## 최종 검증

모든 태스크가 끝난 뒤:

- [ ] `cd backend && python -m pytest` — 전부 통과
- [ ] `cd frontend && npm test -- --run` — 전부 통과
- [ ] `cd frontend && npx tsc --noEmit` — 오류 없음
- [ ] `cd backend && python examples/check_samples.py` — 기대값과 일치
- [ ] 실제 배치 1회: `python dev.py` 로 띄우고 "수집 실행" → 금형 6건, `bad_sheet_names` 비어 있음
- [ ] **실물 관리대장으로 1회** — `backend/data/uploads/EES/` 에 실물 파일을 넣고 수집 실행. 금형이 시트 수만큼 나오고, 두 번째 실행에서 LLM 발견이 돌지 않는지(캐시 적중) 확인
