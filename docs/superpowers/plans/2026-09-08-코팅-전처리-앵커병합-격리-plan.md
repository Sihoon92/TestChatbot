# 코팅 전처리 재정의 (앵커 병합 · ±10분 격리 · 5층 로그) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 조정 이벤트의 정의를 연쇄 병합에서 앵커 병합(span ≤ 2분)으로 바꾸고, 격리를 끝 기준 ±10분으로 통일하며, 전처리 과정을 사람이 눈으로 검증할 수 있는 5층 텍스트 로그를 낸다.

**Architecture:** 규칙 계산은 `app/coating/events.py` 의 순수 함수로 남기고, 로그용 집계는 신설 `app/coating/trace.py` 에 모은다(규칙과 보고를 섞지 않는다). 렌더링은 `app/coating/diagnose.py` 가 하고 파일 쓰기는 `app/coating/dump.py` 만 한다. 기존 파이프라인의 경계(순수 계산 / 가장자리 I/O)를 그대로 따른다.

**Tech Stack:** Python 3.x, pandas, numpy, pytest. 프론트엔드·LLM 없음.

**Spec:** `docs/superpowers/specs/2026-09-08-코팅-전처리-앵커병합-격리-design.md`

## Global Constraints

- **설정은 `.env` 단일 출처.** 새 설정은 `Settings` 필드 + `backend/.env.example` 에 함께 넣는다. 코드에 기본값을 하드코딩하지 않는다. 접근은 `app.config.get_settings()`. (`CLAUDE.md`)
- **실데이터는 Claude 가 볼 수 없다.** 모든 검증은 `backend/tests/` 의 합성 데이터로 한다. "경로를 주면 돌려 보겠다" 는 금지. (`CLAUDE.md`)
- **`schemas.py` 는 app 안의 어떤 모듈도 import 하지 않는다** (stdlib 만).
- **컬럼 문자열 리터럴은 `schemas.py` 에만 존재한다.** 다른 모듈은 `S.XXX` 상수를 쓴다.
- **한글 주석/문서.** 기존 코드베이스 관례. docstring 은 "왜" 를 적는다.
- 테스트 실행: `cd backend && .venv/Scripts/python.exe -m pytest <path> -v`
- 커밋 메시지는 한글 본문 + 아래 트레일러 2줄:
  ```
  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
  ```
- 설정 최종값 (Task 4에서 한 번에 적용):
  `COATING_ISOLATION_PRE_MINUTES=10`, `COATING_ISOLATION_POST_MINUTES=10`,
  `COATING_RESPONSE_PRE_MINUTES=10`, `COATING_RESPONSE_POST_MINUTES=10`,
  `COATING_SETTLE_MAX_WAIT_MINUTES=10`, `COATING_DELTA_WINDOW_MINUTES=3` (신설),
  `COATING_EVENT_MERGE_MINUTES=2` (유지), `COATING_SETTLE_WINDOW_MINUTES=5` (유지)

---

## 파일 구조

| 파일 | 책임 | 상태 |
|---|---|---|
| `app/coating/schemas.py` | 컬럼 상수. `LAST_AT`·`RUN`·`SPAN`·`ISO_REASON` 추가 | 수정 |
| `app/coating/events.py` | **규칙**: 앵커 병합, run 표식, 끝 기준 격리 | 수정 |
| `app/coating/features.py` | Δgap 합산 버그 수정, `delta_samples` 를 격리 기반으로 | 수정 |
| `app/coating/response.py` | `align_events` 의 `CONTAMINATED` 필터 제거 | 수정 |
| `app/coating/trace.py` | **로그용 집계**: 깔때기·원장·구신대조·타임라인 | **신설** |
| `app/coating/diagnose.py` | `--preprocess` 렌더러 (5절 + 기존 3절) | 수정 |
| `app/coating/report.py` | 배선, `12_event_ledger` 덤프 | 수정 |
| `app/config.py`, `backend/.env.example` | 설정 6개 | 수정 |
| `tests/test_coating_events.py` | 앵커·격리 테스트 | 수정 |
| `tests/test_coating_features.py` | 합산·선별 테스트 | 수정 |
| `tests/test_coating_trace.py` | trace 4함수 테스트 | **신설** |
| `tests/test_coating_diagnose.py` | 렌더러 테스트 | 수정 |

`app/coating/dashboard.py` 는 **손대지 않는다** — `ev_mod.build_events` 의 시그니처가 그대로라 열만 늘어난다.

---

### Task 1: Δgap 덮어쓰기 버그 (규칙 변경과 독립)

한 이벤트 안에서 같은 zone 을 두 번 이상 만지면 `delta_samples` 가 같은 칸에 덮어써 마지막 Δ 만 남는다. zone1 을 40→41→42→43 으로 만졌으면 Δgap 은 +3 이어야 하는데 +1 이 된다. ΔWet 은 전체 변화를 담으므로 **게인이 3배로 부풀려진다.** 이 버그는 앵커 병합과 무관하게 이미 존재하고, 앵커 병합이 확률을 줄일 뿐 없애지 못한다. 그래서 먼저 단독으로 고친다.

**Files:**
- Modify: `backend/app/coating/features.py:85-90`
- Test: `backend/tests/test_coating_features.py`

**Interfaces:**
- Consumes: 없음 (첫 태스크)
- Produces: `features.delta_samples` 의 Δgap 이 (event, zone) 별 **합**이 된다. Task 5가 이 함수를 다시 손댄다.

- [ ] **Step 1: Write the failing test**

`backend/tests/test_coating_features.py` 의 `test_delta_samples_only_use_clean_events` 바로 아래에 추가:

```python
def test_delta_samples_sums_repeated_adjustments_of_one_zone():
    """한 이벤트 안에서 같은 zone 을 두 번 만졌으면 Δgap 은 합이다.

    덮어쓰면 마지막 것만 남아 Δgap 이 작아지는데, ΔWet 은 전체 변화를 담으므로
    게인이 그 비율만큼 부풀려진다.
    """
    ev = pd.DataFrame({
        S.LOT: ["L1"],
        S.EVENT: ["L1#1"],
        S.AT: pd.to_datetime(["2026-01-31 19:00"]),
        S.SETTLED_AT: pd.to_datetime(["2026-01-31 19:05"]),
        S.CONTAMINATED: [False],
        S.DROP_REASON: [None],
    })
    # 같은 zone1 을 +1 씩 세 번 = 총 +3
    dl = pd.DataFrame({
        S.EVENT: ["L1#1", "L1#1", "L1#1"],
        S.ITEM: ["30030838"] * 3,
        S.ZONE: [1.0, 1.0, 1.0],
        S.DELTA: [1.0, 1.0, 1.0],
    })
    times = pd.to_datetime([f"2026-01-31 19:{m:02d}" for m in range(0, 30)])
    w = pd.DataFrame({S.LOT: ["L1"] * 30, S.AT: times})
    for z in range(1, 26):
        w[f"z{z}"] = [18.2] * 15 + [18.5] * 15
    out = features.delta_samples(ev, dl, w, valid=list(range(1, 26)), window_minutes=3)
    assert out.iloc[0]["dg1"] == 3.0
```

- [ ] **Step 2: Run test to verify it fails**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_features.py::test_delta_samples_sums_repeated_adjustments_of_one_zone -v
```

Expected: FAIL — `assert 1.0 == 3.0`

- [ ] **Step 3: Write minimal implementation**

`backend/app/coating/features.py` 의 `delta_samples` 끝부분에서 이 블록을

```python
    # Δgap 채우기 — zone 이 있는 항목만. 스칼라 제어값은 레벨 모델이 쓴다.
    zoned = event_deltas[event_deltas[S.ZONE].notna()]
```

다음으로 바꾼다:

```python
    # Δgap 채우기 — zone 이 있는 항목만. 스칼라 제어값은 레벨 모델이 쓴다.
    #
    # (event, zone) 으로 먼저 **합친다**. 한 이벤트 안에서 같은 zone 을 두 번
    # 만지면 행이 두 개 오는데, 아래 루프는 같은 칸에 쓰므로 합치지 않으면
    # 마지막 것만 남는다. ΔWet 은 그 두 번의 결과를 다 담고 있으므로 Δgap 만
    # 작아지고, 그 비율만큼 게인이 부풀려진다.
    zoned = (
        event_deltas[event_deltas[S.ZONE].notna()]
        .groupby([S.EVENT, S.ZONE], as_index=False)[S.DELTA]
        .sum()
    )
```

- [ ] **Step 4: Run tests to verify they pass**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_features.py -v
```

Expected: 전부 PASS (기존 `test_delta_samples_only_use_clean_events` 포함 — 그쪽은 zone 중복이 없어 결과가 같다)

- [ ] **Step 5: Commit**

```bash
git add backend/app/coating/features.py backend/tests/test_coating_features.py
git commit -m "$(cat <<'EOF'
fix(coating): 한 이벤트 안 같은 zone 의 Δgap 을 덮어쓰지 않고 더한다

zone1 을 40→41→42→43 으로 만지면 delta_samples 가 같은 칸에 세 번 써서
Δgap 이 +3 이 아니라 +1 로 남았다. ΔWet 은 전체 변화를 담으므로 게인이
3배로 부풀려진다. (event, zone) 으로 먼저 합친다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

### Task 2: 앵커 병합과 run 표식

`events.py:30` 의 `diff()` 는 **직전 변경과의** 간격이라 2분씩 이어지면 묶음이 무한히 이어붙는다. 앵커(묶음 첫 변경)와 재도록 바꾸면 모든 묶음의 span 이 `merge_minutes` 를 넘지 못한다. 동시에 **구 규칙의 연쇄 묶음**을 `run_id` 로 함께 계산해 둔다 — 로그의 구/신 대조가 이 열 하나로 나온다.

**Files:**
- Modify: `backend/app/coating/schemas.py` (상수 4개 추가)
- Modify: `backend/app/coating/events.py:1-50` (`build_events`)
- Test: `backend/tests/test_coating_events.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `S.LAST_AT = "last_at"`, `S.RUN = "run_id"`, `S.SPAN = "span_min"`, `S.ISO_REASON = "iso_reason"`
  - `events.build_events(changes, merge_minutes) -> (ev, dl)`
    - `ev` 열: `[lot_id, event_id, worked_at, last_at, run_id, span_min, n_items]`
    - `dl` 열: `[event_id, item_id, zone, delta]` (변경 없음, 중복 행 유지)
  - `events._anchor_groups(times: pd.Series, merge_minutes: int) -> np.ndarray`
  - `events._run_groups(times: pd.Series, merge_minutes: int) -> np.ndarray`

- [ ] **Step 1: Write the failing tests**

`backend/tests/test_coating_events.py` 의 `test_changes_beyond_window_become_separate_events` 아래에 추가:

```python
def test_anchor_bounds_the_span_of_one_event():
    """묶음은 앵커에서 merge_minutes 안까지다. 직전 변경이 아니라 앵커와 잰다.

    0, 1.5, 3, 4.5 는 연쇄로 보면 전부 2분 이내로 이어져 한 묶음이 되지만,
    한 번의 조작이 2분 안에 끝난다는 가정에서는 두 번의 조작이다.
    """
    base = pd.Timestamp("2026-02-02 06:00")
    c = _changes([
        ("L1", S.GAP_ITEM_IDS[i], base + pd.Timedelta(minutes=m), 41.0, 40.0)
        for i, m in enumerate([0, 1.5, 3, 4.5])
    ])
    ev, _ = events.build_events(c, merge_minutes=2)
    assert len(ev) == 2
    assert list(ev[S.SPAN]) == [1.5, 1.5]


def test_chaining_no_longer_swallows_a_long_run():
    """8분에 걸친 여섯 번의 손질은 한 번의 계단 입력이 아니다."""
    base = pd.Timestamp("2026-02-02 06:00")
    c = _changes([
        ("L1", S.GAP_ITEM_IDS[i], base + pd.Timedelta(minutes=m), 41.0, 40.0)
        for i, m in enumerate([0, 1.5, 3, 4.5, 6, 8])
    ])
    ev, _ = events.build_events(c, merge_minutes=2)
    assert len(ev) == 3                       # 연쇄였다면 1
    assert (ev[S.SPAN] <= 2.0).all()


def test_run_id_marks_fragments_of_one_continuous_run():
    """쪼개진 조각들은 공통 run_id 를 갖는다. 배제하지는 않는다 - 설명만 한다."""
    base = pd.Timestamp("2026-02-02 06:00")
    c = _changes([
        ("L1", S.GAP_ITEM_IDS[i], base + pd.Timedelta(minutes=m), 41.0, 40.0)
        for i, m in enumerate([0, 1.5, 3, 4.5, 120])
    ])
    ev, _ = events.build_events(c, merge_minutes=2)
    runs = list(ev[S.RUN])
    assert runs[0] == runs[1]                 # 앞 두 조각은 한 덩어리였다
    assert runs[2] != runs[0]                 # 2시간 뒤는 다른 덩어리


def test_last_at_is_the_final_change_of_the_event():
    base = pd.Timestamp("2026-02-02 06:00")
    c = _changes([
        ("L1", S.GAP_ITEM_IDS[0], base, 41.0, 40.0),
        ("L1", S.GAP_ITEM_IDS[1], base + pd.Timedelta(minutes=1), 41.0, 40.0),
    ])
    ev, _ = events.build_events(c, merge_minutes=2)
    assert ev.iloc[0][S.AT] == base
    assert ev.iloc[0][S.LAST_AT] == base + pd.Timedelta(minutes=1)
```

기존 `_changes` 헬퍼는 문자열 시각을 받는데 위 테스트는 `Timestamp` 를 넘긴다.
`_changes` 를 다음으로 바꾼다(둘 다 받게):

```python
def _changes(rows):
    return pd.DataFrame(
        [
            {S.LOT: r[0], S.ITEM: r[1], S.AT: pd.Timestamp(r[2]),
             S.VALUE: r[3], S.PREV_VALUE: r[4]}
            for r in rows
        ]
    )
```

(이미 `pd.Timestamp()` 를 씌우고 있으므로 `Timestamp` 를 넣어도 그대로 통과한다 — 변경 불필요. 확인만 하고 넘어간다.)

- [ ] **Step 2: Run tests to verify they fail**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_events.py -k "anchor or chaining or run_id or last_at" -v
```

Expected: 4개 전부 FAIL — `KeyError: 'span_min'` 또는 `assert 1 == 2`

- [ ] **Step 3: Add the schema constants**

`backend/app/coating/schemas.py` 의 `# 이벤트` 블록을 다음으로 바꾼다:

```python
# 이벤트
EVENT = "event_id"
DELTA = "delta"
SETTLED_AT = "settled_at"
CONTAMINATED = "contaminated"
DROP_REASON = "drop_reason"
# 묶음의 마지막 변경 시각. 앞뒤 간격을 시작이 아니라 **끝** 에서 재기 위해 필요하다 -
# 묶음이 span 을 가지면 실제 조용한 시간은 시작 기준보다 span 만큼 짧다.
LAST_AT = "last_at"
SPAN = "span_min"
# 연속 조작 구간. 앵커 창을 넘겨서도 변경이 끊기지 않을 때 생기는 조각들의 공통 표식이고,
# 정의상 **구 규칙의 연쇄 묶음**과 같다. 배제에는 쓰지 않는다 - 로그에서 그 조각들이
# 한 덩어리였음을 보이고, 구/신 대조를 추가 계산 없이 내는 것이 목적이다.
RUN = "run_id"
ISO_REASON = "iso_reason"
```

- [ ] **Step 4: Rewrite build_events**

`backend/app/coating/events.py` 의 모듈 docstring 아래 import 부와 `build_events` 를 다음으로 교체한다(`_zone_of` 는 그대로 둔다):

```python
def _anchor_groups(times: pd.Series, merge_minutes: int) -> np.ndarray:
    """정렬된 시각들에 **앵커** 묶음 번호를 매긴다. ★순수

    앵커 t0 에서 merge_minutes 안이면 같은 묶음이고, 넘으면 그 시각이 새 앵커다.
    직전 값이 아니라 앵커와 재기 때문에 묶음 span 이 merge_minutes 를 넘지 못한다 -
    "한 번의 제어 조작은 merge_minutes 안에 끝난다" 는 가정을 코드가 강제하는 자리다.
    """
    w = pd.Timedelta(minutes=merge_minutes)
    out = np.empty(len(times), dtype=int)
    g, anchor = 0, None
    for i, t in enumerate(times):
        if anchor is None or t - anchor > w:
            g += 1
            anchor = t
        out[i] = g
    return out


def _run_groups(times: pd.Series, merge_minutes: int) -> np.ndarray:
    """정렬된 시각들에 **연쇄** 묶음 번호를 매긴다. ★순수

    직전 변경과의 간격으로 잇는다. 이것이 구 규칙이고, 지금은 배제가 아니라
    "이 조각들이 원래 한 덩어리였다" 를 보이는 용도로만 쓴다(schemas.RUN).
    """
    gap = times.diff()
    return (gap.isna() | (gap > pd.Timedelta(minutes=merge_minutes))).cumsum().to_numpy()


EVENT_COLS = [S.LOT, S.EVENT, S.AT, S.LAST_AT, S.RUN, S.SPAN, "n_items"]


def build_events(
    changes: pd.DataFrame, merge_minutes: int
) -> tuple[pd.DataFrame, pd.DataFrame]:
    """제어 항목의 '변경'을 앵커 기준 merge_minutes 안으로 묶어 이벤트를 만든다.

    시작값(prev_value 가 NaN)은 사람이 바꾼 것이 아니므로 이벤트가 아니다.
    출력(Wet) 변화도 이벤트가 아니다 — 그건 결과다.

    묶음은 **앵커**에서 잰다. 직전 변경에서 재면(구 규칙) 2분씩 이어질 때 묶음이
    무한히 이어붙어, 8분에 걸친 여섯 번의 손질이 "t0 의 한 번의 계단 입력" 이 된다.
    그 상태로 격리를 통과하면 순수 지연 L 이 그 span 만큼 오염된다.
    """
    ctrl = changes[
        changes[S.ITEM].isin(S.CONTROL_ITEM_IDS) & changes[S.PREV_VALUE].notna()
    ].sort_values([S.LOT, S.AT]).copy()

    if ctrl.empty:
        return (
            pd.DataFrame(columns=EVENT_COLS),
            pd.DataFrame(columns=[S.EVENT, S.ITEM, S.ZONE, S.DELTA]),
        )

    parts = []
    for _, g in ctrl.groupby(S.LOT, sort=False):
        g = g.sort_values(S.AT).copy()
        g["_g"] = _anchor_groups(g[S.AT], merge_minutes)
        g["_r"] = _run_groups(g[S.AT], merge_minutes)
        parts.append(g)
    ctrl = pd.concat(parts, ignore_index=True)

    ctrl[S.EVENT] = ctrl[S.LOT] + "#" + ctrl["_g"].astype(int).astype(str)
    ctrl[S.RUN] = ctrl[S.LOT] + "@" + ctrl["_r"].astype(int).astype(str)
    ctrl[S.DELTA] = ctrl[S.VALUE] - ctrl[S.PREV_VALUE]
    ctrl[S.ZONE] = ctrl[S.ITEM].map(_zone_of)

    ev = (
        ctrl.groupby([S.LOT, S.EVENT], as_index=False)
        .agg(**{
            S.AT: (S.AT, "min"),
            S.LAST_AT: (S.AT, "max"),
            # 앵커 묶음은 연쇄 묶음의 세분이므로 한 이벤트는 한 run 안에만 있다.
            S.RUN: (S.RUN, "first"),
            "n_items": (S.ITEM, "nunique"),
        })
        .sort_values([S.LOT, S.AT])
        .reset_index(drop=True)
    )
    ev[S.SPAN] = (ev[S.LAST_AT] - ev[S.AT]) / pd.Timedelta(minutes=1)
    ev = ev[EVENT_COLS]
    dl = ctrl[[S.EVENT, S.ITEM, S.ZONE, S.DELTA]].reset_index(drop=True)
    return ev, dl
```

- [ ] **Step 5: Run the new tests**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_events.py -k "anchor or chaining or run_id or last_at" -v
```

Expected: 4개 PASS

- [ ] **Step 6: Fix the test whose premise was inverted**

`test_merge_sensitivity_shows_data_lost_to_a_narrow_window` 는 연쇄 동작(`merge=6` 에서 `[0,5,10]` 이 한 묶음)을 검증하고 있다. 앵커에서는 `{0,5}`, `{10}` 으로 갈린다. 이 테스트를 다음으로 교체한다:

```python
def test_merge_sensitivity_shows_data_lost_to_a_narrow_window():
    """병합창이 좁으면 한 번의 조작이 쪼개지고, 쪼개진 것들이 서로를 탈락시킨다.

    앵커 병합에서는 창을 넓혀도 span 상한이 같이 넓어질 뿐 연쇄는 없다.
    그래도 "넓히면 쓸 수 있는 것이 는다" 는 여전히 참이다 - 한 순간의 조작이
    분 단위로 흩어져 기록됐을 때 그것을 하나로 보게 되기 때문이다.
    """
    ch = _chg([0, 5, 10, 300, 303, 600])
    t = ev_mod.merge_sensitivity(ch, merge_windows=(2, 6), pre_minutes=30,
                                 post_minutes=60).set_index("merge_minutes")
    assert t.loc[2, "n_clusters"] == 6
    assert t.loc[6, "n_clusters"] == 4      # {0,5} {10} {300,303} {600}
    assert t.loc[6, "n_isolated"] >= t.loc[2, "n_isolated"]
    assert t.loc[6, "n_items"] >= t.loc[2, "n_items"]
```

- [ ] **Step 7: Run the whole events + report + dashboard test set**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_events.py tests/test_coating_events_settling.py tests/test_coating_report.py -v
```

Expected: 전부 PASS. 실패하면 그 테스트가 연쇄를 전제하는지 확인하고, 전제가 뒤집힌 것이면 위와 같은 방식으로 고친다.

- [ ] **Step 8: Commit**

```bash
git add backend/app/coating/schemas.py backend/app/coating/events.py backend/tests/test_coating_events.py
git commit -m "$(cat <<'EOF'
feat(coating): 묶음을 직전 변경이 아니라 앵커에서 잰다

diff() 는 직전 변경과의 간격이라 2분씩 이어지면 묶음이 무한히 이어붙었다.
8분에 걸친 여섯 번의 손질이 "t0 의 한 번의 계단 입력" 으로 읽히고, 그 상태로
격리를 통과해 L 을 span 만큼 오염시킨다.

앵커에서 재면 모든 묶음의 span 이 merge_minutes 를 넘지 못한다. 구 규칙의
연쇄 묶음은 run_id 로 남겨 둔다 - 배제에는 안 쓰고, 쪼개진 조각들이 원래
한 덩어리였음을 로그에서 보이는 용도다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

### Task 3: 격리를 끝 기준으로, 탈락 사유를 문장으로

간격을 이벤트 **시작**에서 재면 span 만큼 실제보다 길게 잡힌다. 끝에서 잰다. 동시에 "무엇이 얼마나 모자랐나" 를 문자열로 남긴다 — 원장과 렌더러가 같은 문장을 쓰게 하려면 여기서 만들어야 한다.

`last_at` 열이 **없는** 프레임을 받으면 시작 기준으로 되돌아간다. 이건 하위 호환이 아니라 Task 8이 구 규칙을 재현하는 방법이다.

**Files:**
- Modify: `backend/app/coating/events.py` (`isolation`)
- Test: `backend/tests/test_coating_events.py`

**Interfaces:**
- Consumes: Task 2의 `S.LAST_AT`, `S.ISO_REASON`
- Produces: `events.isolation(events_df, pre_minutes, post_minutes, bounds=None) -> DataFrame`
  입력 열 + `["gap_before", "gap_after", "isolated", "iso_reason"]`.
  `last_at` 이 없으면 `worked_at` 을 끝으로 본다(= 시작 기준).

- [ ] **Step 1: Write the failing tests**

`backend/tests/test_coating_events.py` 의 `test_isolation_measures_gaps_to_the_neighbours` 아래에 추가:

```python
def _ev_span(offsets_spans, lot="L1"):
    """(시작분, span분) 목록으로 last_at 이 있는 이벤트 표를 만든다."""
    base = pd.Timestamp("2026-02-02 06:00")
    return pd.DataFrame([
        {SS.LOT: lot, SS.EVENT: f"{lot}#{i+1}",
         SS.AT: base + pd.Timedelta(minutes=m),
         SS.LAST_AT: base + pd.Timedelta(minutes=m + sp),
         SS.RUN: f"{lot}@{i+1}", SS.SPAN: float(sp), "n_items": 1}
        for i, (m, sp) in enumerate(offsets_spans)
    ])


def test_isolation_measures_gaps_from_the_end_of_the_event():
    """뒤 간격은 이 묶음의 **마지막** 변경에서 다음 묶음의 첫 변경까지다.

    시작에서 재면 span 만큼 조용한 시간을 실제보다 길게 잡는다.
    """
    out = ev_mod.isolation(_ev_span([(0, 2), (30, 0)]), pre_minutes=10, post_minutes=10)
    assert out.loc[0, "gap_after"] == 28.0     # 시작 기준이면 30
    assert out.loc[1, "gap_before"] == 28.0


def test_isolation_falls_back_to_start_when_last_at_is_absent():
    """last_at 이 없는 표는 시작 기준으로 잰다 - 구 규칙 재현에 쓴다."""
    out = ev_mod.isolation(_ev([0, 30]), pre_minutes=10, post_minutes=10)
    assert out.loc[0, "gap_after"] == 30.0


def test_isolation_reason_says_what_was_short():
    """창을 몇으로 바꾸면 살아나는지가 그 줄에서 읽혀야 한다."""
    out = ev_mod.isolation(_ev_span([(0, 0), (5, 0), (100, 0)]),
                           pre_minutes=10, post_minutes=10)
    assert out.loc[0, SS.ISO_REASON] == "뒤 5.0<10"
    assert out.loc[1, SS.ISO_REASON].startswith("앞뒤")
    assert out.loc[2, SS.ISO_REASON] is None
```

- [ ] **Step 2: Run tests to verify they fail**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_events.py -k "from_the_end or falls_back or reason_says" -v
```

Expected: FAIL — `KeyError: 'iso_reason'` / `assert 30.0 == 28.0`

- [ ] **Step 3: Rewrite isolation**

`backend/app/coating/events.py` 의 `isolation` 본문(docstring 아래 전부)을 다음으로 교체한다. docstring 은 유지하되 마지막에 한 문단을 덧붙인다:

```python
    간격은 **끝**에서 잰다. 시작에서 재면 묶음이 span 을 가질 때 조용한 시간을
    그만큼 길게 잡는다. `last_at` 이 없는 표는 시작 기준으로 돌아가는데, 그것이
    구 규칙을 그대로 재현하는 방법이다(trace.rule_comparison).
    """
    cols = ["gap_before", "gap_after", "isolated", S.ISO_REASON]
    if events_df.empty:
        out = events_df.copy()
        for c in cols:
            out[c] = pd.Series(dtype="bool" if c == "isolated" else "object")
        return out

    out = events_df.sort_values([S.LOT, S.AT]).reset_index(drop=True).copy()
    end = out[S.LAST_AT] if S.LAST_AT in out.columns else out[S.AT]
    prev_end = end.groupby(out[S.LOT]).shift(1)
    next_at = out.groupby(S.LOT)[S.AT].shift(-1)

    if bounds is not None and not bounds.empty:
        b = bounds.set_index(S.LOT)
        # 이웃이 없는 쪽은 lot 경계가 대신 벽이 된다.
        prev_end = prev_end.fillna(out[S.LOT].map(b["start"]))
        next_at = next_at.fillna(out[S.LOT].map(b["end"]))

    minute = pd.Timedelta(minutes=1)
    out["gap_before"] = (out[S.AT] - prev_end) / minute
    out["gap_after"] = (next_at - end) / minute
    # 이웃도 경계도 없으면 제약이 없는 것이다. 없는 이웃을 이유로 버리지 않는다.
    out["isolated"] = (
        out["gap_before"].fillna(np.inf) >= pre_minutes
    ) & (out["gap_after"].fillna(np.inf) >= post_minutes)
    out[S.ISO_REASON] = [
        _iso_reason(gb, ga, pre_minutes, post_minutes)
        for gb, ga in zip(out["gap_before"], out["gap_after"])
    ]
    return out
```

그리고 `isolation` 바로 위에 헬퍼를 추가한다:

```python
def _iso_reason(gb, ga, pre, post) -> str | None:
    """무엇이 얼마나 모자랐나. ★순수

    "탈락" 만 적으면 창을 몇으로 바꿔야 살아나는지 알 수 없다. 부족분을 숫자로
    남겨야 그 줄 하나로 다음 설정이 정해진다.
    """
    short_before = pd.notna(gb) and gb < pre
    short_after = pd.notna(ga) and ga < post
    if short_before and short_after:
        return f"앞뒤 {gb:.1f}/{ga:.1f}<{pre}/{post}"
    if short_before:
        return f"앞 {gb:.1f}<{pre}"
    if short_after:
        return f"뒤 {ga:.1f}<{post}"
    return None
```

- [ ] **Step 4: Run the isolation tests**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_events.py -v
```

Expected: 전부 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/coating/events.py backend/tests/test_coating_events.py
git commit -m "$(cat <<'EOF'
feat(coating): 격리 간격을 묶음의 끝에서 재고 부족분을 문장으로 남긴다

시작에서 재면 묶음이 span 을 가질 때 조용한 시간을 그만큼 길게 잡는다.
last_at 이 없는 표는 시작 기준으로 되돌아가는데, 그게 구 규칙을 재현하는
방법이다.

탈락 사유는 "뒤 1.5<10" 처럼 부족분을 담는다. "탈락" 만 적으면 창을 몇으로
바꿔야 살아나는지 그 줄에서 읽히지 않는다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

### Task 4: 설정값 교체

뒤 격리 10분과 응답창 60분이 같이 서면, 응답으로 읽는 60분 중 50분은 다른 조정이 섞여 있어도 통과한다. **뒤 격리 = 응답창** 불변식을 세우고 딸린 값들을 함께 내린다. ΔWet 평균창은 신설한다 — `coating_settle_window_minutes` 를 재사용하면 `annotate_settling` 의 이동창과 뜻이 엉킨다.

**Files:**
- Modify: `backend/app/config.py:110-122`
- Modify: `backend/.env.example:100-109`
- Modify: `backend/app/coating/events.py` (`isolation_table` 기본 windows)
- Test: `backend/tests/test_config.py`

**Interfaces:**
- Consumes: 없음
- Produces: `Settings.coating_delta_window_minutes: int = 3` (신설). 나머지는 기본값 변경.

- [ ] **Step 1: Write the failing test**

`backend/tests/test_config.py` 끝에 추가:

```python
def test_coating_isolation_window_matches_the_response_window():
    """뒤 격리가 응답창보다 짧으면, 응답으로 읽는 구간 일부가 다른 조정에
    오염돼 있어도 통과한다. 그건 이 이벤트의 응답이 아니다."""
    from app.config import Settings

    s = Settings()
    assert s.coating_isolation_post_minutes == s.coating_response_post_minutes


def test_coating_delta_window_fits_inside_the_response_window():
    """ΔWet 은 앞뒤 delta_window 분 평균으로 잰다. 둘이 겹치면 같은 구간을
    빼는 셈이라 Δ 가 0 으로 눌린다."""
    from app.config import Settings

    s = Settings()
    assert s.coating_delta_window_minutes * 2 < s.coating_response_post_minutes
```

- [ ] **Step 2: Run tests to verify they fail**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_config.py -k coating -v
```

Expected: 두 번째 테스트가 FAIL — `AttributeError: 'Settings' object has no attribute 'coating_delta_window_minutes'`.

첫 번째(`..._matches_the_response_window`)는 지금도 통과한다 — `isolation_post=60, response_post=60` 이라 우연히 같기 때문이다. 그건 결함이 아니라 이 테스트의 성격이다: **변경 감지기가 아니라 불변식 가드**다. 두 값이 앞으로 따로 움직이는 순간 실패해야 하고, 지금 통과한다고 지울 이유가 없다.

- [ ] **Step 3: Change the settings**

`backend/app/config.py` 에서 해당 블록을 다음으로 교체한다:

```python
    # 이벤트 정렬 창. pre 는 기준선 이전 구간을 눈으로 보기 위한 것이고,
    # post 는 반응을 관측할 길이다.
    #
    # post 는 **뒤 격리와 같은 값이어야 한다**. 뒤 10분만 조용함을 보장하면서
    # 60분을 응답으로 읽으면, 그 50분에 다른 조정이 섞여 있어도 통과한다.
    # 그건 이 이벤트의 응답이 아니다.
    coating_response_pre_minutes: int = 10
    coating_response_post_minutes: int = 10
    # ΔWet 을 재는 앞뒤 평균창. 한 시점끼리 빼면 측정 노이즈가 그대로 신호에 섞인다.
    # after = mean[t1+post-w, t1+post], before = mean[t0-w, t0).
    # coating_settle_window_minutes 를 재사용하지 않는 이유: 그건 annotate_settling
    # 의 이동창 폭이고 계속 그 용도로 남는다. 뜻이 다른 두 창이 한 설정을 공유하면
    # 한쪽을 조정할 때 다른 쪽이 조용히 따라 움직인다.
    coating_delta_window_minutes: int = 3
    # 제어 구간 격리 — "이 이벤트가 홀로 섰는가" 를 이벤트 시각만으로 가른다.
    # 정착(annotate_settling)으로 가르면 순환에 빠진다: 정착을 제대로 잡으려면
    # 순수 지연 L 을 알아야 하는데 L 을 재려면 깨끗한 이벤트가 먼저 필요하다.
    # 앞뒤로 다른 조정이 없었다는 것은 Wet 을 한 번도 안 보고 정해지므로 그
    # 고리를 끊는다.
    #
    # 앞뒤를 같은 값으로 둔다. "한 번의 조작이 홀로 섰는가" 는 대칭인 질문이고,
    # 뒤쪽은 위 불변식에 따라 응답창과 묶여 있다.
    coating_isolation_pre_minutes: int = 10
    coating_isolation_post_minutes: int = 10
```

그리고 `coating_settle_max_wait_minutes` 를 찾아 `= 10` 으로 바꾸고 주석을 덧붙인다:

```python
    # 응답창(10분)을 넘겨 정착을 기다리면 다음 이벤트의 영역을 보게 된다.
    coating_settle_max_wait_minutes: int = 10
```

- [ ] **Step 4: Mirror into .env.example**

`backend/.env.example` 의 해당 블록을 다음으로 교체한다:

```
# 이벤트 정렬 창(분). post 는 반응을 관측할 길이다.
# post 는 COATING_ISOLATION_POST_MINUTES 와 **같은 값이어야 한다** - 뒤 10분만
# 조용함을 보장하면서 60분을 응답으로 읽으면 그 50분이 오염돼 있어도 통과한다.
COATING_RESPONSE_PRE_MINUTES=10
COATING_RESPONSE_POST_MINUTES=10
# ΔWet 을 재는 앞뒤 평균창(분). 한 시점끼리 빼면 측정 노이즈가 신호에 섞인다.
# 2배가 응답창보다 짧아야 한다 - 앞뒤 창이 겹치면 Δ 가 0 으로 눌린다.
COATING_DELTA_WINDOW_MINUTES=3
# 제어 구간 격리 — 앞뒤로 다른 조정이 이만큼 없어야 "홀로 선 이벤트" 로 본다.
# Wet 을 안 보고 이벤트 시각만으로 정하므로 L 을 몰라도 쓸 수 있고, 그래서
# L 을 재는 데 쓸 수 있다. 간격은 묶음의 **끝** 에서 잰다.
# 리포트/진단의 격리 표를 보고 정한다 - 넓히면 깨끗해지고 좁히면 표본이 는다.
COATING_ISOLATION_PRE_MINUTES=10
COATING_ISOLATION_POST_MINUTES=10
```

그리고 `COATING_SETTLE_MAX_WAIT_MINUTES=30` 을 `COATING_SETTLE_MAX_WAIT_MINUTES=10` 으로 바꾼다.

- [ ] **Step 5: Move the isolation_table window range down**

`backend/app/coating/events.py` 의 `isolation_table` 시그니처에서

```python
    windows=(10, 20, 30, 45, 60),
    pre_ratio: float = 0.5,
```

를 다음으로 바꾸고, docstring 의 `pre_ratio` 설명 문단을 교체한다:

```python
    windows=(5, 10, 15, 20, 30),
    pre_ratio: float = 1.0,
```

docstring 마지막 문단을 다음으로 교체:

```
    앞뒤 창을 같게 둔다(pre_ratio=1.0). "이 조작이 홀로 섰는가" 는 대칭인 질문이고,
    뒤쪽은 응답창과 묶여 있으므로 앞만 따로 줄일 근거가 없다. 비대칭이 필요하면
    isolation() 을 직접 부른다.
```

- [ ] **Step 6: Run the affected tests**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_config.py tests/test_coating_events.py -v
```

Expected: 전부 PASS. `test_isolation_table_shows_the_tradeoff` 가 `windows=(10, 30, 60)` 를 명시로 넘기므로 영향이 없다.

- [ ] **Step 7: Commit**

```bash
git add backend/app/config.py backend/.env.example backend/app/coating/events.py backend/tests/test_config.py
git commit -m "$(cat <<'EOF'
feat(coating): 뒤 격리와 응답창을 같은 수로 묶는다

뒤 10분만 조용함을 보장하면서 60분을 응답으로 읽으면, 그 50분에 다른 조정이
섞여 있어도 통과한다. 그건 이 이벤트의 응답이 아니다. 둘을 10분으로 묶고
딸린 값들(response_pre, settle_max_wait, isolation_table 범위)을 함께 내린다.

ΔWet 평균창은 COATING_DELTA_WINDOW_MINUTES 로 신설한다. settle_window 를
재사용하면 annotate_settling 의 이동창과 뜻이 엉켜, 한쪽을 조정할 때 다른
쪽이 조용히 따라 움직인다.

10분은 실측이 아니라 가정이다. 자기반증 절(Task 10)이 매 실행마다 검사한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

### Task 5: 선별을 격리 하나로 통일

같은 파이프라인에 "쓸 수 있는 이벤트" 정의가 둘이다 — 커널은 정착 판정, 동특성은 격리. 정착 판정은 L 을 알아야 서는데 L 을 거기서 잰다(순환). 격리 하나로 통일한다. `annotate_settling` 자체는 튜닝 종료 교차검증용으로 남긴다.

**Files:**
- Modify: `backend/app/coating/features.py` (`delta_samples`, `_window_mean`)
- Modify: `backend/app/coating/response.py:79` (`align_events`)
- Modify: `backend/app/coating/report.py:85-110` (`profile_readings` 배선)
- Test: `backend/tests/test_coating_features.py`, `backend/tests/test_coating_response.py`

**Interfaces:**
- Consumes: Task 2의 `S.LAST_AT`, Task 3의 `isolation`, Task 4의 `coating_delta_window_minutes`
- Produces:
  - `features.delta_samples(events_df, event_deltas, wet, valid, post_minutes, delta_window_minutes) -> DataFrame`
    — `CONTAMINATED` 를 보지 않는다. 호출자가 이미 격리로 거른 표를 준다.
  - `features._window_mean(wet, lot_id, start, end, zone_cols, end_open=False)`

- [ ] **Step 1: Write the failing tests**

`backend/tests/test_coating_features.py` 의 `test_delta_samples_only_use_clean_events` 를 다음으로 **교체**한다(정착 기반 선별이 사라지므로 전제가 뒤집힌다):

```python
def test_delta_samples_do_not_look_at_contamination():
    """선별은 호출자(격리)가 끝내고 온다. 여기서 정착 판정이 다시 끼어들면
    격리로 고른 뜻이 사라진다."""
    ev = pd.DataFrame({
        S.LOT: ["L1", "L1"],
        S.EVENT: ["L1#1", "L1#2"],
        S.AT: pd.to_datetime(["2026-01-31 19:00", "2026-01-31 19:20"]),
        S.LAST_AT: pd.to_datetime(["2026-01-31 19:00", "2026-01-31 19:20"]),
        S.CONTAMINATED: [False, True],       # 있어도 무시돼야 한다
    })
    dl = pd.DataFrame({
        S.EVENT: ["L1#1", "L1#2"],
        S.ITEM: ["30030838", "30030838"],
        S.ZONE: [1.0, 1.0],
        S.DELTA: [5.0, 7.0],
    })
    times = pd.to_datetime([f"2026-01-31 19:{m:02d}" for m in range(0, 40)])
    w = pd.DataFrame({S.LOT: ["L1"] * 40, S.AT: times})
    for z in range(1, 26):
        w[f"z{z}"] = [18.2] * 40
    out = features.delta_samples(
        ev, dl, w, valid=list(range(1, 26)),
        post_minutes=10, delta_window_minutes=3,
    )
    assert list(out[S.EVENT]) == ["L1#1", "L1#2"]


def test_delta_samples_read_wet_at_the_end_of_the_quiet_window():
    """after 는 보장된 조용 구간의 맨 끝 w분이다. 정착 시각에 의존하지 않는다."""
    ev = pd.DataFrame({
        S.LOT: ["L1"],
        S.EVENT: ["L1#1"],
        S.AT: pd.to_datetime(["2026-01-31 19:00"]),
        S.LAST_AT: pd.to_datetime(["2026-01-31 19:01"]),
    })
    dl = pd.DataFrame({S.EVENT: ["L1#1"], S.ITEM: ["30030838"],
                       S.ZONE: [1.0], S.DELTA: [5.0]})
    times = pd.to_datetime([f"2026-01-31 19:{m:02d}" for m in range(0, 30)])
    w = pd.DataFrame({S.LOT: ["L1"] * 30, S.AT: times})
    # 19:00 이전 18.0, 19:08 부터 19.0. after 창은 [19:01+10-3, 19:01+10] = 19:08~19:11
    for z in range(1, 26):
        w[f"z{z}"] = [18.0 if t < pd.Timestamp("2026-01-31 19:08") else 19.0
                      for t in times]
    out = features.delta_samples(
        ev, dl, w, valid=list(range(1, 26)),
        post_minutes=10, delta_window_minutes=3,
    )
    assert out.iloc[0]["dw1"] == pytest.approx(1.0)
    assert out.iloc[0]["dg1"] == 5.0
```

- [ ] **Step 2: Run tests to verify they fail**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_features.py -k "contamination or quiet_window" -v
```

Expected: FAIL — `TypeError: delta_samples() got an unexpected keyword argument 'post_minutes'`

- [ ] **Step 3: Rewrite delta_samples**

`backend/app/coating/features.py` 의 `delta_samples` 시그니처와 앞부분을 다음으로 교체한다:

```python
def delta_samples(
    events_df: pd.DataFrame,
    event_deltas: pd.DataFrame,
    wet: pd.DataFrame,
    valid: list[int],
    post_minutes: int,
    delta_window_minutes: int,
) -> pd.DataFrame:
    """쓸 수 있는 이벤트마다 (Δgap 25, ΔWet 25) 한 행.

    ΔWet 은 `mean[t1+post-w, t1+post] − mean[t0-w, t0)` 이다.
    t0 = 묶음의 첫 변경, t1 = 마지막 변경, w = delta_window_minutes.
    한 시점끼리 빼면 측정 노이즈가 그대로 신호에 섞인다.

    **선별은 하지 않는다.** 호출자가 격리로 거른 표를 준다. 여기서 정착 판정
    (contaminated)을 다시 보면 격리로 고른 뜻이 사라지고, 정착은 L 을 알아야
    서는데 L 을 그 뒤에서 재므로 순환이 돌아온다.
    """
    zone_cols = [S.zone_col(z) for z in valid]
    win = pd.Timedelta(minutes=delta_window_minutes)
    post = pd.Timedelta(minutes=post_minutes)

    rows = []
    for _, e in events_df.iterrows():
        t0 = e[S.AT]
        t1 = e[S.LAST_AT] if S.LAST_AT in events_df.columns else t0
        before = _window_mean(wet, e[S.LOT], t0 - win, t0, zone_cols, end_open=True)
        after = _window_mean(
            wet, e[S.LOT], t1 + post - win, t1 + post, zone_cols
        )
        if before is None or after is None:
            continue
        row = {S.EVENT: e[S.EVENT], S.LOT: e[S.LOT], S.AT: t0}
        for z in range(1, S.N_ZONES + 1):
            col = S.zone_col(z)
            row[WET_DELTA_COLS[z - 1]] = (
                after.get(col, np.nan) - before.get(col, np.nan)
            )
            row[GAP_DELTA_COLS[z - 1]] = 0.0
        rows.append(row)
```

(`out = pd.DataFrame(rows)` 이후는 Task 1에서 고친 그대로 둔다.)

그리고 `_window_mean` 을 다음으로 교체한다:

```python
def _window_mean(wet, lot_id, start, end, zone_cols, end_open: bool = False):
    """[start, end] 구간의 zone 평균. end_open 이면 end 를 제외한다.

    기준선 창은 조정이 일어난 분 자체를 포함하면 안 된다 - 그 분의 값에는 이미
    조정의 영향이 섞여 있을 수 있다.
    """
    upper = (wet[S.AT] < end) if end_open else (wet[S.AT] <= end)
    g = wet[(wet[S.LOT] == lot_id) & (wet[S.AT] >= start) & upper]
    if g.empty:
        return None
    return g[zone_cols].mean(axis=0, skipna=True).to_dict()
```

- [ ] **Step 4: Drop the contamination filter from align_events**

`backend/app/coating/response.py:79` 의

```python
    clean = events[~events[S.CONTAMINATED].astype(bool)] if S.CONTAMINATED in events else events
```

를 다음으로 바꾼다:

```python
    # 선별은 호출자(격리)가 끝내고 온다. 여기서 정착 판정을 보면 격리로 고른
    # 뜻이 사라지고, 정착은 L 을 알아야 서는데 L 을 여기서 잰다(순환).
    clean = events
```

- [ ] **Step 5: Rewire report.profile_readings**

`backend/app/coating/report.py` 의 `profile_readings` 에서 `ev, dl = ev_mod.build_events(...)` 부터 `tables["07_delta_samples"] = ds` 까지를 다음으로 교체한다:

```python
    ev, dl = ev_mod.build_events(changes, s.coating_event_merge_minutes)
    bounds = segment.lot_bounds(deduped)
    if tables is not None:
        # 격리 판정이 붙기 **전** 의 이벤트다. 10 과 나란히 놓고 보면 어느 것이
        # 왜 버려졌는지가 두 파일의 차이로 드러난다.
        tables["01_changes_control"] = changes[changes[S.ITEM].isin(S.CONTROL_ITEM_IDS)]
        tables["02_events"] = ev
        tables["03_event_deltas"] = dl

    iso = ev_mod.isolation(
        ev, s.coating_isolation_pre_minutes, s.coating_isolation_post_minutes, bounds
    ) if not ev.empty else ev
    usable = iso[iso["isolated"]] if len(iso) else iso

    if not ev.empty:
        # 선별에는 안 쓴다. 튜닝 종료 교차검증과 오염 비율 진단용으로만 남긴다.
        ev = ev_mod.annotate_settling(
            ev, wm, s.coating_settle_std_max,
            s.coating_settle_window_minutes, s.coating_settle_max_wait_minutes,
        )
        ds = features.delta_samples(
            usable, dl, wet, valid,
            s.coating_response_post_minutes, s.coating_delta_window_minutes,
        )
    else:
        ds = pd.DataFrame(columns=features.GAP_DELTA_COLS)
    if tables is not None:
        tables["04_events_settled"] = ev
        tables["07_delta_samples"] = ds
```

이어서 `facts` 딕셔너리에서 `n_clean_events` 를 다음으로 바꾼다:

```python
        # "쓸 수 있는 이벤트" 는 이제 격리 통과 건수 하나뿐이다. 정착 기반
        # contaminated_ratio 는 진단으로만 남는다.
        "n_clean_events": int(usable["isolated"].sum()) if len(usable) else 0,
```

그리고 아래쪽의 `bounds = segment.lot_bounds(deduped)` 한 줄은 위로 옮겼으므로 **삭제**한다.

- [ ] **Step 6: Run the full coating suite**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/ -k coating -v
```

Expected: 전부 PASS.

**Task 1이 추가한 `test_delta_samples_sums_repeated_adjustments_of_one_zone` 은 반드시 고쳐야 한다** — 옛 시그니처(`window_minutes=3`)로 부르고 `SETTLED_AT` 에 기대고 있다. 다음으로 교체한다:

```python
def test_delta_samples_sums_repeated_adjustments_of_one_zone():
    """한 이벤트 안에서 같은 zone 을 두 번 만졌으면 Δgap 은 합이다.

    덮어쓰면 마지막 것만 남아 Δgap 이 작아지는데, ΔWet 은 전체 변화를 담으므로
    게인이 그 비율만큼 부풀려진다.
    """
    ev = pd.DataFrame({
        S.LOT: ["L1"],
        S.EVENT: ["L1#1"],
        S.AT: pd.to_datetime(["2026-01-31 19:00"]),
        S.LAST_AT: pd.to_datetime(["2026-01-31 19:01"]),
    })
    # 같은 zone1 을 +1 씩 세 번 = 총 +3
    dl = pd.DataFrame({
        S.EVENT: ["L1#1"] * 3,
        S.ITEM: ["30030838"] * 3,
        S.ZONE: [1.0, 1.0, 1.0],
        S.DELTA: [1.0, 1.0, 1.0],
    })
    times = pd.to_datetime([f"2026-01-31 19:{m:02d}" for m in range(0, 30)])
    w = pd.DataFrame({S.LOT: ["L1"] * 30, S.AT: times})
    for z in range(1, 26):
        w[f"z{z}"] = [18.2] * 30
    out = features.delta_samples(
        ev, dl, w, valid=list(range(1, 26)),
        post_minutes=10, delta_window_minutes=3,
    )
    assert out.iloc[0]["dg1"] == 3.0
```

다른 테스트가 `delta_samples` 를 옛 시그니처로 부르면 같은 방식으로 고친다.

- [ ] **Step 7: Commit**

```bash
git add backend/app/coating/features.py backend/app/coating/response.py backend/app/coating/report.py backend/tests/
git commit -m "$(cat <<'EOF'
feat(coating): "쓸 수 있는 이벤트" 정의를 격리 하나로 남긴다

커널은 정착 판정으로, 동특성은 격리로 각각 고르고 있었다. 리포트가 내는
"쓸 수 있는 이벤트 N건" 이 절마다 다른 뜻이었다는 얘기다. 정착 판정은 L 을
알아야 서는데 L 을 그 뒤에서 재므로 순환이기도 하다.

delta_samples 와 align_events 에서 contaminated 필터를 뗀다. 선별은 호출자가
격리로 끝내고 온다. ΔWet 은 정착 시각 대신 보장된 조용 구간의 맨 끝 3분에서
잰다. annotate_settling 은 튜닝 종료 교차검증용으로 남는다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

### Task 6: 원장 — 묶음 하나당 한 줄

로그의 뼈대다. 사용자가 한 줄을 집어 `01_changes_control.csv` 와 손으로 대조할 수 있어야 하므로 원시 타임스탬프를 그대로 싣는다.

**Files:**
- Create: `backend/app/coating/trace.py`
- Test: `backend/tests/test_coating_trace.py` (신설)

**Interfaces:**
- Consumes: Task 2의 `build_events` 출력, Task 3의 `isolation` 출력
- Produces: `trace.event_ledger(iso, event_deltas) -> DataFrame`
  열: `[event_id, lot_id, run_id, worked_at, last_at, span_min, n_items, n_dup_zones, gap_before, gap_after, zones, isolated, iso_reason]`
  - `run_id` 는 그 run 에 이벤트가 하나뿐이면 `""`
  - `zones` 는 `"z3,z7,z12"` (조정된 zone 오름차순, 전부)
  - `n_dup_zones` 는 그 이벤트 안에서 **두 번 이상** 만져진 zone 의 개수

- [ ] **Step 1: Write the failing test**

`backend/tests/test_coating_trace.py` 를 만든다:

```python
"""전처리 로그 — 사람이 눈으로 검증하려면 각 줄이 손검증 가능해야 한다.

실데이터는 사내 PC 에만 있고 여기서는 볼 수 없다(CLAUDE.md). 그래서 이 표들의
계약을 합성으로 못박아 둔다 - 열이 조용히 바뀌면 사용자가 읽는 법을 잃는다.
"""
import pandas as pd
import pytest

from app.coating import events as ev_mod
from app.coating import schemas as S
from app.coating import trace

BASE = pd.Timestamp("2026-02-02 06:00")


def _changes(rows, lot="L1"):
    """(분, zone인덱스, prev, value) 목록으로 제어값 변경 표를 만든다."""
    return pd.DataFrame([
        {S.LOT: lot, S.ITEM: S.GAP_ITEM_IDS[zi], S.AT: BASE + pd.Timedelta(minutes=m),
         S.VALUE: v, S.PREV_VALUE: p}
        for m, zi, p, v in rows
    ])


def _bounds(lot="L1", start_min=-30, end_min=300):
    return pd.DataFrame([{
        S.LOT: lot,
        "start": BASE + pd.Timedelta(minutes=start_min),
        "end": BASE + pd.Timedelta(minutes=end_min),
    }])


def _pipeline(changes, pre=10, post=10, merge=2):
    ev, dl = ev_mod.build_events(changes, merge)
    iso = ev_mod.isolation(ev, pre, post, _bounds())
    return ev, dl, iso


def test_ledger_has_one_row_per_event_with_raw_timestamps():
    """원시 시각이 있어야 01_changes_control.csv 와 손으로 대조할 수 있다."""
    ch = _changes([(0, 0, 40.0, 41.0), (1, 1, 40.0, 41.0), (100, 2, 40.0, 41.0)])
    ev, dl, iso = _pipeline(ch)
    led = trace.event_ledger(iso, dl)
    assert len(led) == 2
    assert led.iloc[0][S.AT] == BASE
    assert led.iloc[0][S.LAST_AT] == BASE + pd.Timedelta(minutes=1)
    assert led.iloc[0]["zones"] == "z1,z2"


def test_ledger_blanks_run_id_when_the_run_has_one_event():
    """눈에 띄어야 할 것은 쪼개진 것뿐이다."""
    ch = _changes([(0, 0, 40.0, 41.0), (100, 1, 40.0, 41.0)])
    _, dl, iso = _pipeline(ch)
    led = trace.event_ledger(iso, dl)
    assert list(led[S.RUN]) == ["", ""]


def test_ledger_keeps_run_id_when_a_run_was_split():
    # 0→1.5→3 은 직전 변경과 매번 2분 이내(= 한 연쇄)지만 전체 폭이 3분이라
    # 앵커는 둘로 쪼갠다. run_id 가 그 둘을 다시 묶어 보여야 한다.
    ch = _changes([(0, 0, 40.0, 41.0), (1.5, 1, 40.0, 41.0), (3, 2, 40.0, 41.0),
                   (100, 3, 40.0, 41.0)])
    _, dl, iso = _pipeline(ch)
    led = trace.event_ledger(iso, dl).set_index(S.EVENT)
    split = led[led[S.RUN] != ""]
    assert len(split) == 2
    assert split[S.RUN].nunique() == 1


def test_ledger_flags_a_zone_touched_twice_in_one_event():
    """Δgap 이 눌릴 수 있는 자리다. 표시가 없으면 사람이 못 찾는다."""
    ch = _changes([(0, 0, 40.0, 41.0), (1, 0, 41.0, 42.0)])
    _, dl, iso = _pipeline(ch)
    led = trace.event_ledger(iso, dl)
    assert led.iloc[0]["n_dup_zones"] == 1


def test_ledger_carries_the_isolation_verdict_and_reason():
    ch = _changes([(0, 0, 40.0, 41.0), (5, 1, 40.0, 41.0), (100, 2, 40.0, 41.0)])
    _, dl, iso = _pipeline(ch)
    led = trace.event_ledger(iso, dl)
    assert list(led["isolated"]) == [False, False, True]
    assert led.iloc[0][S.ISO_REASON] == "뒤 5.0<10"
```

- [ ] **Step 2: Run tests to verify they fail**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_trace.py -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'app.coating.trace'`

- [ ] **Step 3: Create trace.py with event_ledger**

`backend/app/coating/trace.py`:

```python
"""전처리 로그용 집계. ★순수 — 파일·설정을 만지지 않는다.

규칙(events.py)과 보고(여기)를 나눈 이유는 하나다. 규칙은 파이프라인이 매번
쓰는 것이고 이 표들은 사람이 볼 때만 필요하다. 섞어 두면 규칙을 읽을 때
렌더링 관심사가 같이 딸려온다.

이 파일의 표들은 **사용자가 혼자 읽고 혼자 판정할 수 있어야** 한다. 실데이터는
사내 PC 에만 있고 여기서는 볼 수 없으므로(CLAUDE.md), 숫자 옆에 판정 기준이
같이 붙지 않으면 그 숫자는 왕복 한 번을 더 쓰게 만든다.
"""
import pandas as pd

from app.coating import schemas as S

LEDGER_COLS = [
    S.EVENT, S.LOT, S.RUN, S.AT, S.LAST_AT, S.SPAN, "n_items",
    "n_dup_zones", "gap_before", "gap_after", "zones", "isolated", S.ISO_REASON,
]


def event_ledger(iso: pd.DataFrame, event_deltas: pd.DataFrame) -> pd.DataFrame:
    """묶음 하나당 한 줄. ★순수

    원시 타임스탬프를 그대로 싣는다. 파생된 분 수만 있으면 사람이 원본과 대조할
    방법이 없고, 대조가 안 되면 이 표는 믿을 근거가 없는 숫자 더미가 된다.
    """
    if iso.empty:
        return pd.DataFrame(columns=LEDGER_COLS)

    out = iso.copy()
    # run 이 이벤트 하나짜리면 비운다 - 눈에 띄어야 할 것은 쪼개진 것뿐이다.
    sizes = out.groupby(S.RUN)[S.EVENT].transform("size")
    out[S.RUN] = out[S.RUN].where(sizes > 1, "")

    zoned = event_deltas[event_deltas[S.ZONE].notna()].copy()
    zoned[S.ZONE] = zoned[S.ZONE].astype(int)
    per = zoned.groupby([S.EVENT, S.ZONE]).size().rename("k").reset_index()

    zones = (
        per.sort_values([S.EVENT, S.ZONE])
        .groupby(S.EVENT)[S.ZONE]
        .apply(lambda z: ",".join(f"z{int(v)}" for v in z))
    )
    dups = per[per["k"] > 1].groupby(S.EVENT).size()

    out["zones"] = out[S.EVENT].map(zones).fillna("")
    out["n_dup_zones"] = out[S.EVENT].map(dups).fillna(0).astype(int)
    for c in LEDGER_COLS:
        if c not in out.columns:
            out[c] = None
    return out[LEDGER_COLS].reset_index(drop=True)
```

- [ ] **Step 4: Run tests to verify they pass**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_trace.py -v
```

Expected: 6개 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/coating/trace.py backend/tests/test_coating_trace.py
git commit -m "$(cat <<'EOF'
feat(coating): 묶음 하나당 한 줄인 원장을 낸다

전처리 로그의 뼈대다. 원시 타임스탬프를 그대로 실어 01_changes_control.csv 와
손으로 대조할 수 있게 한다 - 파생된 분 수만 있으면 믿을 근거가 없다.

run_id 는 쪼개진 것에만 남기고, 한 묶음 안에서 두 번 이상 만져진 zone 수를
따로 센다. 거기가 Δgap 이 눌릴 수 있는 자리다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

### Task 7: 깔때기 — 행이 어디서 죽었나

원본 100만 행이 이벤트 31건이 되는 과정에서 **이름 없는 감소가 하나도 없어야** 한다. 96%가 한 줄에서 사라져도 그 줄 이름이 "값이 바뀐 시점만" 이면 놀랄 일이 아니다.

**Files:**
- Modify: `backend/app/coating/trace.py`
- Test: `backend/tests/test_coating_trace.py`

**Interfaces:**
- Consumes: Task 6의 `trace` 모듈
- Produces: `trace.funnel(readings, deduped, changes, iso) -> DataFrame`
  열: `[stage, n, delta, note]`. `delta` 는 직전 단계 대비 증감(첫 줄은 `None`),
  `note` 는 사람이 읽을 부연(없으면 `""`).

- [ ] **Step 1: Write the failing test**

`backend/tests/test_coating_trace.py` 끝에 추가:

```python
def test_funnel_accounts_for_every_drop():
    """단계마다 이름이 붙고, 증감이 앞 단계와 이어져야 한다."""
    ch = _changes([(0, 0, 40.0, 41.0), (5, 1, 40.0, 41.0), (100, 2, 40.0, 41.0)])
    ev, dl, iso = _pipeline(ch)
    readings = pd.DataFrame({S.LOT: ["L1"] * 50})
    deduped = pd.DataFrame({S.LOT: ["L1"] * 20})
    f = trace.funnel(readings, deduped, ch, iso)
    stages = list(f["stage"])
    assert stages[0] == "원본 행"
    assert f.iloc[0]["n"] == 50
    assert f.iloc[1]["n"] == 20
    assert f.iloc[1]["delta"] == -30
    # 마지막은 격리 통과
    assert stages[-1] == "격리 통과"
    assert f.iloc[-1]["n"] == 1


def test_funnel_names_the_rejection_breakdown():
    """탈락을 앞/뒤/양쪽으로 갈라야 창을 어느 쪽으로 움직일지 정해진다."""
    ch = _changes([(0, 0, 40.0, 41.0), (5, 1, 40.0, 41.0), (100, 2, 40.0, 41.0)])
    _, _, iso = _pipeline(ch)
    f = trace.funnel(pd.DataFrame({S.LOT: []}), pd.DataFrame({S.LOT: []}), ch, iso)
    note = f[f["stage"] == "격리 통과"].iloc[0]["note"]
    assert "앞" in note and "뒤" in note


def test_funnel_counts_fragments_of_continuous_runs():
    # 0→1.5→3 은 한 연쇄인데 앵커가 둘로 쪼갠다. 그 둘이 "조각" 이다.
    ch = _changes([(0, 0, 40.0, 41.0), (1.5, 1, 40.0, 41.0), (3, 2, 40.0, 41.0),
                   (100, 3, 40.0, 41.0)])
    _, _, iso = _pipeline(ch)
    f = trace.funnel(pd.DataFrame({S.LOT: []}), pd.DataFrame({S.LOT: []}), ch, iso)
    row = f[f["stage"] == "연속 조작 구간 조각"].iloc[0]
    assert row["n"] == 2
```

- [ ] **Step 2: Run tests to verify they fail**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_trace.py -k funnel -v
```

Expected: FAIL — `AttributeError: module 'app.coating.trace' has no attribute 'funnel'`

- [ ] **Step 3: Add funnel to trace.py**

`backend/app/coating/trace.py` 끝에 추가:

```python
FUNNEL_COLS = ["stage", "n", "delta", "note"]


def funnel(
    readings: pd.DataFrame,
    deduped: pd.DataFrame,
    changes: pd.DataFrame,
    iso: pd.DataFrame,
) -> pd.DataFrame:
    """행이 어느 단계에서 얼마나 줄었나. ★순수

    **이름 없는 감소가 없어야 한다.** 원본 100만 행이 이벤트 31건이 되는 것은
    정상인데, 어느 줄에서 얼마가 빠졌는지 이름이 붙지 않으면 사람은 그 정상을
    이상으로 읽고 파이프라인을 의심하는 데 시간을 쓴다.
    """
    ctrl = changes[changes[S.ITEM].isin(S.CONTROL_ITEM_IDS)]
    real = ctrl[ctrl[S.PREV_VALUE].notna()] if S.PREV_VALUE in ctrl.columns else ctrl

    n_events = int(len(iso))
    if n_events:
        sizes = iso.groupby(S.RUN)[S.EVENT].transform("size")
        n_frag = int((sizes > 1).sum())
        ok = iso["isolated"].astype(bool)
        n_iso = int(ok.sum())
        rej = iso.loc[~ok, S.ISO_REASON].fillna("")
        n_before = int(rej.str.startswith("앞 ").sum())
        n_after = int(rej.str.startswith("뒤 ").sum())
        n_both = int(rej.str.startswith("앞뒤").sum())
        note = f"탈락 앞 {n_before} · 뒤 {n_after} · 양쪽 {n_both}"
    else:
        n_frag = n_iso = 0
        note = "탈락 앞 0 · 뒤 0 · 양쪽 0"

    rows = [
        ("원본 행", len(readings), ""),
        ("분 중복 접기", len(deduped), ""),
        ("값이 바뀐 시점만", len(changes), ""),
        ("제어 항목만", len(ctrl), ""),
        ("시작값 제외", len(real), "lot·항목별 첫 관측은 사람이 바꾼 것이 아니다"),
        ("앵커 묶음", n_events, "묶음 수 — 위와 단위가 다르다"),
        ("연속 조작 구간 조각", n_frag, "2분 가정을 넘겨 쪼개진 것"),
        ("격리 통과", n_iso, note),
    ]
    out = pd.DataFrame(rows, columns=["stage", "n", "note"])
    # 단위가 바뀌는 자리(행 → 묶음)에서는 증감을 내지 않는다. 뺄셈이 뜻을 잃는다.
    deltas = [None]
    for i in range(1, len(out)):
        same_unit = i <= 4
        deltas.append(int(out.loc[i, "n"] - out.loc[i - 1, "n"]) if same_unit else None)
    out["delta"] = deltas
    return out[FUNNEL_COLS]
```

- [ ] **Step 4: Run tests to verify they pass**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_trace.py -v
```

Expected: 9개 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/coating/trace.py backend/tests/test_coating_trace.py
git commit -m "$(cat <<'EOF'
feat(coating): 행이 어느 단계에서 얼마나 줄었는지 이름을 붙인다

원본 100만 행이 이벤트 31건이 되는 것은 정상이다. 그런데 어느 줄에서 얼마가
빠졌는지 이름이 없으면 사람은 그 정상을 이상으로 읽고 파이프라인을 의심하는
데 시간을 쓴다.

행에서 묶음으로 단위가 바뀌는 자리에서는 증감을 내지 않는다 - 뺄셈이 뜻을
잃는다. 격리 탈락은 앞/뒤/양쪽으로 갈라 어느 쪽 창을 움직일지가 보이게 한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

### Task 8: 구/신 규칙 대조

전처리를 바꾸는 순간 가장 궁금한 것은 "몇 건이 새로 살고 몇 건이 죽었나" 다. `run_id` 가 곧 구 규칙의 묶음이므로 묶음 계산은 공짜고, 격리만 옛 파라미터로 한 번 더 돌린다. 구 규칙은 **세 가지를 다 되돌린 것** — 연쇄 병합 + 시작 기준 간격 + 앞30/뒤60. 하나만 되돌리면 무엇이 차이를 만들었는지 갈리지 않는다.

**Files:**
- Modify: `backend/app/coating/trace.py`
- Test: `backend/tests/test_coating_trace.py`

**Interfaces:**
- Consumes: Task 3의 `isolation` (last_at 없으면 시작 기준), Task 6의 `trace`
- Produces:
  - `trace.legacy_events(iso) -> DataFrame` — run 단위로 접은 구 규칙 묶음.
    `last_at` 열을 **넣지 않는다**(그래야 `isolation` 이 시작 기준으로 돈다).
  - `trace.rule_comparison(iso, event_deltas, bounds, old_pre, old_post, new_pre, new_post) -> dict`
    키: `old`/`new` (각각 `{n_clusters, n_isolated, n_items, pre, post}`), `both`, `only_new`, `only_old`, `only_old_examples`, `n_runs`
    - `new_pre`/`new_post` 는 **적용하지 않고 기록만 한다** — `iso` 는 이미 그 창으로 계산돼 왔다.
      그래도 받아서 `new` 에 실어야 출력이 "이 숫자가 어느 창에서 나왔는지" 를 스스로 말한다.
    - `only_old_examples[i]` 의 항목 수 필드는 `n_item_touches` 다. 조각별 `n_items` 의 합이라
      한 run 의 두 조각에서 같은 항목을 만졌으면 두 번 센다. `old`/`new` 의 `n_items` 와는
      보장이 다르므로 **같은 이름을 쓰지 않는다**

- [ ] **Step 1: Write the failing test**

`backend/tests/test_coating_trace.py` 끝에 추가:

```python
def test_legacy_events_fold_fragments_back_into_runs():
    """구 규칙의 묶음 = 연쇄 = run. 접으면 그대로 나온다."""
    ch = _changes([(0, 0, 40.0, 41.0), (1.5, 1, 40.0, 41.0), (3, 2, 40.0, 41.0),
                   (100, 3, 40.0, 41.0)])
    _, dl, iso = _pipeline(ch)
    old = trace.legacy_events(iso)
    assert len(old) == 2                      # 앞 셋이 한 연쇄
    assert S.LAST_AT not in old.columns       # 시작 기준으로 재게 한다


def test_rule_comparison_finds_events_only_the_old_rule_kept():
    """연쇄가 삼켰던 긴 구간이 여기서 사례로 나온다."""
    ch = _changes([(0, 0, 40.0, 41.0), (1.5, 1, 40.0, 41.0), (3, 2, 40.0, 41.0),
                   (4.5, 3, 40.0, 41.0), (200, 4, 40.0, 41.0)])
    _, dl, iso = _pipeline(ch)
    cmp = trace.rule_comparison(iso, dl, _bounds(), 30, 60, 10, 10)
    # 연쇄로 보면 0~4.5 가 한 덩어리 + 200 = 2. 앵커로 보면 {0,1.5} {3,4.5} {200} = 3.
    assert cmp["old"]["n_clusters"] == 2
    assert cmp["new"]["n_clusters"] == 3
    assert cmp["only_old"] >= 1
    assert cmp["only_old_examples"]
    ex = cmp["only_old_examples"][0]
    assert ex["n_fragments"] >= 2


def test_rule_comparison_counts_overlap_at_the_run_level():
    """구는 run, 신은 이벤트라 단위가 다르다. 겹침은 run 으로 센다."""
    ch = _changes([(0, 0, 40.0, 41.0), (200, 1, 40.0, 41.0)])
    _, dl, iso = _pipeline(ch, pre=10, post=10)
    cmp = trace.rule_comparison(iso, dl, _bounds(), 10, 10, 10, 10)
    assert cmp["both"] + cmp["only_new"] + cmp["only_old"] >= 1
```

- [ ] **Step 2: Run tests to verify they fail**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_trace.py -k "legacy or rule_comparison" -v
```

Expected: FAIL — `AttributeError: module 'app.coating.trace' has no attribute 'legacy_events'`

- [ ] **Step 3: Add legacy_events and rule_comparison**

`backend/app/coating/trace.py` 끝에 추가:

```python
def legacy_events(iso: pd.DataFrame) -> pd.DataFrame:
    """구 규칙의 묶음. ★순수

    구 규칙은 연쇄 병합이고, 연쇄 묶음은 run 그 자체다. 그래서 새 이벤트 표를
    run 으로 접으면 구 규칙의 묶음이 그대로 나온다 - 원본을 다시 훑을 필요가 없다.

    `last_at` 을 **넣지 않는다.** events.isolation 은 그 열이 없으면 시작 기준으로
    간격을 재는데, 그것이 구 규칙의 나머지 절반이다.
    """
    if iso.empty:
        return pd.DataFrame(columns=[S.LOT, S.EVENT, S.AT, "n_items"])
    out = (
        iso.groupby([S.LOT, S.RUN], as_index=False)
        .agg(**{S.AT: (S.AT, "min"), "n_items": ("n_items", "sum")})
    )
    out[S.EVENT] = out[S.RUN]
    return out[[S.LOT, S.EVENT, S.AT, "n_items"]].sort_values(
        [S.LOT, S.AT]
    ).reset_index(drop=True)


def rule_comparison(
    iso: pd.DataFrame,
    event_deltas: pd.DataFrame,
    bounds: pd.DataFrame | None,
    old_pre: int,
    old_post: int,
    new_pre: int,
    new_post: int,
) -> dict:
    """구 규칙과 신 규칙이 무엇을 살리고 무엇을 죽였나. ★순수

    구 규칙은 **세 가지를 다 되돌린 것**이다 - 연쇄 병합 + 시작 기준 간격 +
    옛 창(앞30/뒤60). 하나만 되돌리면 무엇이 차이를 만들었는지 갈리지 않는다.

    겹침은 **run 단위**로 센다. 구는 묶음이 run 이고 신은 그 안의 조각이라
    단위가 다른데, 그대로 빼면 뜻이 없는 숫자가 나온다.
    """
    from app.coating import events as ev_mod

    empty = {"n_clusters": 0, "n_isolated": 0, "n_items": 0}
    if iso.empty:
        return {"old": empty, "new": empty, "both": 0,
                "only_new": 0, "only_old": 0, "only_old_examples": []}

    old = ev_mod.isolation(legacy_events(iso), old_pre, old_post, bounds)
    new_ok = iso["isolated"].astype(bool)

    zoned = event_deltas[event_deltas[S.ZONE].notna()]
    n_items_new = int(zoned[zoned[S.EVENT].isin(iso.loc[new_ok, S.EVENT])].shape[0])
    old_ok_runs = set(old.loc[old["isolated"].astype(bool), S.EVENT])
    n_items_old = int(zoned[zoned[S.EVENT].isin(
        iso.loc[iso[S.RUN].isin(old_ok_runs), S.EVENT]
    )].shape[0])

    runs_with_new = set(iso.loc[new_ok, S.RUN])
    all_runs = set(iso[S.RUN])
    both = old_ok_runs & runs_with_new
    only_old = old_ok_runs - runs_with_new
    only_new = runs_with_new - old_ok_runs

    examples = []
    for r in sorted(only_old)[:3]:
        g = iso[iso[S.RUN] == r]
        examples.append({
            "run": r,
            "first": g[S.AT].min(),
            "last": g[S.LAST_AT].max(),
            "n_fragments": int(len(g)),
            "n_items": int(g["n_items"].sum()),
        })

    return {
        "old": {"n_clusters": int(len(old)),
                "n_isolated": int(len(old_ok_runs)),
                "n_items": n_items_old},
        "new": {"n_clusters": int(len(iso)),
                "n_isolated": int(new_ok.sum()),
                "n_items": n_items_new},
        "both": len(both),
        "only_new": len(only_new),
        "only_old": len(only_old),
        "only_old_examples": examples,
        "n_runs": len(all_runs),
    }
```

- [ ] **Step 4: Run tests to verify they pass**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_trace.py -v
```

Expected: 12개 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/coating/trace.py backend/tests/test_coating_trace.py
git commit -m "$(cat <<'EOF'
feat(coating): 구 규칙이 무엇을 만들어내고 있었는지 사례로 낸다

전처리를 바꾸는 순간 가장 궁금한 것은 몇 건이 새로 살고 몇 건이 죽었나다.
run_id 가 곧 구 규칙의 연쇄 묶음이므로 묶음 계산은 공짜고, 격리만 옛
파라미터로 한 번 더 돌린다. last_at 을 빼면 isolation 이 알아서 시작 기준으로
돌아 구 규칙의 나머지 절반도 재현된다.

겹침은 run 단위로 센다. 구는 묶음이 run 이고 신은 그 안의 조각이라 단위가
다른데 그대로 빼면 뜻이 없는 숫자가 나온다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

### Task 9: 타임라인 스트립

표로는 안 보이고 이것으로만 보이는 것이 있다 — "튜닝이 lot 초반에 몰린다" 같은 것. **모든 lot 을 보여준다.** 통과 이벤트가 하나도 없는 lot 이야말로 봐야 할 것이다. **스케일은 전체 공통**이다 — lot 마다 폭에 맞춰 늘이면 모든 lot 이 같은 길이로 보여 길이 정보가 사라진다.

**Files:**
- Modify: `backend/app/coating/trace.py`
- Test: `backend/tests/test_coating_trace.py`

**Interfaces:**
- Consumes: Task 3의 `isolation` 출력, `segment.lot_bounds` 출력
- Produces: `trace.timeline(iso, bounds, width=80) -> tuple[float, list[dict]]`
  - 첫 값: 칸당 분(`minutes_per_cell`)
  - 둘째 값: lot 당 `{lot_id, start, end, cells, runs}`. `cells` 는 `─`/`[`/`]`/`✓`/`✗` 로 채운 문자열, `runs` 는 `[{run_id, n, first_cell, last_cell}]`

- [ ] **Step 1: Write the failing test**

`backend/tests/test_coating_trace.py` 끝에 추가:

```python
def test_timeline_uses_one_common_scale_for_every_lot():
    """lot 마다 폭에 맞춰 늘이면 모든 lot 이 같은 길이로 보여 길이가 사라진다."""
    ch = pd.concat([
        _changes([(0, 0, 40.0, 41.0)], lot="L1"),
        _changes([(0, 1, 40.0, 41.0)], lot="L2"),
    ], ignore_index=True)
    ev, dl = ev_mod.build_events(ch, 2)
    bounds = pd.DataFrame([
        {S.LOT: "L1", "start": BASE, "end": BASE + pd.Timedelta(minutes=400)},
        {S.LOT: "L2", "start": BASE, "end": BASE + pd.Timedelta(minutes=100)},
    ])
    iso = ev_mod.isolation(ev, 10, 10, bounds)
    mpc, rows = trace.timeline(iso, bounds, width=40)
    by = {r[S.LOT]: r for r in rows}
    # 4배 긴 lot 이 4배 길게 그려져야 한다
    assert len(by["L1"]["cells"]) > 3 * len(by["L2"]["cells"])
    assert mpc == pytest.approx(10.0)


def test_timeline_includes_lots_with_no_isolated_event():
    """통과가 0건인 lot 이야말로 봐야 할 것이다."""
    ch = _changes([(0, 0, 40.0, 41.0), (5, 1, 40.0, 41.0)])
    _, _, iso = _pipeline(ch)
    _, rows = trace.timeline(iso, _bounds(), width=40)
    assert len(rows) == 1
    assert "✗" in rows[0]["cells"]


def test_timeline_marks_split_runs():
    # 0→1.5→3 은 한 연쇄인데 앵커가 둘로 쪼갠다.
    ch = _changes([(0, 0, 40.0, 41.0), (1.5, 1, 40.0, 41.0), (3, 2, 40.0, 41.0),
                   (200, 3, 40.0, 41.0)])
    _, _, iso = _pipeline(ch)
    _, rows = trace.timeline(iso, _bounds(), width=40)
    assert rows[0]["runs"]
    assert rows[0]["runs"][0]["n"] == 2
```

- [ ] **Step 2: Run tests to verify they fail**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_trace.py -k timeline -v
```

Expected: FAIL — `AttributeError: module 'app.coating.trace' has no attribute 'timeline'`

- [ ] **Step 3: Add timeline to trace.py**

`backend/app/coating/trace.py` 끝에 추가:

```python
def timeline(
    iso: pd.DataFrame, bounds: pd.DataFrame, width: int = 80
) -> tuple[float, list[dict]]:
    """lot 하나를 한 줄로 그린다. ★순수 — 문자열만 만들고 출력은 안 한다.

    스케일은 **전체 공통**이다. 가장 긴 lot 이 width 칸에 들어가도록 분/칸을 정하고
    모든 lot 에 같은 값을 쓴다. lot 마다 폭에 맞춰 늘이면 모든 lot 이 같은 길이로
    보여 "이 lot 은 짧다" 는 정보가 사라진다.

    통과가 0건인 lot 도 그린다. 그런 lot 이야말로 봐야 할 것이다.
    """
    if bounds is None or bounds.empty:
        return 0.0, []

    minute = pd.Timedelta(minutes=1)
    spans = (bounds["end"] - bounds["start"]) / minute
    longest = float(spans.max()) if len(spans) else 0.0
    mpc = max(longest / width, 1.0) if longest > 0 else 1.0

    rows = []
    for b in bounds.sort_values(S.LOT).itertuples(index=False):
        lot = getattr(b, S.LOT)
        span = (b.end - b.start) / minute
        n = max(int(round(span / mpc)), 1)
        cells = ["─"] * n
        g = iso[iso[S.LOT] == lot] if len(iso) else iso.iloc[0:0]

        pos = {}
        for e in g.itertuples(index=False):
            i = min(int(((getattr(e, S.AT) - b.start) / minute) / mpc), n - 1)
            pos[getattr(e, S.EVENT)] = i
            cells[i] = "✓" if bool(e.isolated) else "✗"

        runs = []
        if len(g):
            sizes = g.groupby(S.RUN)[S.EVENT].size()
            for run_id, k in sizes[sizes > 1].items():
                idx = [pos[e] for e in g.loc[g[S.RUN] == run_id, S.EVENT]]
                runs.append({S.RUN: run_id, "n": int(k),
                             "first_cell": min(idx), "last_cell": max(idx)})

        rows.append({S.LOT: lot, "start": b.start, "end": b.end,
                     "cells": "".join(cells), "runs": runs})
    return mpc, rows
```

- [ ] **Step 4: Run tests to verify they pass**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_trace.py -v
```

Expected: 15개 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/coating/trace.py backend/tests/test_coating_trace.py
git commit -m "$(cat <<'EOF'
feat(coating): lot 하나를 한 줄로 그리는 타임라인을 낸다

"튜닝이 lot 초반에 몰린다" 같은 것은 표로는 안 보이고 이것으로만 보인다.

스케일은 전체 공통이다. lot 마다 폭에 맞춰 늘이면 모든 lot 이 같은 길이로
보여 길이 정보가 사라진다. 통과가 0건인 lot 도 그린다 - 그런 lot 이야말로
봐야 할 것이다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

### Task 10: `--preprocess` 렌더러 — 다섯 절 + 기존 세 절

계산은 다 됐고 이제 사람이 읽는 형태로 낸다. 마지막 절(자기반증)이 이 변경의 안전장치다 — 10분은 실측이 아니라 가정이고, 응답 곡선의 최댓값이 마지막 lag 에서 나오면 창이 짧다는 뜻이다.

**Files:**
- Modify: `backend/app/coating/diagnose.py` (`render_isolation` → `render_preprocess`, CLI)
- Test: `backend/tests/test_coating_diagnose.py`

**Interfaces:**
- Consumes: Task 6~9의 `trace.event_ledger`/`funnel`/`rule_comparison`/`timeline`
- Produces:
  - `diagnose.render_preprocess(path, s) -> str`
  - `diagnose.render_isolation` 은 `render_preprocess` 를 부르는 별칭으로 남는다
  - CLI 플래그 `--preprocess` (신설) 와 `--isolation` (별칭)

- [ ] **Step 1: Write the failing test**

`backend/tests/test_coating_diagnose.py` 끝에 추가:

```python
def test_preprocess_renders_all_five_sections(tmp_path, monkeypatch):
    """다섯 절이 다 있어야 한다. 절이 통째로 사라지면 '안 쟀다' 와
    '재서 0 이다' 를 구별할 수 없다."""
    import pandas as pd

    from app.coating import diagnose, parse
    from app.coating import schemas as S
    from app.config import get_settings

    base = pd.Timestamp("2026-02-02 06:00")
    rows = []
    for m in range(0, 240):
        at = base + pd.Timedelta(minutes=m)
        for zi in range(3):
            v = 41.0 if (zi == 0 and m >= 30) else 40.0
            rows.append({S.LOT: "L1", S.AT: at, S.PRODUCT: "P",
                         S.ITEM: S.GAP_ITEM_IDS[zi], S.VALUE: v,
                         S.ROW_NO: len(rows)})
        for zi in range(3):
            rows.append({S.LOT: "L1", S.AT: at, S.PRODUCT: "P",
                         S.ITEM: S.WET_ITEM_IDS[zi],
                         S.VALUE: 18.0 if m < 38 else 18.4,
                         S.ROW_NO: len(rows)})
    src = tmp_path / "synth.parquet"
    pd.DataFrame(rows).to_parquet(src)

    monkeypatch.setattr(
        parse, "load_readings",
        lambda *a, **k: pd.read_parquet(src).assign(**{
            S.IO: lambda d: [
                S.IO_OUTPUT if i.startswith("9") else S.IO_INPUT for i in d[S.ITEM]
            ]
        }),
    )
    text = diagnose.render_preprocess(str(src), get_settings())
    for head in ("## 0.", "## 1.", "## 2.", "## 3.", "## 4."):
        assert head in text
    assert "격리 통과" in text


def test_isolation_flag_is_an_alias_for_preprocess():
    from app.coating import diagnose

    p = diagnose.build_parser()
    args = p.parse_args(["--isolation", "x.parquet"])
    assert args.preprocess_input == "x.parquet"
```

- [ ] **Step 2: Run tests to verify they fail**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_diagnose.py -k "five_sections or alias" -v
```

Expected: FAIL — `AttributeError: module 'app.coating.diagnose' has no attribute 'render_preprocess'`

- [ ] **Step 3: Replace render_isolation with render_preprocess**

`backend/app/coating/diagnose.py` 의 `render_isolation` 함수 **전체**를 다음으로 교체한다:

```python
def render_preprocess(path, s) -> str:
    """원본에서 바로 전처리 전 과정을 낸다. ★파일을 읽는 두 번째 가장자리.

    리포트 전체를 돌리지 않고도 "무엇이 어디서 걸러졌나" 를 보는 경로다. 창을
    정하는 일은 이 출력을 몇 번 들여다보는 일이라, 그때마다 파이프라인 전체를
    도는 것은 비싸다.

    사용자가 돌려줄 것은 §0·§4·§5 세 덩어리뿐이다. 나머지는 혼자 판정하는 데 쓴다.
    """
    from app.coating import (
        events as ev_mod, panel as panel_mod, parse, pivot,
        response as resp_mod, segment, trace,
    )

    fmt = parse.format_for(path, None, s.coating_input_format)
    readings = parse.load_readings(path, parse.DEFAULT_DICT_PATH, source=fmt)
    deduped = pivot.dedupe_minute(readings)
    changes = pivot.compress_runs(deduped)
    bounds = segment.lot_bounds(deduped)
    ev, dl = ev_mod.build_events(changes, s.coating_event_merge_minutes)
    iso = ev_mod.isolation(
        ev, s.coating_isolation_pre_minutes, s.coating_isolation_post_minutes, bounds
    )

    lines = [
        "# 전처리 — 무엇이 어디서 걸러졌나",
        "",
        f"- lot {len(bounds)}개",
        f"- 묶음 규칙: 앵커 {s.coating_event_merge_minutes}분"
        f" (한 묶음의 폭이 이 값을 넘지 못한다)",
        f"- 격리 규칙: 앞{s.coating_isolation_pre_minutes}분 /"
        f" 뒤{s.coating_isolation_post_minutes}분, 묶음의 **끝**에서 잰다",
        "",
        "이 판정은 Wet 을 보지 않는다. 이벤트 시각만으로 정해지므로 L 을 몰라도",
        "쓸 수 있고, 그래서 L 을 재는 데 쓸 수 있다.",
        "",
    ]
    lines += _funnel_lines(trace.funnel(readings, deduped, changes, iso))
    lines += _ledger_lines(trace.event_ledger(iso, dl))
    lines += _timeline_lines(*trace.timeline(iso, bounds))
    lines += _comparison_lines(trace.rule_comparison(
        iso, dl, bounds, 30, 60,
        s.coating_isolation_pre_minutes, s.coating_isolation_post_minutes,
    ))
    lines += _window_check_lines(
        deduped, iso, dl, s, panel_mod, resp_mod
    )
    lines += _legacy_tables(ev, changes, bounds, s, ev_mod)
    return LF.join(lines) + LF


# --isolation 은 별칭으로 남긴다. 기존 문서와 손버릇이 깨지지 않게.
render_isolation = render_preprocess


def _funnel_lines(f) -> list[str]:
    out = ["## 0. 어디서 얼마나 줄었나", ""]
    for r in f.itertuples(index=False):
        d = "" if r.delta is None else f"  ({r.delta:+,})"
        note = f"   {r.note}" if r.note else ""
        out.append(f"   {r.stage:<20} {r.n:>10,}{d}{note}")
    return out + [""]


def _ledger_lines(led, head: int = 20) -> list[str]:
    out = [
        f"## 1. 묶음 원장  ({len(led)}건 중 앞 {min(head, len(led))}"
        f" · 전체는 12_event_ledger.csv)",
        "",
        "   event         run   첫 변경           마지막   span  n  중복"
        "   앞간격   뒤간격  판정",
    ]
    if led.empty:
        return out + ["   (조정 이벤트가 0건이다)", ""]
    for r in led.head(head).itertuples(index=False):
        run = getattr(r, S.RUN) or "-"
        first = getattr(r, S.AT)
        last = getattr(r, S.LAST_AT)
        dup = f"⚠{r.n_dup_zones}" if r.n_dup_zones else "-"
        gb = "  없음" if pd.isna(r.gap_before) else f"{r.gap_before:6.1f}"
        ga = "  없음" if pd.isna(r.gap_after) else f"{r.gap_after:6.1f}"
        verdict = "✓" if r.isolated else f"✗ {getattr(r, S.ISO_REASON)}"
        out.append(
            f"   {getattr(r, S.EVENT):<13} {run:<5} {first:%m-%d %H:%M:%S}"
            f"  {last:%H:%M:%S} {getattr(r, S.SPAN):5.1f} {r.n_items:>2}"
            f"  {dup:>3} {gb} {ga}  {verdict}"
        )
    out += ["", "   zone 별 Δ 는 CSV 의 zones 열에 있다.", ""]
    return out


def _timeline_lines(mpc, rows) -> list[str]:
    out = [
        f"## 2. 타임라인  (─ 조용  ✓ 격리통과  ✗ 탈락  1칸 = {mpc:.0f}분)",
        "",
    ]
    for r in rows:
        out.append(
            f"   {r[S.LOT]:<12} {r['start']:%m-%d %H:%M} ├{r['cells']}┤"
            f" {r['end']:%H:%M}"
        )
        for run in r["runs"]:
            pad = " " * (len(f"   {r[S.LOT]:<12} ") + 12)
            out.append(f"{pad}└─ {run[S.RUN]}: 조각 {run['n']}개")
    return out + [""]


def _comparison_lines(c) -> list[str]:
    """창 값은 c 에서 읽는다. 헤더에 30/60 을 박아 두면 다른 창으로 부른 날
    제목만 옛 숫자를 말하는 거짓말이 된다."""
    o, n = c["old"], c["new"]
    out = [
        f"## 3. 구 규칙(연쇄·시작 기준·앞{o['pre']}/뒤{o['post']}) 대비"
        f" — 신 규칙은 앵커·끝 기준·앞{n['pre']}/뒤{n['post']}",
        "",
        "                     구       신",
        f"   묶음          {o['n_clusters']:>6}  {n['n_clusters']:>6}",
        f"   격리 통과     {o['n_isolated']:>6}  {n['n_isolated']:>6}",
        f"   Δgap 항목     {o['n_items']:>6}  {n['n_items']:>6}",
        "",
        f"   양쪽 통과 {c['both']}건 · 신에만 {c['only_new']}건"
        f" · 구에만 {c['only_old']}건   (연속 구간 {c['n_runs']}개 중,"
        " 어느 쪽도 통과 못 한 구간은 어디에도 안 든다)",
    ]
    if c["only_old_examples"]:
        out += ["", "   구에만 남은 것 = 연쇄가 삼켰던 긴 구간:"]
        for e in c["only_old_examples"]:
            out.append(
                f"     {e['run']}  구 = {e['first']:%m-%d %H:%M}~{e['last']:%H:%M}"
                f" 한 건 / 신 = 조각 {e['n_fragments']}개"
                f" · 항목 손질 {e['n_item_touches']}회"
            )
    return out + [""]


def _window_check_lines(deduped, iso, dl, s, panel_mod, resp_mod) -> list[str]:
    """응답창이 반응을 담고 있나. 10분은 실측이 아니라 가정이다.

    곡선의 최댓값이 마지막 lag 에서 나오면 창이 짧다는 뜻이다. 이 두 줄이 매
    실행마다 그 가정을 스스로 검사한다 - 없으면 틀린 창으로 낸 L·τ 가 숫자처럼
    보여서 더 위험하다.
    """
    post = s.coating_response_post_minutes
    out = [f"## 4. {post}분 창이 반응을 담고 있나", ""]
    usable = iso[iso["isolated"]] if len(iso) else iso
    if not len(usable):
        return out + ["   격리를 통과한 이벤트가 0건이라 판정할 수 없다.", ""]
    p = panel_mod.build_panel(deduped, s.coating_panel_ffill_max_minutes)
    aligned = resp_mod.align_events(
        p, usable, dl, s.coating_response_pre_minutes, post,
        s.coating_delta_window_minutes,
    )
    curve = resp_mod.response_curve(aligned)
    fwd = curve[curve[resp_mod.LAG] >= 0] if len(curve) else curve
    if not len(fwd):
        return out + ["   정렬된 응답이 없다. 표본이 모자라거나 패널이 비었다.", ""]
    peak_lag = int(fwd.loc[fwd["mean"].idxmax(), resp_mod.LAG])
    last_lag = int(fwd[resp_mod.LAG].max())
    mark = "  ⚠  마지막 칸이다 — 창이 짧다." if peak_lag >= last_lag else "  ✓"
    return out + [
        f"   정렬 응답 곡선 최댓값 lag = +{peak_lag}분 (관측 끝 +{last_lag}분){mark}",
        f"   (최댓값이 창 한가운데면 정상. 마지막 lag 이면 "
        f"COATING_ISOLATION_POST_MINUTES 와 COATING_RESPONSE_POST_MINUTES 를 올린다.)",
        "",
    ]


def _legacy_tables(ev, changes, bounds, s, ev_mod) -> list[str]:
    """창을 고르는 근거 세 표. 새 다섯 절 뒤에 그대로 유지한다."""
    lines = ["## 5. 격리 창을 바꿔가며", "", "   창(앞=뒤)   홀로 선 이벤트"]
    if ev.empty:
        return lines + ["   (조정 이벤트가 0건이다)", ""]
    table = ev_mod.isolation_table(ev, bounds=bounds)
    for r in table.itertuples(index=False):
        lines.append(
            f"   {r.post_minutes:>6}분   {r.n_isolated:>4} / {r.n_events}"
            f" ({r.ratio:.0%})"
        )
    lines += ["", "## 6. 변경 사이의 간격 분포 — 병합창은 몇 분이어야 하나", ""]
    hist = ev_mod.gap_histogram(changes)
    peak = max(hist["n"]) or 1
    for r in hist.itertuples(index=False):
        bar = "█" * int(round(r.n / peak * 28))
        lines.append(f"   {r.bucket:>8}  {r.n:>5} ({r.ratio:>4.0%})  {bar}")
    lines += ["", _gap_verdict(hist)]

    lines += ["", "## 7. 병합창을 바꾸면 쓸 수 있는 것이 얼마나 달라지나", ""]
    sens = ev_mod.merge_sensitivity(
        changes,
        pre_minutes=s.coating_isolation_pre_minutes,
        post_minutes=s.coating_isolation_post_minutes,
        bounds=bounds,
    )
    lines.append("   병합창   묶음   격리 통과   쓸 수 있는 Δgap 항목")
    for r in sens.itertuples(index=False):
        mark = "  ← 현재" if r.merge_minutes == s.coating_event_merge_minutes else ""
        lines.append(
            f"   {r.merge_minutes:>4}분  {r.n_clusters:>5}   {r.n_isolated:>7}건"
            f"   {r.n_items:>14}개{mark}"
        )
    lines += ["", _merge_verdict(sens, s.coating_event_merge_minutes)]
    return lines
```

`_isolation_verdict`·`_gap_verdict`·`_merge_verdict` 는 **전부 유지**한다. `_isolation_verdict` 는 격리 표 뒤에서 계속 부른다 — 0건일 때 무엇을 해야 하는지 알려 주는 유일한 문장이고, `tests/test_coating_diagnose.py:302-330` 이 세 케이스를 검증하고 있다. `_legacy_tables` 의 시그니처를 `_legacy_tables(ev, iso, changes, bounds, s, ev_mod)` 로 하고, 격리 표 바로 뒤에 다음 두 줄을 넣는다:

```python
    n_now = int(iso["isolated"].sum()) if len(iso) else 0
    lines += ["", f"- 현재 설정 → **{n_now}건**", "", _isolation_verdict(table, n_now)]
```

`render_preprocess` 의 호출부도 `_legacy_tables(ev, iso, changes, bounds, s, ev_mod)` 로 맞춘다.

- [ ] **Step 4: Update the CLI**

`backend/app/coating/diagnose.py` 의 `build_parser` 에서 `--isolation` 인자를 다음으로 교체한다:

```python
    p.add_argument(
        "--preprocess", "--isolation", dest="preprocess_input",
        default=None, metavar="PARQUET",
        help="원본에서 전처리 전 과정을 낸다(깔때기·원장·타임라인·구신대조·창 검사)."
             " 리포트를 안 돌려도 된다. --isolation 은 별칭이다.",
    )
```

`main` 에서 `args.isolation_input` 을 `args.preprocess_input` 으로, `render_isolation(...)` 을 `render_preprocess(...)` 로 바꾸고, 아래 `raise SystemExit` 의 안내문을 고친다:

```python
        raise SystemExit(
            "무엇을 진단할지 정해야 한다.\n"
            "  --preprocess <원본 parquet>   전처리 전 과정 (별칭: --isolation)\n"
            "  --dump <덤프 폴더>             커널 진단 (리포트를 --dump 로 먼저 돌린다)"
        )
```

별칭 이름을 메시지에 남기는 이유는 두 가지다. 손버릇이 `--isolation` 인 사람이 안내문에서 자기 플래그를 못 찾으면 없어진 줄 안다. 그리고 `tests/test_coating_diagnose.py:338` 이 이 문자열을 검증하고 있다.

`build_parser` 의 `epilog` 첫 예시도 함께 고친다:

```
            "예) 전처리 전 과정 — 리포트를 안 돌려도 된다\n"
            "    python -m app.coating.diagnose \\\n"
            "      --preprocess data/coating/raw/merged.parquet\n\n"
```

- [ ] **Step 5: Run the diagnose tests**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_diagnose.py -v
```

Expected: 전부 PASS. 기존 테스트가 `render_isolation` 을 부르면 별칭이라 그대로 통과한다.

- [ ] **Step 6: Commit**

```bash
git add backend/app/coating/diagnose.py backend/tests/test_coating_diagnose.py
git commit -m "$(cat <<'EOF'
feat(coating): 전처리 전 과정을 한 번에 읽는 --preprocess 를 연다

깔때기·원장·타임라인·구신대조·창 검사 다섯 절 + 창을 고르는 기존 세 표.
리포트를 안 돌리고 원본에서 바로 낸다 - 창을 정하는 일은 이 출력을 몇 번
들여다보는 일이라 그때마다 파이프라인 전체를 도는 것은 비싸다.

마지막 절이 안전장치다. 10분은 실측이 아니라 가정이고, 응답 곡선의 최댓값이
마지막 lag 에서 나오면 창이 짧다는 뜻이다. 매 실행이 그 가정을 스스로 검사한다.

--isolation 은 별칭으로 남긴다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

### Task 11: 원장을 덤프로 남기고 사용자 안내를 갱신한다

리포트를 돌릴 때도 같은 원장이 CSV 로 떨어져야 엑셀에서 정렬·필터할 수 있다. 그리고 사용자가 사내 PC 에서 무엇을 실행하고 출력을 어떻게 읽을지가 문서에 있어야 한다 — 그게 없으면 이 전환은 왕복 한 번을 더 쓴다.

**Files:**
- Modify: `backend/app/coating/report.py` (`DUMP_TABLES`, `profile_readings`)
- Modify: `backend/.env.example` (덤프 설명에 원장 추가)
- Create: `docs/measurements/2026-09-08-coating-preprocess-switch-howto.md`
- Test: `backend/tests/test_coating_dump.py`, `backend/tests/test_coating_report.py`

**Interfaces:**
- Consumes: Task 6의 `trace.event_ledger`
- Produces: 덤프 표 `12_event_ledger`

- [ ] **Step 1: Write the failing test**

`backend/tests/test_coating_report.py` 끝에 추가:

```python
def test_dump_tables_include_the_event_ledger():
    """원장이 없으면 리포트를 돌린 사람이 왜 그 판정이 났는지 열어볼 것이 없다."""
    from app.coating import report

    assert "12_event_ledger" in report.DUMP_TABLES
```

- [ ] **Step 2: Run test to verify it fails**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_report.py -k event_ledger -v
```

Expected: FAIL — `assert '12_event_ledger' in (...)`

- [ ] **Step 3: Add the dump table**

`backend/app/coating/report.py` 의 `DUMP_TABLES` 마지막 항목 뒤에 추가:

```python
    # 묶음 하나당 한 줄. 어느 것이 왜 걸러졌는지의 근거이고, diagnose --preprocess
    # 가 터미널에 앞 20건만 보여주므로 전체는 여기서만 볼 수 있다.
    "12_event_ledger",
```

`profile_readings` 의 `iso`/`usable` 계산 바로 뒤(Task 5에서 만든 자리)에 추가:

```python
    if tables is not None:
        from app.coating import trace as trace_mod

        tables["12_event_ledger"] = trace_mod.event_ledger(iso, dl)
```

`backend/.env.example` 의 덤프 설명 줄을 고친다:

```
# 남기는 것: 제어값 변경 · 이벤트 · zone별 Δgap · 정착 판정 · lag 정렬표 · 응답곡선
#            · 격리 판정 · **묶음 원장**(어느 것이 왜 걸러졌나).
```

- [ ] **Step 4: Run the report and dump tests**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/test_coating_report.py tests/test_coating_dump.py -v
```

Expected: 전부 PASS

- [ ] **Step 5: Write the user-facing howto**

`docs/measurements/2026-09-08-coating-preprocess-switch-howto.md` 를 만든다:

````markdown
# 전처리 전환 확인 — 사내 PC 에서 무엇을 돌리고 무엇을 읽나

전처리 규칙이 바뀌었다(앵커 병합 + 끝 기준 ±10분 격리 + 선별 통일).
실데이터는 사내 PC 에만 있으므로 아래를 직접 돌리고, **§0 · §3 · §4 세 덩어리만**
잘라서 돌려주면 된다. 원본 데이터는 필요 없다.

## 1. 돌릴 것

```
cd backend
python -m app.coating.diagnose --preprocess data/coating/raw/merged.parquet
```

리포트까지 같이 보려면:

```
python -m app.coating.report --input data/coating/raw/merged.parquet --dump
```

`--dump` 를 주면 `12_event_ledger.csv` 가 함께 떨어진다. 엑셀에서 열어
`isolated` 로 필터하면 통과한 것만 볼 수 있다.

## 2. 읽는 법 — §4 (창이 반응을 담고 있나)

| 출력 | 뜻 | 다음 |
|---|---|---|
| 최댓값 lag 이 창 한가운데 (+3~+7) | 10분 창이 반응을 담고 있다 | 그대로 진행 |
| `⚠ 마지막 칸이다` | 창이 짧다. 반응이 아직 안 끝났다 | `COATING_ISOLATION_POST_MINUTES` 와 `COATING_RESPONSE_POST_MINUTES` 를 **둘 다** 20으로 올리고 재실행 |
| `격리를 통과한 이벤트가 0건` | 표본이 없다 | §0 의 탈락 내역을 본다 |
| `정렬된 응답이 없다` | 패널이 비었거나 Δgap 이 전부 0 | 원본에 제어값 변경이 있는지 §0 의 "시작값 제외" 줄을 본다 |

두 값은 **항상 같이** 움직여야 한다. 뒤 격리보다 응답창이 길면, 응답으로 읽는
구간의 일부가 다른 조정에 오염돼 있어도 통과한다.

## 3. 읽는 법 — §0 (격리 통과 건수)

| 통과 건수 | 뜻 | 다음 |
|---|---|---|
| 20건 이상 | 커널 추정 하한(`_MIN_EVENTS=20`)을 넘는다 | 진행 |
| 5~19건 | L·τ 는 볼 수 있으나 커널은 이르다 | 격리 창을 ±5로 좁혀 재시도하고 §3 으로 손실을 확인 |
| 5건 미만 | 이 데이터로는 안 된다 | §3 의 "구에만" 건수를 근거로 사업부에 요구 |

## 4. 읽는 법 — §3 (구/신 대조)

- **신에만 N건**: 뒤 창이 60→10 으로 풀려 살아난 것. 늘어나는 게 정상이다.
- **구에만 N건**: 연쇄 병합이 삼켰던 긴 구간. 이게 크면 현장에서 조정이 촘촘하게
  일어난다는 뜻이고, **그 자체가 사업부에 낼 요구서다** — "한 번 만지고 10분
  기다려 달라" 의 근거.
- **양쪽 통과 N건**: 두 규칙이 같이 인정한 것. 이게 0이면 전환의 영향이 크므로
  숫자를 그대로 돌려준다.

## 5. 무엇을 돌려주나

§0 깔때기 전체, §3 대조표 전체, §4 두 줄. 세 덩어리를 그대로 복사해 붙이면 된다.
원본 CSV·parquet 은 보내지 않는다.
````

- [ ] **Step 6: Run the whole test suite**

```
cd backend && .venv/Scripts/python.exe -m pytest tests/ -v
```

Expected: 전부 PASS

- [ ] **Step 7: Commit**

```bash
git add backend/app/coating/report.py backend/.env.example backend/tests/ docs/measurements/2026-09-08-coating-preprocess-switch-howto.md
git commit -m "$(cat <<'EOF'
feat(coating): 원장을 덤프로 남기고 전환 확인 절차를 문서로 낸다

터미널은 앞 20건만 보여주므로 전체 원장은 12_event_ledger.csv 로만 볼 수 있다.
엑셀에서 isolated 로 필터하면 통과한 것만 나온다.

실데이터는 사내 PC 에만 있으므로 "돌려 보겠다" 가 성립하지 않는다. 사용자가
직접 돌릴 명령과, 나올 수 있는 경우를 미리 가른 판정 분기표를 함께 낸다.
돌려줄 것은 세 덩어리 텍스트뿐이고 원본은 요청하지 않는다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0186wADM7bUgQB4GQC5Bhsb4
EOF
)"
```

---

## Self-Review

**1. Spec coverage**

| 스펙 요소 | 태스크 |
|---|---|
| 문제 1 연쇄 병합 | Task 2 |
| 문제 2 시작 기준 간격 | Task 3 |
| 문제 3 선별 이중화 | Task 5 |
| 문제 4 Δgap 덮어쓰기 | Task 1 |
| 앵커 병합 | Task 2 |
| run_id | Task 2 (계산), 6 (표시), 8 (구신대조) |
| 끝 기준 격리 | Task 3 |
| 선별 통일 + ΔWet 고정창 | Task 5 |
| 설정 8개 | Task 4 |
| 층 0 깔때기 | Task 7 + 10 |
| 층 1 원장 | Task 6 + 10 + 11 |
| 층 2 타임라인 | Task 9 + 10 |
| 층 3 구/신 대조 | Task 8 + 10 |
| 층 4 자기반증 | Task 10 |
| 배치(trace/diagnose/dump) | Task 6~11 |
| 테스트 재작성 | Task 2 Step 6, Task 5 Step 1 |
| 사용자 실행 명령 + 분기표 | Task 11 |

빠진 것 없음.

**2. Placeholder scan**

"TBD"·"적절히"·"비슷하게" 없음. 모든 코드 스텝에 실제 코드 블록이 있고, 모든 테스트에 실제 assert 가 있다.

**3. Type consistency**

- `S.LAST_AT`/`S.RUN`/`S.SPAN`/`S.ISO_REASON` — Task 2에서 정의, Task 3·6·8·9·10에서 사용. 일치.
- `events.build_events -> (ev, dl)`, `ev` 열은 `EVENT_COLS` 로 고정. Task 6·8·9가 이 열들만 쓴다.
- `events.isolation` 반환 열 `gap_before`/`gap_after`/`isolated`/`iso_reason` — Task 3에서 정의, Task 6·7·8·9·10에서 사용. 일치.
- `features.delta_samples(..., post_minutes, delta_window_minutes)` — Task 5에서 변경, Task 5 Step 5(report)에서만 호출. 일치.
- `trace.timeline -> (float, list[dict])` — Task 9에서 정의, Task 10의 `_timeline_lines(*...)` 가 두 인자를 받는다. 일치.
- `trace.rule_comparison` 반환 키 — Task 8에서 정의, Task 10의 `_comparison_lines` 가 `old`/`new`/`both`/`only_new`/`only_old`/`only_old_examples` 를 읽는다. 일치.

## 남은 위험

- **Task 5 이후 실데이터에서 격리 통과가 0건일 수 있다.** 앵커가 조이고 창이 푸는 방향이라 순효과를 코드로 알 수 없다. Task 11의 분기표가 그 경우를 명시적으로 다룬다 — 실패가 아니라 산출물이다.
- **`_settle_time` 이 10분 창에서 거의 무력해진다.** `annotate_settling` 은 선별에서 빠졌으므로 파이프라인은 굴러가지만, `contaminated_ratio` 가 1.0 에 가까워질 수 있다. 진단 숫자일 뿐 판정에 안 쓴다.
