# 교육자료 2편·3편 (프롬프트/LangGraph, Tool calling) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 실무자 대상 교육자료 2편(프롬프트 구성 & LangChain vs LangGraph)과 3편(Tool calling)을 실습 코드 + PPTX로 만든다.

**Architecture:** 각 편은 "단계별로 진화하는 단일 실습 파일"(`--step N` 으로 개별 실행 가능) + 그 코드를 그대로 옮긴 PPTX로 구성된다. 1편(`connect_llm.py` + `사내LLM연결_LangChain입문.pptx`)의 톤과 컨벤션을 그대로 계승한다.

**Tech Stack:** Python 3.11+, LangChain(`langchain-core`/`langchain-openai`/`langchain-ollama`), LangGraph(`StateGraph`/`create_react_agent`/`MemorySaver`), python-pptx(`document-skills:pptx` 스킬 경유), 선택적 `xlwings`.

## Global Constraints

- **새 pip 의존성 추가 금지.** `langchain-core`/`langchain-openai`/`langchain-ollama`/`langgraph`는 이미 `backend/pyproject.toml`에 존재한다. `xlwings`는 선택적으로만 쓰고(Step 6), 미설치 시 `try/except ImportError`로 `SKIP` — pyproject에 추가하지 않는다.
- **두 예제 파일 모두 `app` 패키지에 의존하지 않는 순수 예제**(`backend/examples/connect_llm.py` 패턴)로 만든다. `CLAUDE.md`의 "get_settings()를 못 쓰는 순수 예제" carve-out을 적용해, `backend/.env`를 직접 읽는 `load_env_file()`을 각 파일에 그대로 둔다(공유 헬퍼 모듈로 추출하지 않는다 — 학습자가 파일 하나만 열어도 완결되게 하기 위함, 1편과 동일한 설계 판단).
- **자동 pytest 단위테스트를 만들지 않는다.** 각 스텝은 실제 LLM을 호출하므로 응답이 비결정적이다 — `connect_llm.py`/`verify_tool_calling.py`/`analyze_excel.py`와 동일하게, 스크립트를 직접 실행해 사람이 출력 패턴(에러 없음, 기대 구조)을 확인하는 수동 검증을 쓴다.
- **코드와 PPTX는 주제별로 하나의 커밋으로 묶는다.** 스크립트 작성 태스크는 파일 저장까지만 하고, 해당 주제의 마지막 태스크(PPTX 제작)에서 스크립트 + PPTX를 함께 `git add` + `commit` 한다.
- **PPTX 제작에는 `document-skills:pptx` 스킬을 사용**하고, 1편(`docs/교육자료/사내LLM연결_LangChain입문.pptx`)을 참조해 동일한 시각 스타일(카드형 레이아웃, "코드 뜯어보기 ①②③" 패턴, 하단 진행 표시 "〈시리즈명〉 · N")을 따른다.
- **실행 확인에는 응답 가능한 LLM 백엔드가 필요하다.** `backend/.env`에 `LLM_BACKEND=ollama`(로컬 Ollama 구동 중) 또는 `LLM_BACKEND=internal`(사내 게이트웨이 값 채움)이 설정된 환경에서 실행 확인 스텝을 수행한다.

---

## Task 1: 주제2 실습 스크립트 — 골격 + Step 1~2

**Files:**
- Create: `backend/examples/prompt_and_graph.py`

**Interfaces:**
- Consumes: 없음(표준 라이브러리 + 기존 의존성만).
- Produces:
  - `load_env_file() -> None`
  - `build_model(backend: str | None = None) -> BaseChatModel`
  - `step_1(model) -> None`, `step_2(model) -> None`
  - `STEPS: dict[int, Callable]` (이후 태스크에서 계속 채워짐)
  - `main() -> int` (`--step N`, `--backend internal|ollama` 인자)

- [ ] **Step 1: 파일 골격 작성**

`backend/examples/prompt_and_graph.py`:
```python
"""프롬프트 구성과 LangChain vs LangGraph 를 코드로 비교해보는 실습 (한 파일).

교육자료 "프롬프트와 LangGraph" 의 실습용 예제다. 문자열 하나로 묻는 것에서 시작해,
프롬프트 템플릿 -> 멀티턴 대화를 LangChain 만으로 손수 관리 -> 같은 걸 LangGraph 로
넘겨 상태 관리를 위임하는 순서로 4단계를 거친다.

실행 (backend/ 에서, venv 파이썬으로):
    python examples/prompt_and_graph.py            # 전체 4단계
    python examples/prompt_and_graph.py --step 2    # 특정 단계만
    python examples/prompt_and_graph.py --backend ollama

설치: langchain-openai, langchain-ollama, langchain-core, langgraph, httpx
     (백엔드 의존성에 이미 포함되어 있어 추가 설치 불필요)

설정: backend/.env 에 LLM_BACKEND 와 해당 백엔드 값을 채운다.
"""
import argparse
import os
from pathlib import Path

import httpx
from langchain_core.messages import AIMessage, HumanMessage, SystemMessage
from langchain_core.prompts import ChatPromptTemplate
from langchain_ollama import ChatOllama
from langchain_openai import ChatOpenAI


def load_env_file() -> None:
    """backend/.env 를 읽어 환경변수로 채운다(이미 설정된 값은 덮어쓰지 않음)."""
    env_path = Path(__file__).resolve().parents[1] / ".env"
    if not env_path.exists():
        return
    for line in env_path.read_text(encoding="utf-8").splitlines():
        line = line.strip()
        if not line or line.startswith("#") or "=" not in line:
            continue
        key, _, val = line.partition("=")
        os.environ.setdefault(key.strip(), val.strip())


load_env_file()

# 사내망 대응: 회사 프록시 우회 (내부 엔드포인트는 프록시를 거치면 못 닿는 경우가 많다)
for var in ("HTTP_PROXY", "HTTPS_PROXY", "http_proxy", "https_proxy"):
    os.environ.pop(var, None)


def build_model(backend: str | None = None):
    """LLM_BACKEND(또는 --backend 인자)에 따라 ChatOpenAI/ChatOllama 를 만든다.

    app.llm.get_chat_model 과 같은 분기를 프레임워크 없이 손수 보여준다
    (이 예제는 app 패키지에 의존하지 않는 단일 파일이므로).
    """
    backend = backend or os.environ.get("LLM_BACKEND", "ollama")
    if backend == "internal":
        missing = [
            k for k in ("INTERNAL_LLM_BASE_URL", "INTERNAL_LLM_MODEL")
            if not os.environ.get(k)
        ]
        if missing:
            raise SystemExit(
                "사내 LLM 설정이 비어 있습니다: " + ", ".join(missing)
                + "\nbackend/.env 를 채우세요."
            )
        return ChatOpenAI(
            base_url=os.environ["INTERNAL_LLM_BASE_URL"],
            api_key=os.environ.get("INTERNAL_LLM_API_KEY", "not-needed"),
            model=os.environ["INTERNAL_LLM_MODEL"],
            http_client=httpx.Client(verify=False),
        )
    return ChatOllama(
        model=os.environ.get("OLLAMA_MODEL", "gemma3n:e4b"),
        base_url=os.environ.get("OLLAMA_BASE_URL", "http://localhost:11434"),
    )


def step_1(model) -> None:
    """Step 1: 문자열 하나만 던지기 - 역할 지정 없이"""
    reply = model.invoke("자기소개해줘")
    print(reply.content)


def step_2(model) -> None:
    """Step 2: ChatPromptTemplate 로 역할 · 변수를 구조화"""
    template = ChatPromptTemplate.from_messages(
        [
            ("system", "너는 {role} 전문가야. 항상 한국어로 간결하게 답해."),
            ("human", "{topic} 을 한 문장으로 설명해줘."),
        ]
    )
    chain = template | model
    reply = chain.invoke({"role": "데이터 분석", "topic": "이상치 탐지"})
    print(reply.content)


STEPS = {1: step_1, 2: step_2}


def main() -> int:
    parser = argparse.ArgumentParser()
    parser.add_argument("--step", type=int, choices=sorted(STEPS), default=None)
    parser.add_argument("--backend", choices=("internal", "ollama"), default=None)
    args = parser.parse_args()

    model = build_model(args.backend)
    steps = [STEPS[args.step]] if args.step else list(STEPS.values())
    for fn in steps:
        print(f"\n{'=' * 60}\n[{fn.__doc__}]\n{'=' * 60}")
        fn(model)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: Step 1 실행 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\prompt_and_graph.py --step 1`
Expected: 에러 없이 종료되고, 모델의 자유 형식 답변 텍스트가 출력된다(정확한 문장은 백엔드/모델에 따라 다름 — 실행 성공과 비어있지 않은 출력을 확인).

- [ ] **Step 3: Step 2 실행 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\prompt_and_graph.py --step 2`
Expected: "이상치 탐지"에 대한 한 문장 설명이 출력된다. Step 1과 달리 시스템 역할(`데이터 분석 전문가`)이 반영된 톤인지 확인.

- [ ] **Step 4: 인자 없이 전체 실행해 --step 라우팅 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\prompt_and_graph.py`
Expected: `[Step 1: ...]`, `[Step 2: ...]` 헤더가 순서대로 출력되고 두 단계 모두 실행된다(Step 3~4는 아직 STEPS에 없으므로 이 시점엔 2단계만 실행됨 — 정상).

(이 태스크는 커밋하지 않는다 — Task 3에서 PPTX와 함께 커밋한다.)

---

## Task 2: 주제2 실습 스크립트 — Step 3~4 추가 (멀티턴 LangChain / LangGraph)

**Files:**
- Modify: `backend/examples/prompt_and_graph.py`

**Interfaces:**
- Consumes: Task 1의 `load_env_file`/`build_model`/`STEPS`/`main` 구조.
- Produces: `step_3(model) -> None`, `step_4(model) -> None`, 확장된 `STEPS`(1~4).

- [ ] **Step 1: import 추가**

파일 상단 import 블록에 추가:
```python
from typing import Annotated, TypedDict

from langgraph.checkpoint.memory import MemorySaver
from langgraph.graph import END, START, StateGraph
from langgraph.graph.message import add_messages
```

- [ ] **Step 2: step_3 작성 (LangChain 만으로 멀티턴)**

`step_2` 함수 바로 아래에 추가:
```python
def step_3(model) -> None:
    """Step 3: LangChain만으로 멀티턴 - 히스토리를 손으로 append"""
    messages = [SystemMessage(content="너는 친절한 한국어 비서야.")]

    messages.append(HumanMessage(content="LangGraph가 뭐야? 한 문장으로."))
    reply = model.invoke(messages)
    messages.append(AIMessage(content=reply.content))
    print("[1턴]", reply.content)

    messages.append(HumanMessage(content="방금 답을 더 짧게 줄여줘."))
    reply = model.invoke(messages)
    messages.append(AIMessage(content=reply.content))
    print("[2턴]", reply.content)

    print(
        f"\n(대화가 길어질수록 messages 리스트를 매번 손으로 관리해야 한다: "
        f"현재 {len(messages)}개)"
    )
```

- [ ] **Step 3: step_4 작성 (LangGraph 로 위임)**

`step_3` 함수 바로 아래에 추가:
```python
class ChatState(TypedDict):
    messages: Annotated[list, add_messages]


def step_4(model) -> None:
    """Step 4: 같은 대화를 LangGraph 로 - 상태(히스토리)를 대신 관리"""

    def chat_node(state: ChatState) -> dict:
        response = model.invoke(state["messages"])
        return {"messages": [response]}

    graph = StateGraph(ChatState)
    graph.add_node("chat", chat_node)
    graph.add_edge(START, "chat")
    graph.add_edge("chat", END)
    app = graph.compile(checkpointer=MemorySaver())

    config = {"configurable": {"thread_id": "demo"}}
    out = app.invoke(
        {"messages": [HumanMessage(content="LangGraph가 뭐야? 한 문장으로.")]}, config
    )
    print("[1턴]", out["messages"][-1].content)

    out = app.invoke(
        {"messages": [HumanMessage(content="방금 답을 더 짧게 줄여줘.")]}, config
    )
    print("[2턴]", out["messages"][-1].content)

    print(
        f"\n(messages 를 손으로 append 하지 않았다 - thread_id 로 이전 턴이 자동 "
        f"복원됐다: 현재 {len(out['messages'])}개)"
    )
```

- [ ] **Step 4: STEPS 딕셔너리 확장**

```python
STEPS = {1: step_1, 2: step_2, 3: step_3, 4: step_4}
```

- [ ] **Step 5: Step 3 실행 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\prompt_and_graph.py --step 3`
Expected: `[1턴]`, `[2턴]` 답변이 출력되고, 2턴 답변이 1턴보다 짧아진 흐름이 보인다(모델이 "방금 답"을 기억하고 있다는 뜻 — `messages` 리스트가 정상적으로 누적됐음을 확인).

- [ ] **Step 6: Step 4 실행 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\prompt_and_graph.py --step 4`
Expected: Step 3과 동일한 2턴 패턴(짧아진 2턴 답변)이 출력되지만, 코드에는 `messages.append`가 없다 — `thread_id="demo"` 하나로 히스토리가 자동 복원됐음을 콘솔 마지막 문장으로 확인.

- [ ] **Step 7: 전체 실행(4단계) 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\prompt_and_graph.py`
Expected: `[Step 1]`~`[Step 4]` 헤더가 순서대로 출력되며 4단계 모두 에러 없이 완주.

(이 태스크도 커밋하지 않는다 — Task 3에서 PPTX와 함께 커밋한다.)

---

## Task 3: 주제2 PPTX 제작 + 스크립트와 함께 커밋

**Files:**
- Create: `docs/교육자료/02_프롬프트와_LangGraph.pptx`
- (커밋 시 포함) `backend/examples/prompt_and_graph.py` (Task 1~2에서 이미 작성됨)

**Interfaces:**
- Consumes: Task 1~2에서 완성된 `prompt_and_graph.py`의 코드 내용(슬라이드에 그대로 인용), `backend/app/graph/builder.py`, `backend/app/graph/nodes/chat.py`, `backend/app/prompts.py`(실제 앱 코드 슬라이드용).

- [ ] **Step 1: 1편 PPTX를 스타일 참조로 확인**

`document-skills:pptx` 스킬로 `docs/교육자료/사내LLM연결_LangChain입문.pptx`를 열람해 레이아웃(카드형 3단 구성, "코드 뜯어보기 ①②③" 반복 패턴, 하단 진행 표시 "사내 LLM 연결 & LangChain 입문 · N")을 확인한다. 이번 파일의 하단 진행 표시는 "프롬프트 & LangGraph · N"으로 바꾼다.

- [ ] **Step 2: 슬라이드 13장 작성**

`document-skills:pptx` 스킬로 아래 내용을 담아 `docs/교육자료/02_프롬프트와_LangGraph.pptx`를 만든다:

1. **제목**: "교육자료 · 2편" / "프롬프트 입력 & LangChain vs LangGraph" / 부제 "프롬프트를 구조화하고, 멀티턴 대화의 상태를 누가 관리하는지 코드로 비교한다" / 아이콘 흐름: 앱 코드 → LangChain → LangGraph
2. **AGENDA**: 1 프롬프트 구조화(문자열 한 줄 → 역할·변수가 있는 템플릿) / 2 LangChain만으로 멀티턴(히스토리를 손으로 관리하는 불편함) / 3 LangGraph로 전환(상태 관리를 위임) / 4 실전 비교(언제 뭘 쓰나)
3. **개념**: "프롬프트란 무엇인가" — 문자열 / 역할 있는 메시지 리스트 / 변수가 있는 템플릿, 3단 비교 카드
4. **코드 뜯어보기 ①**: Step 1 코드(`model.invoke("자기소개해줘")`) — 포인트: 역할 지정이 안 됨, 재사용 불가
5. **코드 뜯어보기 ②**: Step 2 코드(`ChatPromptTemplate.from_messages` + `{role}`/`{topic}`) — 포인트: "프롬프트 = 템플릿 + 변수"
6. **코드 뜯어보기 ③**: Step 3 코드(`messages.append(...)` 반복) — 포인트: 턴마다 히스토리 관리를 직접 해야 함
7. **다이어그램**: Step 3 문제점 — "매 턴마다 append → invoke → append" 를 손으로 반복하는 흐름도
8. **코드 뜯어보기 ④**: Step 4 코드(`StateGraph` + `add_messages` + `MemorySaver` + `thread_id`)
9. **다이어그램**: LangGraph 내부 동작 — `add_messages` 리듀서가 자동 누적, `checkpointer`가 `thread_id`별로 영속화
10. **비교표**: LangChain 단독 vs LangGraph — 히스토리 관리(수동/자동), 세션 복원(불가/가능), 분기·반복(직접 구현/그래프로 표현), 코드량
11. **우리 앱은 이렇게**: `app/graph/builder.py`(22줄 전체) + `app/graph/nodes/chat.py`의 `make_chat_node` + `app/prompts.py`의 `CHAT_SYSTEM_PROMPT` 한 컷 — "지금 배운 걸 그대로 쓰고 있다"
12. **정리**: "LangChain은 모델 호출을 표준화하고, LangGraph는 대화의 상태·흐름을 표준화한다. 한 번 묻고 끝이면 LangChain만으로 충분하고, 여러 턴에 걸친 기억·분기·반복이 필요하면 LangGraph." 외 핵심 3문장
13. **다음 단계 예고**: "주제 3: Tool calling — LLM이 직접 계산 대신 도구를 부르는 법"

- [ ] **Step 3: 슬라이드 텍스트 육안 확인**

`document-skills:pptx` 스킬(또는 `python-pptx`로 텍스트 추출)로 13장 모두 제목·본문이 비어있지 않은지, 1편과 톤이 일치하는지 확인한다.

- [ ] **Step 4: 스크립트 + PPTX 함께 커밋**

```bash
git add backend/examples/prompt_and_graph.py "docs/교육자료/02_프롬프트와_LangGraph.pptx"
git commit -m "Add teaching slides + example: 프롬프트 입력 & LangChain vs LangGraph

13-slide PPTX for educational topic 2, paired with
backend/examples/prompt_and_graph.py (4-step runnable example)."
```

---

## Task 4: 주제3 실습 스크립트 — 골격 + Step 1~3

**Files:**
- Create: `backend/examples/tool_calling_step_by_step.py`

**Interfaces:**
- Consumes: 없음(표준 라이브러리 + 기존 의존성만).
- Produces: `load_env_file`, `build_model`(Task 1과 동일 패턴), `multiply` 도구, `step_1`~`step_3`, `STEPS`, `main`.

- [ ] **Step 1: 파일 골격 + Step 1~3 작성**

`backend/examples/tool_calling_step_by_step.py`:
```python
"""tool-calling 을 원리부터 단계별로 뜯어보는 실습 (한 파일).

교육자료 "Tool calling" 의 실습용 예제다. LLM 이 큰 수 곱셈을 틀리는 것에서
시작해, 도구 정의 -> bind_tools 로 tool_calls 확인 -> 수동 왕복 루프 ->
create_react_agent 한 줄 -> 엑셀 셀 write/read 도구까지 6단계로 tool calling 의
원리를 보여준다.

실행 (backend/ 에서, venv 파이썬으로):
    python examples/tool_calling_step_by_step.py            # 전체 6단계
    python examples/tool_calling_step_by_step.py --step 3    # 특정 단계만
    python examples/tool_calling_step_by_step.py --backend internal

설치: langchain-openai, langchain-ollama, langchain-core, langgraph, httpx
     (백엔드 의존성에 이미 포함) / Step 6 만 선택적으로 xlwings 필요
     (pip install xlwings, 이 PC 에 Microsoft Excel 설치 필요 - 없으면 SKIP)
"""
import argparse
import os
from pathlib import Path

import httpx
from langchain_core.messages import HumanMessage, ToolMessage
from langchain_core.tools import tool
from langchain_ollama import ChatOllama
from langchain_openai import ChatOpenAI
from langgraph.prebuilt import create_react_agent

A, B = 4823, 6791  # 암산으로는 못 맞히는 크기 - 도구가 필요한 이유를 보여준다


def load_env_file() -> None:
    """backend/.env 를 읽어 환경변수로 채운다(이미 설정된 값은 덮어쓰지 않음)."""
    env_path = Path(__file__).resolve().parents[1] / ".env"
    if not env_path.exists():
        return
    for line in env_path.read_text(encoding="utf-8").splitlines():
        line = line.strip()
        if not line or line.startswith("#") or "=" not in line:
            continue
        key, _, val = line.partition("=")
        os.environ.setdefault(key.strip(), val.strip())


load_env_file()

for var in ("HTTP_PROXY", "HTTPS_PROXY", "http_proxy", "https_proxy"):
    os.environ.pop(var, None)


def build_model(backend: str | None = None):
    """LLM_BACKEND(또는 --backend 인자)에 따라 ChatOpenAI/ChatOllama 를 만든다."""
    backend = backend or os.environ.get("LLM_BACKEND", "ollama")
    if backend == "internal":
        missing = [
            k for k in ("INTERNAL_LLM_BASE_URL", "INTERNAL_LLM_MODEL")
            if not os.environ.get(k)
        ]
        if missing:
            raise SystemExit(
                "사내 LLM 설정이 비어 있습니다: " + ", ".join(missing)
                + "\nbackend/.env 를 채우세요."
            )
        return ChatOpenAI(
            base_url=os.environ["INTERNAL_LLM_BASE_URL"],
            api_key=os.environ.get("INTERNAL_LLM_API_KEY", "not-needed"),
            model=os.environ["INTERNAL_LLM_MODEL"],
            http_client=httpx.Client(verify=False),
        )
    return ChatOllama(
        model=os.environ.get("OLLAMA_MODEL", "gemma3n:e4b"),
        base_url=os.environ.get("OLLAMA_BASE_URL", "http://localhost:11434"),
    )


@tool
def multiply(a: int, b: int) -> int:
    """두 정수 a, b 를 곱한 값을 돌려준다. 곱셈이 필요하면 반드시 이 도구를 써라."""
    return a * b


def step_1(model) -> None:
    """Step 1: 도구 없이 큰 수 곱셈을 직접 시키기"""
    reply = model.invoke(f"{A} 곱하기 {B} 는 얼마야? 숫자만 답해.")
    correct = A * B
    print(f"모델 답변: {reply.content!r}")
    print(f"정답({correct})이 답변에 포함됐는가: {str(correct) in reply.content}")


def step_2(model) -> None:
    """Step 2: @tool 로 도구를 정의만 해보기 (아직 LLM 에 연결 안 함)"""
    print("도구 이름:", multiply.name)
    print("도구 설명(LLM 이 읽는 부분):", multiply.description)
    print("도구 스키마:", multiply.args)
    print("직접 호출:", multiply.invoke({"a": A, "b": B}))


def step_3(model) -> None:
    """Step 3: bind_tools - LLM 이 '부르고 싶다'고 말하는 것을 확인"""
    bound = model.bind_tools([multiply])
    reply = bound.invoke(
        [HumanMessage(content=f"{A} 곱하기 {B} 를 계산해줘. 반드시 도구를 써라.")]
    )
    print("tool_calls:", reply.tool_calls)
    print("content(비어있을 수 있다 - 아직 계산 안 함):", repr(reply.content))
    print(
        "\n주의: 이 시점까지 실제 곱셈은 한 번도 실행되지 않았다 - "
        "LLM 은 '불러달라'는 요청만 만들었다."
    )


STEPS = {1: step_1, 2: step_2, 3: step_3}


def main() -> int:
    parser = argparse.ArgumentParser()
    parser.add_argument("--step", type=int, choices=sorted(STEPS), default=None)
    parser.add_argument("--backend", choices=("internal", "ollama"), default=None)
    args = parser.parse_args()

    model = build_model(args.backend)
    steps = [STEPS[args.step]] if args.step else list(STEPS.values())
    for fn in steps:
        print(f"\n{'=' * 60}\n[{fn.__doc__}]\n{'=' * 60}")
        fn(model)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: Step 1 실행 확인 (도구 없이 → 오답 유도)**

Run: `cd backend && .venv\Scripts\python.exe examples\tool_calling_step_by_step.py --step 1`
Expected: `모델 답변:` 뒤에 숫자가 출력되고, `정답(...)이 답변에 포함됐는가: False`인 경우가 많음(모델이 암산으로 큰 수 곱셈을 틀리는 것을 보여주는 것이 목적 — 우연히 맞혀도 실습 진행에는 지장 없음, 슬라이드에서는 "대개 틀린다"로 서술).

- [ ] **Step 3: Step 2 실행 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\tool_calling_step_by_step.py --step 2`
Expected: `도구 이름: multiply`, `도구 설명: ...곱셈이 필요하면...`, `직접 호출: 32752232`(= 4823×6791)가 출력.

- [ ] **Step 4: Step 3 실행 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\tool_calling_step_by_step.py --step 3`
Expected: `tool_calls: [{'name': 'multiply', 'args': {'a': 4823, 'b': 6791}, ...}]` 형태가 출력되고, `content`가 비어있거나 짧은 문자열임을 확인(모델이 아직 결과를 모른다는 뜻).

(이 태스크는 커밋하지 않는다 — Task 6에서 PPTX와 함께 커밋한다.)

---

## Task 5: 주제3 실습 스크립트 — Step 4~6 추가 (수동 루프 / react agent / 엑셀 왕복)

**Files:**
- Modify: `backend/examples/tool_calling_step_by_step.py`

**Interfaces:**
- Consumes: Task 4의 `multiply`, `build_model`, `STEPS`, `main` 구조.
- Produces: `step_4(model) -> None`, `step_5(model) -> None`, `step_6(model) -> None`, 확장된 `STEPS`(1~6).

- [ ] **Step 1: step_4 작성 (수동 왕복 루프)**

`step_3` 함수 바로 아래에 추가:
```python
def step_4(model) -> None:
    """Step 4: 수동 왕복 루프 - 도구를 실제로 실행하고 결과를 되돌려준다"""
    tools_by_name = {multiply.name: multiply}
    bound = model.bind_tools([multiply])

    messages = [
        HumanMessage(content=f"{A} 곱하기 {B} 를 계산해줘. 반드시 도구를 써라.")
    ]
    ai_msg = bound.invoke(messages)
    messages.append(ai_msg)

    for call in ai_msg.tool_calls:
        result = tools_by_name[call["name"]].invoke(call["args"])
        print(f"[실행] {call['name']}({call['args']}) = {result}")
        messages.append(ToolMessage(content=str(result), tool_call_id=call["id"]))

    final = bound.invoke(messages)
    print("최종 답변:", final.content)
```

- [ ] **Step 2: step_5 작성 (create_react_agent 로 대체)**

`step_4` 함수 바로 아래에 추가:
```python
def step_5(model) -> None:
    """Step 5: create_react_agent 한 줄로 Step 4 의 루프를 대체"""
    agent = create_react_agent(model, [multiply])
    result = agent.invoke(
        {
            "messages": [
                HumanMessage(content=f"{A} 곱하기 {B} 를 계산해줘. 반드시 도구를 써라.")
            ]
        }
    )
    used = [
        c["name"]
        for m in result["messages"]
        for c in (getattr(m, "tool_calls", None) or [])
    ]
    print("호출된 도구:", used)
    print("최종 답변:", result["messages"][-1].content)
```

- [ ] **Step 3: step_6 작성 (엑셀 write→read 왕복)**

`step_5` 함수 바로 아래에 추가:
```python
def step_6(model) -> None:
    """Step 6: 엑셀 셀 write→read 왕복 - 실무 데이터에도 그대로 적용"""
    try:
        import xlwings as xw
    except ImportError:
        print("SKIP: xlwings 미설치 (pip install xlwings)")
        return

    try:
        app = xw.App(visible=False, add_book=False)
    except Exception as exc:  # noqa: BLE001
        print(f"SKIP: Excel COM 사용 불가 ({exc})")
        return

    app.display_alerts = False
    book = None
    try:
        book = app.books.add()
        sheet = book.sheets[0]

        @tool
        def excel_write_cell(address: str, value: str) -> str:
            """엑셀 시트의 특정 셀(예 'B2')에 값을 기록한다."""
            sheet.range(address).value = value
            return f"{address} 에 '{value}' 를 기록했다."

        @tool
        def excel_read_cell(address: str) -> str:
            """엑셀 시트의 특정 셀(예 'B2') 값을 읽어 돌려준다."""
            return f"{address} = {sheet.range(address).value}"

        agent = create_react_agent(model, [excel_write_cell, excel_read_cell])
        token = "엑셀툴검증OK"
        prompt = (
            f"먼저 excel_write_cell 로 셀 B2 에 '{token}' 를 기록해라. "
            "그다음 excel_read_cell 로 B2 를 읽어 그 값을 그대로 알려줘. "
            "반드시 두 도구를 순서대로 사용해라."
        )
        result = agent.invoke({"messages": [HumanMessage(content=prompt)]})
        used = [
            c["name"]
            for m in result["messages"]
            for c in (getattr(m, "tool_calls", None) or [])
        ]
        print("호출된 도구:", used)
        print("실제 B2 셀 값:", sheet.range("B2").value)
        print("최종 답변:", result["messages"][-1].content)
    finally:
        try:
            if book is not None:
                book.close()
        finally:
            app.quit()
```

- [ ] **Step 4: STEPS 딕셔너리 확장**

```python
STEPS = {1: step_1, 2: step_2, 3: step_3, 4: step_4, 5: step_5, 6: step_6}
```

- [ ] **Step 5: Step 4 실행 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\tool_calling_step_by_step.py --step 4`
Expected: `[실행] multiply({'a': 4823, 'b': 6791}) = 32752232` 출력 후, `최종 답변:`에 `32752232`가 포함됨.

- [ ] **Step 6: Step 5 실행 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\tool_calling_step_by_step.py --step 5`
Expected: `호출된 도구: ['multiply']`, `최종 답변:`에 `32752232` 포함 — Step 4와 동일 결과를 더 적은 코드로 확인.

- [ ] **Step 7: Step 6 실행 확인 (Excel 있는 PC 에서)**

Run: `cd backend && .venv\Scripts\python.exe examples\tool_calling_step_by_step.py --step 6`
Expected: Excel + xlwings 있는 PC에서는 `호출된 도구: ['excel_write_cell', 'excel_read_cell']`, `실제 B2 셀 값: 엑셀툴검증OK`가 출력. 둘 중 하나 없는 PC에서는 `SKIP: ...` 한 줄만 출력되고 정상 종료(에러 아님).

- [ ] **Step 8: 전체 실행(6단계) 확인**

Run: `cd backend && .venv\Scripts\python.exe examples\tool_calling_step_by_step.py`
Expected: `[Step 1]`~`[Step 6]` 헤더가 순서대로 출력되며 전체 완주(Step 6은 환경에 따라 SKIP 가능).

(이 태스크도 커밋하지 않는다 — Task 6에서 PPTX와 함께 커밋한다.)

---

## Task 6: 주제3 PPTX 제작 + 스크립트와 함께 커밋

**Files:**
- Create: `docs/교육자료/03_ToolCalling.pptx`
- (커밋 시 포함) `backend/examples/tool_calling_step_by_step.py` (Task 4~5에서 이미 작성됨)

**Interfaces:**
- Consumes: Task 4~5에서 완성된 `tool_calling_step_by_step.py`의 코드 내용, `docs/superpowers/plans/2026-07-24-excel-analysis-agent.md`(다음 단계 예고용 링크).

- [ ] **Step 1: 슬라이드 14장 작성**

`document-skills:pptx` 스킬로 1편·2편과 동일한 시각 스타일을 따르되 하단 진행 표시는 "Tool calling · N"으로, 아래 내용을 담아 `docs/교육자료/03_ToolCalling.pptx`를 만든다:

1. **제목**: "교육자료 · 3편" / "Tool calling" / 부제 "LLM이 계산을 틀리는 이유에서 시작해, 도구를 정의하고 호출하는 원리를 코드로 뜯어본다"
2. **AGENDA**: 1 왜 필요한가(도구 없이 vs 있이) / 2 정의와 바인딩(`@tool`, `bind_tools`) / 3 실행 원리(수동 루프 vs `create_react_agent`) / 4 실전(엑셀 도구)
3. **개념**: "tool calling 이란" — 핵심 오해 교정: "LLM은 함수를 직접 실행하지 않는다. '이 도구를, 이 인자로 불러줘'라고 말할 뿐이고, 실제 실행은 우리 코드가 한다."
4. **코드 뜯어보기 ①**: Step 1 코드 + 실행 결과(오답) — 포인트: "왜 도구가 필요한가"
5. **코드 뜯어보기 ②**: Step 2 코드(`@tool` + docstring) — 포인트: "docstring이 곧 LLM용 설명서"
6. **코드 뜯어보기 ③**: Step 3 코드(`bind_tools` + `tool_calls` 출력) — 포인트: "아직 계산은 실행되지 않았다"
7. **다이어그램**: 도구 호출 왕복 흐름 — 우리 코드(요청) → LLM(`tool_calls` 응답) → 우리 코드(실제 실행) → `ToolMessage`로 결과 전달 → LLM(재호출, 최종 답)
8. **코드 뜯어보기 ④**: Step 4 코드(수동 루프 — `tool_calls` 파싱 → 실행 → `ToolMessage` 조립 → 재호출)
9. **코드 뜯어보기 ⑤**: Step 5 코드(`create_react_agent(model, [multiply])` 한 줄)
10. **비교표**: 수동 루프 vs `create_react_agent` — 코드량, 다중 도구 처리, 반복 호출(에이전트 루프) 지원 여부
11. **코드 뜯어보기 ⑥**: Step 6 코드(엑셀 `excel_write_cell`/`excel_read_cell` 도구 정의 + 실행 프롬프트)
12. **실전 결과**: Step 6 실행 시 콘솔 출력 예시(`호출된 도구`, `실제 B2 셀 값`, `최종 답변`) — "실무 데이터(엑셀)에도 그대로 적용된다"
13. **정리**: "LLM은 도구를 실행하지 않고 호출을 요청한다", "`@tool`의 docstring이 LLM에게 주는 유일한 설명서다", "`create_react_agent`는 수동 왕복 루프를 감싼 것뿐이다", "도구만 바뀌면 같은 패턴으로 어떤 업무든 자동화할 수 있다" 등 핵심 4문장
14. **다음 단계 예고**: "엑셀 분석 에이전트는 이미 설계·사전검증 완료(`verify_tool_calling.py` PASS 기준) — 다음 자료에서 이어감" (`docs/superpowers/plans/2026-07-24-excel-analysis-agent.md` 언급)

- [ ] **Step 2: 슬라이드 텍스트 육안 확인**

`document-skills:pptx` 스킬(또는 `python-pptx`로 텍스트 추출)로 14장 모두 제목·본문이 비어있지 않은지, 1·2편과 톤이 일치하는지 확인한다.

- [ ] **Step 3: 스크립트 + PPTX 함께 커밋**

```bash
git add backend/examples/tool_calling_step_by_step.py "docs/교육자료/03_ToolCalling.pptx"
git commit -m "Add teaching slides + example: Tool calling

14-slide PPTX for educational topic 3, paired with
backend/examples/tool_calling_step_by_step.py (6-step runnable example,
Excel round-trip step SKIPs gracefully without xlwings/Excel)."
```

---

## Self-Review

- **Spec 커버리지**: 설계 문서(`docs/superpowers/specs/2026-07-25-교육자료-2-3편-design.md`)의 2편 Step 1~4 표, 3편 Step 1~6 표, "우리 앱은 이렇게" 슬라이드, 정리 슬라이드 핵심 문장, 다음 단계 예고, 공통 골격(`--step N` 디스패치), 커밋 방식(주제별 1커밋)이 모두 Task 1~6에 매핑됨.
- **Placeholder 스캔**: 모든 코드 스텝에 실제 실행 가능한 코드가 포함됨. TBD/TODO 없음. PPTX 슬라이드도 각 장의 제목·본문·강조 포인트를 구체적으로 명시(빈 "적절한 내용 추가" 없음).
- **타입/이름 일관성**: `build_model(backend: str | None = None)`, `STEPS: dict[int, Callable]`, `main()`의 `--step`/`--backend` 인자명이 Task 1(주제2)과 Task 4(주제3)에서 동일하게 유지됨. `multiply` 도구 이름·시그니처가 Task 4~5에서 일관되게 사용됨.
